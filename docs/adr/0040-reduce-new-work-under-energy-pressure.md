# Reduce new work under energy pressure

Each Plane applies an Energy policy expressed as a concurrency budget. Low-power state prevents new background Runs and subagents above the reduced limit but never kills active work. A Jet Craft declares whether it can observe or limit native subagents; Jet applies native controls when supported and reports monitor-only behavior otherwise. Foreground user-started work may override the budget through an explicit, visible action.
