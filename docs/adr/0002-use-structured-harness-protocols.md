# Use structured harness protocols

Jet is a structured harness orchestrator rather than a terminal multiplexer. Harness-native protocols are authoritative for conversations, approvals, plans, diffs, usage, and other semantic events; terminal and screen inspection remain compatibility fallbacks so Jet can preserve rich harness behavior without reducing every integration to terminal text.
