//! `jetfueld` keeps a Harness Run or Workspace terminal alive while `jetd`
//! is unavailable. It is a separate executable by design (ADR-0060) and is
//! implemented under the execution-recovery issue.

fn main() {
	eprintln!("jetfueld is not implemented yet");
	std::process::exit(1);
}
