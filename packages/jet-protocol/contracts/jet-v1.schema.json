{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "JetContracts",
  "type": "object",
  "properties": {
    "client_hello": {
      "$ref": "#/$defs/ClientHello"
    },
    "client_message": {
      "$ref": "#/$defs/ClientMessage"
    },
    "connection_proof": {
      "$ref": "#/$defs/ConnectionProof"
    },
    "event": {
      "$ref": "#/$defs/Event"
    },
    "pairing_request": {
      "$ref": "#/$defs/RemotePairingRequest"
    },
    "pairing_response": {
      "$ref": "#/$defs/RemotePairingResponse"
    },
    "plane_status": {
      "$ref": "#/$defs/PlaneStatus"
    },
    "server_hello": {
      "$ref": "#/$defs/ServerHello"
    },
    "server_message": {
      "$ref": "#/$defs/ServerMessage"
    },
    "stream_control": {
      "$ref": "#/$defs/StreamControl"
    },
    "wire_error": {
      "$ref": "#/$defs/WireError"
    }
  },
  "required": [
    "client_hello",
    "server_hello",
    "connection_proof",
    "pairing_request",
    "pairing_response",
    "client_message",
    "server_message",
    "stream_control",
    "event",
    "plane_status",
    "wire_error"
  ],
  "$defs": {
    "AccountBinding": {
      "description": "One Plane-local Account binding.",
      "type": "object",
      "properties": {
        "binding_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        },
        "created_at_unix_ms": {
          "description": "When the binding was established, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "credential_reference": {
          "description": "The reference its Credential resolves through.",
          "$ref": "#/$defs/CredentialReference"
        },
        "label": {
          "description": "The user-facing name of the binding.",
          "type": "string"
        },
        "provider": {
          "description": "The Provider this binding authenticates to, such as `anthropic`.",
          "type": "string"
        },
        "provider_account": {
          "description": "The Provider's own account identity, when it supplies one. Bindings\ngroup into one Provider account automatically only when they share\none.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "binding_id",
        "provider",
        "label",
        "credential_reference",
        "created_at_unix_ms"
      ]
    },
    "AccountBindingList": {
      "description": "Every Account binding on one Plane, fenced by a journal cursor\n(ADR-0092).",
      "type": "object",
      "properties": {
        "bindings": {
          "description": "The bindings in the order they were established.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/AccountBindingStatus"
          }
        },
        "cursor": {
          "description": "Newest Event sequence visible when the snapshot was read, carried as\na decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        }
      },
      "required": [
        "cursor",
        "bindings"
      ]
    },
    "AccountBindingStatus": {
      "description": "One Account binding beside the state of the Credential it resolves. The\nbinding is durable Plane state; the state beside it is observed when the\nQuery runs.",
      "type": "object",
      "properties": {
        "binding": {
          "description": "The binding as the Plane recorded it.",
          "$ref": "#/$defs/AccountBinding"
        },
        "credential_state": {
          "description": "Whether its Credential can be resolved right now.",
          "$ref": "#/$defs/CredentialState"
        }
      },
      "required": [
        "binding",
        "credential_state"
      ]
    },
    "Actor": {
      "description": "Who caused an Event.",
      "oneOf": [
        {
          "description": "An interactive GUI client.",
          "type": "object",
          "properties": {
            "client_id": {
              "description": "The client's durable identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "interactive_client"
            }
          },
          "required": [
            "type",
            "client_id"
          ]
        }
      ]
    },
    "ArtifactAvailability": {
      "description": "Whether complete Artifact bytes were retained within ingestion limits.",
      "oneOf": [
        {
          "description": "Complete bytes are stored under the advertised hash.",
          "type": "string",
          "const": "stored"
        },
        {
          "description": "Only hash and size were retained because the Run budget was exhausted.",
          "type": "string",
          "const": "run_budget_exceeded"
        },
        {
          "description": "Only hash and size were retained because the patch exceeded the size limit.",
          "type": "string",
          "const": "artifact_size_exceeded"
        }
      ]
    },
    "AuditActor": {
      "description": "The origin of a Security-audit decision, independent of Command authority.",
      "oneOf": [
        {
          "description": "An authenticated interactive GUI client.",
          "type": "object",
          "properties": {
            "client_id": {
              "description": "The client's durable identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "interactive_client"
            }
          },
          "required": [
            "type",
            "client_id"
          ]
        },
        {
          "description": "Jet applied signed release revocations on this Plane.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "craft_revocation"
            }
          },
          "required": [
            "type"
          ]
        }
      ]
    },
    "AuditBreach": {
      "description": "How the Security audit failed to validate. It names positions and\nhashes and quotes no record content, because the audit holds none.",
      "oneOf": [
        {
          "description": "The Plane has recorded decisions, but nothing outside the store says\nhow far its chain had reached.",
          "type": "object",
          "properties": {
            "breach": {
              "type": "string",
              "const": "head_missing"
            }
          },
          "required": [
            "breach"
          ]
        },
        {
          "description": "The store does not hold the record the head names, so it moved\nbackwards behind the audit.",
          "type": "object",
          "properties": {
            "breach": {
              "type": "string",
              "const": "head_not_in_store"
            }
          },
          "required": [
            "breach"
          ]
        },
        {
          "description": "The store holds that record, and it is not the one the head names,\nso the history behind it was rewritten.",
          "type": "object",
          "properties": {
            "breach": {
              "type": "string",
              "const": "head_diverged"
            }
          },
          "required": [
            "breach"
          ]
        },
        {
          "description": "The record at this position no longer folds to its own link.",
          "type": "object",
          "properties": {
            "breach": {
              "type": "string",
              "const": "record_altered"
            },
            "sequence": {
              "description": "Where the fold first disagreed, as a decimal string.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            }
          },
          "required": [
            "breach",
            "sequence"
          ]
        },
        {
          "description": "The identity at this position is not the one its opaque target\nreference was derived from.",
          "type": "object",
          "properties": {
            "breach": {
              "type": "string",
              "const": "target_altered"
            },
            "sequence": {
              "description": "Where the target and its reference first disagreed, as a\ndecimal string.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            }
          },
          "required": [
            "breach",
            "sequence"
          ]
        }
      ]
    },
    "AuditEntry": {
      "description": "One recorded decision.",
      "type": "object",
      "properties": {
        "actor": {
          "description": "The responsible origin, which grants no Command authority.",
          "$ref": "#/$defs/AuditActor"
        },
        "decision": {
          "description": "What was decided, such as `account.bound`.",
          "type": "string"
        },
        "epoch": {
          "description": "The authority epoch the record belongs to, carried as a decimal\nstring. It changes only when an owner explicitly carries on past an\nintegrity failure.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "outcome": {
          "description": "What became of it.",
          "$ref": "#/$defs/AuditOutcome"
        },
        "plane_id": {
          "description": "The Plane that made it.",
          "type": "string",
          "format": "uuid"
        },
        "record_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        },
        "recorded_at_unix_ms": {
          "description": "When the decision was made, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "risk": {
          "description": "How much it could cost.",
          "$ref": "#/$defs/AuditRisk"
        },
        "sequence": {
          "description": "Plane-local audit position, carried as a decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "target": {
          "description": "What it was about.",
          "$ref": "#/$defs/AuditTarget"
        }
      },
      "required": [
        "sequence",
        "epoch",
        "record_id",
        "recorded_at_unix_ms",
        "plane_id",
        "actor",
        "target",
        "decision",
        "risk",
        "outcome"
      ]
    },
    "AuditHead": {
      "description": "How far the audit chain had reached when its head was last published.",
      "type": "object",
      "properties": {
        "entry_hash": {
          "description": "Lowercase hexadecimal of the chain link it folded to.",
          "type": "string"
        },
        "epoch": {
          "description": "The epoch the record it names belongs to, as a decimal string.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "sequence": {
          "description": "That record's position, as a decimal string.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        }
      },
      "required": [
        "epoch",
        "sequence",
        "entry_hash"
      ]
    },
    "AuditOutcome": {
      "description": "What became of a decision.",
      "oneOf": [
        {
          "description": "It was carried out.",
          "type": "string",
          "const": "succeeded"
        },
        {
          "description": "It was refused before anything changed.",
          "type": "string",
          "const": "denied"
        },
        {
          "description": "It was allowed but did not complete.",
          "type": "string",
          "const": "failed"
        }
      ]
    },
    "AuditRisk": {
      "description": "How much a decision could cost if it was not the one the owner intended,\nas the Plane judged it when the decision was made.",
      "oneOf": [
        {
          "description": "Recorded so it can be reviewed; it widens nothing and destroys\nnothing.",
          "type": "string",
          "const": "routine"
        },
        {
          "description": "Widens trust, changes policy, or exposes state.",
          "type": "string",
          "const": "elevated"
        },
        {
          "description": "May destroy state that cannot be brought back from within Jet.",
          "type": "string",
          "const": "destructive"
        }
      ]
    },
    "AuditTarget": {
      "description": "What one recorded decision was about.",
      "type": "object",
      "properties": {
        "identity": {
          "description": "The target's own identity, while the Plane still keeps the target.\nDeleting the target leaves only `reference`.",
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "description": "The kind of thing it was about, such as `account_binding`. A client\nthat does not know a kind shows the record generically (ADR-0094).",
          "type": "string"
        },
        "reference": {
          "description": "Lowercase hexadecimal of the opaque identifier the audit's integrity\nchain covers. It outlives the target, so records about one thing\nstill group together after that thing is deleted.",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "reference"
      ]
    },
    "BaseSelection": {
      "description": "The base a Workspace starts from, as the user selects it. The Plane\nresolves it to one commit when the Workspace is created and never\nagain.",
      "oneOf": [
        {
          "description": "Whatever the Project's Local checkout has checked out.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "head"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "A branch, tag, or other revision as Git spells it.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "revision"
            },
            "revision": {
              "description": "The revision name.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "revision"
          ]
        }
      ]
    },
    "BrokerPermission": {
      "description": "Jet-enforced broker permissions, all required declarations in v1.",
      "oneOf": [
        {
          "description": "Read authorized Artifacts through the broker.",
          "type": "string",
          "const": "artifact_read"
        },
        {
          "description": "Publish Artifacts through the broker.",
          "type": "string",
          "const": "artifact_write"
        },
        {
          "description": "Use authorized paired-Plane tools through the broker.",
          "type": "string",
          "const": "remote_tools"
        }
      ]
    },
    "CapabilityObservation": {
      "description": "Which snapshot a Capability Query answers with. A Plane is observed at\nstartup and whenever a caller asks for a new look, never on a timer.",
      "oneOf": [
        {
          "description": "The snapshot the Plane observed most recently.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "last_observed"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "A new observation, taken now and kept as the Plane's latest.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "fresh"
            }
          },
          "required": [
            "type"
          ]
        }
      ]
    },
    "CapabilitySnapshot": {
      "description": "A point-in-time report of what a Plane can do.",
      "type": "object",
      "properties": {
        "core_version": {
          "description": "Version of the core that observed it.",
          "type": "string"
        },
        "crafts": {
          "description": "The Crafts installed on the Plane.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/InstalledCraft"
          }
        },
        "credential_store": {
          "description": "Whether credentials can be resolved without storing secrets.",
          "$ref": "#/$defs/CredentialStoreStatus"
        },
        "degraded": {
          "description": "What the Plane cannot do in this state.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/DegradedCondition"
          }
        },
        "external_tools": {
          "description": "Every external tool the core knows how to invoke.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ExternalToolStatus"
          }
        },
        "harnesses": {
          "description": "Every Harness an installed Craft adapts.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "observed_at_unix_ms": {
          "description": "When the Plane was observed, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "platform": {
          "description": "The operating system the Plane runs.",
          "$ref": "#/$defs/Platform"
        }
      },
      "required": [
        "observed_at_unix_ms",
        "core_version",
        "platform",
        "external_tools",
        "credential_store",
        "crafts",
        "harnesses",
        "degraded"
      ]
    },
    "ChangeArtifact": {
      "description": "An immutable payload under Jet's SHA-256 Artifact namespace.",
      "type": "object",
      "properties": {
        "availability": {
          "description": "Content availability; metadata-only references must not be treated as empty patches.",
          "$ref": "#/$defs/ArtifactAvailability",
          "default": "stored"
        },
        "sha256": {
          "description": "Lowercase SHA-256 content address.",
          "type": "string"
        },
        "size": {
          "description": "Full payload length in bytes.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "sha256",
        "size"
      ]
    },
    "ChangeArtifactChunk": {
      "description": "A bounded slice of an immutable patch, for clients to verify and assemble.",
      "type": "object",
      "properties": {
        "artifact": {
          "description": "Complete content address and size.",
          "$ref": "#/$defs/ChangeArtifact"
        },
        "bytes": {
          "description": "At most 64 KiB. Empty only at the end of the Artifact.",
          "type": "array",
          "items": {
            "type": "integer",
            "format": "uint8",
            "maximum": 255,
            "minimum": 0
          }
        },
        "offset": {
          "description": "Offset of these bytes in the complete Artifact.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        }
      },
      "required": [
        "artifact",
        "offset",
        "bytes"
      ]
    },
    "ChangeDiff": {
      "description": "A diff's boundaries, metadata, and bounded patch preview.",
      "type": "object",
      "properties": {
        "after": {
          "description": "Later state.",
          "$ref": "#/$defs/ChangeSnapshot"
        },
        "artifact": {
          "description": "Complete patch.",
          "$ref": "#/$defs/ChangeArtifact"
        },
        "before": {
          "description": "Earlier state.",
          "$ref": "#/$defs/ChangeSnapshot"
        },
        "cursor": {
          "description": "Journal fence for the durable metadata; Current also inspects live Git.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "files": {
          "description": "Changed paths and origin evidence.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ChangedFile"
          }
        },
        "latest_turn": {
          "description": "Number of retained completed or interrupted turns.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "next_page": {
          "description": "Opaque continuation bound to this query and snapshot; expires after five minutes.",
          "anyOf": [
            {
              "$ref": "#/$defs/PageCursor"
            },
            {
              "type": "null"
            }
          ]
        },
        "outcome": {
          "description": "Present for a per-turn diff.",
          "anyOf": [
            {
              "$ref": "#/$defs/TurnOutcome"
            },
            {
              "type": "null"
            }
          ]
        },
        "patch": {
          "description": "UTF-8 preview of the patch; binary files use Git binary patches.",
          "type": "string"
        },
        "patch_truncated": {
          "description": "Whether the Artifact contains more bytes than this preview.",
          "type": "boolean"
        },
        "plane_id": {
          "description": "Plane where these changes were observed.",
          "type": "string",
          "format": "uuid"
        },
        "run_id": {
          "description": "Owning Run.",
          "type": "string",
          "format": "uuid"
        },
        "scope": {
          "description": "Compared boundaries.",
          "$ref": "#/$defs/DiffScope"
        },
        "total_files": {
          "description": "Total number of changed paths across every page.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "workspace_id": {
          "description": "Workspace, absent for Local-checkout Runs.",
          "type": [
            "string",
            "null"
          ],
          "format": "uuid"
        }
      },
      "required": [
        "total_files",
        "cursor",
        "plane_id",
        "run_id",
        "scope",
        "latest_turn",
        "before",
        "after",
        "files",
        "artifact",
        "patch",
        "patch_truncated"
      ]
    },
    "ChangeKind": {
      "description": "What a promotion does to one path.",
      "oneOf": [
        {
          "description": "The destination gains the path.",
          "type": "string",
          "const": "added"
        },
        {
          "description": "The destination's content or mode at the path changes.",
          "type": "string",
          "const": "modified"
        },
        {
          "description": "The destination loses the path.",
          "type": "string",
          "const": "deleted"
        }
      ]
    },
    "ChangeOrigin": {
      "description": "Attribution justified by exact content and durable activity evidence.",
      "oneOf": [
        {
          "description": "An authenticated direct edit through Jet.",
          "type": "object",
          "properties": {
            "client_id": {
              "description": "Client responsible for the edit.",
              "type": "string",
              "format": "uuid"
            },
            "kind": {
              "type": "string",
              "const": "user_edit"
            }
          },
          "required": [
            "kind",
            "client_id"
          ]
        },
        {
          "description": "An operation in a Jet-owned Workspace terminal.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "workspace_terminal"
            },
            "terminal_id": {
              "description": "Durable terminal identity.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "terminal_id"
          ]
        },
        {
          "description": "A correlated Harness file operation.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "harness"
            },
            "run_id": {
              "description": "Run responsible for the operation.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "run_id"
          ]
        },
        {
          "description": "A complete chain contains more than one origin.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "mixed"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "No activity evidence explains the complete content transition.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "external_or_unknown"
            }
          },
          "required": [
            "kind"
          ]
        }
      ]
    },
    "ChangeSnapshot": {
      "description": "Git state at an observed boundary, independent of Harness commits.",
      "type": "object",
      "properties": {
        "commit": {
          "description": "Checked-out commit at capture time.",
          "type": "string"
        },
        "content_complete": {
          "description": "False when oversized file content was omitted; file pages retain metadata.",
          "type": "boolean"
        },
        "tree": {
          "description": "Captured working-tree object, including uncommitted changes.",
          "type": "string"
        },
        "uncommitted": {
          "description": "Binary-capable patch from commit to working tree.",
          "$ref": "#/$defs/ChangeArtifact"
        }
      },
      "required": [
        "content_complete",
        "commit",
        "tree",
        "uncommitted"
      ]
    },
    "ChangedFile": {
      "description": "One changed path and the Git content that establishes its change.",
      "type": "object",
      "properties": {
        "after_mode": {
          "description": "Resulting Git mode, zero for deletions.",
          "type": "string"
        },
        "after_object": {
          "description": "Resulting Git object, zeros for deletions; absent when content was omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "after_size": {
          "description": "Observed size of omitted resulting content, when available.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "before_mode": {
          "description": "Original Git mode, zero for additions.",
          "type": "string"
        },
        "before_object": {
          "description": "Original Git object, zeros for additions; absent when content was omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "before_size": {
          "description": "Observed size of omitted original content, when available.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "origin": {
          "description": "Evidence-backed attribution, never derived from timestamps.",
          "$ref": "#/$defs/ChangeOrigin"
        },
        "path": {
          "description": "Repository-relative path; renames appear as deletion and addition.",
          "type": "string"
        }
      },
      "required": [
        "path",
        "before_mode",
        "after_mode",
        "origin"
      ]
    },
    "Checkout": {
      "description": "Whether a working tree is checked out in full.",
      "oneOf": [
        {
          "description": "Every tracked path is present.",
          "type": "string",
          "const": "full"
        },
        {
          "description": "Sparse checkout narrows which paths are present.",
          "type": "string",
          "const": "sparse"
        }
      ]
    },
    "ClientHello": {
      "description": "First control frame from a client, sent right after the preface.",
      "type": "object",
      "properties": {
        "capabilities": {
          "description": "Protocol capability flags the client supports. No flags are defined\nin v1; compatible minors may introduce them.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "client_id": {
          "description": "Durable Client identity of the connecting installation.",
          "type": "string",
          "format": "uuid"
        },
        "codec": {
          "description": "Requested codec name.",
          "type": "string"
        },
        "max_control_frame": {
          "description": "Largest control frame the client is willing to receive.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "max_data_frame": {
          "description": "Largest data frame the client is willing to receive.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "minor": {
          "description": "Newest minor of the `max` major the client speaks (ADR-0019).",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "protocol": {
          "description": "Protocol majors the client can speak.",
          "$ref": "#/$defs/VersionRange"
        }
      },
      "required": [
        "protocol",
        "minor",
        "codec",
        "client_id",
        "max_control_frame",
        "max_data_frame"
      ]
    },
    "ClientMessage": {
      "description": "Control message sent by a client.",
      "oneOf": [
        {
          "description": "One bounded operation from an authenticated Home daemon.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Request correlation.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "remote_tool"
            },
            "request": {
              "description": "Destination Workspace, origin, and declared permissions.",
              "$ref": "#/$defs/RemoteToolRequest"
            }
          },
          "required": [
            "kind",
            "id",
            "request"
          ]
        },
        {
          "description": "Bind this numbered stream to a terminal. Disconnecting leaves it alive.",
          "type": "object",
          "properties": {
            "after": {
              "description": "First desired output byte.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "credit": {
              "description": "Initial output byte credit; further credit uses StreamControl.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "id": {
              "description": "Request correlation.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "attach_terminal"
            },
            "terminal_id": {
              "description": "Terminal identity.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "id",
            "terminal_id",
            "after",
            "credit"
          ]
        },
        {
          "description": "Resize the terminal attached to this stream.",
          "type": "object",
          "properties": {
            "columns": {
              "description": "Width in cells.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "id": {
              "description": "Request correlation.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "resize_terminal"
            },
            "rows": {
              "description": "Height in cells.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "id",
            "rows",
            "columns"
          ]
        },
        {
          "description": "Run a Query and return its snapshot.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Correlation identifier echoed in the reply.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "query"
            },
            "query": {
              "description": "The Query to run.",
              "$ref": "#/$defs/QueryRequest"
            },
            "timeout_ms": {
              "description": "How long the client is willing to wait, in milliseconds from\narrival, at most [`MAX_QUERY_TIMEOUT_MS`]. A Query is a\nnon-durable read, so abandoning it changes no Plane state\n(ADR-0095). Absent before protocol minor 19.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "id",
            "query"
          ]
        },
        {
          "description": "Execute a Command and return its durable outcome.",
          "type": "object",
          "properties": {
            "command": {
              "description": "The Command to execute.",
              "$ref": "#/$defs/CommandRequest"
            },
            "command_id": {
              "description": "Actor-scoped identity used to deduplicate retries.",
              "type": "string",
              "format": "uuid"
            },
            "id": {
              "description": "Correlation identifier echoed in the reply.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "command"
            }
          },
          "required": [
            "kind",
            "id",
            "command_id",
            "command"
          ]
        }
      ]
    },
    "ClientPublicKey": {
      "description": "The public half of one Client identity: the durable credential a\ncompleted Pairing leaves behind.",
      "type": "object",
      "properties": {
        "algorithm": {
          "description": "The algorithm it signs with.",
          "$ref": "#/$defs/PairingKeyAlgorithm"
        },
        "key": {
          "description": "The key itself, as 64 lowercase hexadecimal characters.",
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      },
      "required": [
        "algorithm",
        "key"
      ]
    },
    "CommandRequest": {
      "description": "Commands a client may execute.",
      "oneOf": [
        {
          "description": "Stage one native extension mutation for subsequent Runs (Jet 1.28).",
          "type": "object",
          "properties": {
            "confirmation": {
              "description": "Complete native consent snapshot.",
              "$ref": "#/$defs/ExtensionConfirmation"
            },
            "type": {
              "type": "string",
              "const": "change_extension"
            }
          },
          "required": [
            "type",
            "confirmation"
          ]
        },
        {
          "description": "Reviews one immutable No-Visa action captured by the destination.",
          "type": "object",
          "properties": {
            "client_id": {
              "description": "Paired installation that submitted the action.",
              "type": "string",
              "format": "uuid"
            },
            "decision": {
              "description": "Applies only to this exact stored action.",
              "$ref": "#/$defs/RemoteToolDecision"
            },
            "operation_id": {
              "description": "Captured remote operation identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "review_remote_tool"
            }
          },
          "required": [
            "type",
            "client_id",
            "operation_id",
            "decision"
          ]
        },
        {
          "description": "Admit one bounded Utility request.",
          "type": "object",
          "properties": {
            "request": {
              "description": "Purpose-specific input references.",
              "$ref": "#/$defs/UtilityRequest"
            },
            "type": {
              "type": "string",
              "const": "request_utility"
            }
          },
          "required": [
            "type",
            "request"
          ]
        },
        {
          "description": "Install only the exact Craft proposal returned by discovery.",
          "type": "object",
          "properties": {
            "craft_id": {
              "description": "Installed Craft identity, never an executable path.",
              "type": "string"
            },
            "mode": {
              "description": "Wait for pinned Runs or stop their Craft immediately.",
              "$ref": "#/$defs/CraftDisableMode"
            },
            "type": {
              "type": "string",
              "const": "disable_craft"
            }
          },
          "required": [
            "type",
            "craft_id",
            "mode"
          ]
        },
        {
          "description": "Install a confirmed Artifact as the default for subsequent Runs.",
          "type": "object",
          "properties": {
            "confirmation": {
              "description": "Repository, provenance, Artifact, authority, and trust acceptance.",
              "$ref": "#/$defs/CraftInstallationConfirmation"
            },
            "type": {
              "type": "string",
              "const": "install_craft"
            }
          },
          "required": [
            "type",
            "confirmation"
          ]
        },
        {
          "description": "Attach a daily Scheduled task to a retained Conversation.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "Owning Conversation.",
              "type": "string",
              "format": "uuid"
            },
            "local_time": {
              "description": "Daily local time in HH:MM:SS form.",
              "type": "string"
            },
            "prompt": {
              "description": "Scheduled input, 1 to 8192 UTF-8 bytes.",
              "type": "string"
            },
            "time_zone": {
              "description": "Original IANA zone.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "create_schedule"
            }
          },
          "required": [
            "type",
            "conversation_id",
            "time_zone",
            "local_time",
            "prompt"
          ]
        },
        {
          "description": "Cancel future firings and withdraw this schedule's pending input.",
          "type": "object",
          "properties": {
            "schedule_id": {
              "description": "Immutable schedule identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "cancel_schedule"
            }
          },
          "required": [
            "type",
            "schedule_id"
          ]
        },
        {
          "description": "Apply a bounded UTF-8 edit through a registered root.",
          "type": "object",
          "properties": {
            "content": {
              "description": "Complete replacement content, at most 128 KiB.",
              "type": "string"
            },
            "expected_revision": {
              "description": "Exact file state the editor read.",
              "$ref": "#/$defs/FileRevision"
            },
            "path": {
              "description": "Path relative to that root.",
              "type": "string"
            },
            "target": {
              "description": "Registered Project or Workspace root.",
              "$ref": "#/$defs/FileTarget"
            },
            "type": {
              "type": "string",
              "const": "apply_user_edit"
            }
          },
          "required": [
            "type",
            "target",
            "path",
            "expected_revision",
            "content"
          ]
        },
        {
          "description": "Submit a batch of inline comments as one user Turn.",
          "type": "object",
          "properties": {
            "comments": {
              "description": "Structured comments, preserved together in submission order.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/ReviewComment"
              }
            },
            "conversation_id": {
              "description": "Conversation whose normal queue receives the Turn.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "submit_review"
            }
          },
          "required": [
            "type",
            "conversation_id",
            "comments"
          ]
        },
        {
          "description": "Set the authoritative manual name of a Conversation.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "Conversation to name.",
              "type": "string",
              "format": "uuid"
            },
            "expected_revision": {
              "description": "Revision observed when the Command was prepared.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "name": {
              "description": "Validated as 1 to 256 UTF-8 bytes by the trusted core.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "set_conversation_name"
            }
          },
          "required": [
            "type",
            "conversation_id",
            "expected_revision",
            "name"
          ]
        },
        {
          "description": "Set the authoritative manual name of a Run.",
          "type": "object",
          "properties": {
            "expected_revision": {
              "description": "Revision observed when the Command was prepared.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "name": {
              "description": "Validated as 1 to 256 UTF-8 bytes by the trusted core.",
              "type": "string"
            },
            "run_id": {
              "description": "Run to name.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "set_run_name"
            }
          },
          "required": [
            "type",
            "run_id",
            "expected_revision",
            "name"
          ]
        },
        {
          "description": "Open a PTY under a Workspace-owned helper.",
          "type": "object",
          "properties": {
            "columns": {
              "description": "Width in cells (1–1000).",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "rows": {
              "description": "Height in cells (1–1000).",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "type": {
              "type": "string",
              "const": "open_terminal"
            },
            "workspace_id": {
              "description": "Registered Workspace identity.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "type",
            "workspace_id",
            "rows",
            "columns"
          ]
        },
        {
          "description": "Explicitly end a Workspace terminal.",
          "type": "object",
          "properties": {
            "terminal_id": {
              "description": "Terminal identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "close_terminal"
            }
          },
          "required": [
            "type",
            "terminal_id"
          ]
        },
        {
          "description": "Withdraw only the caller's own queued user input.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "Conversation owning the queue.",
              "type": "string",
              "format": "uuid"
            },
            "turn_id": {
              "description": "Admitted input identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "withdraw_turn"
            }
          },
          "required": [
            "type",
            "conversation_id",
            "turn_id"
          ]
        },
        {
          "description": "Admit input to one Conversation. Source selects a slot, not an Actor.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "Conversation that owns the input.",
              "type": "string",
              "format": "uuid"
            },
            "prompt": {
              "description": "Input of 1 to 65536 UTF-8 bytes.",
              "type": "string"
            },
            "source": {
              "description": "Input class; user work is never replaced.",
              "$ref": "#/$defs/TurnSource",
              "default": "user"
            },
            "type": {
              "type": "string",
              "const": "submit_turn"
            }
          },
          "required": [
            "type",
            "conversation_id",
            "prompt"
          ]
        },
        {
          "description": "Resolve only the helper instance inspected by an interactive user.",
          "type": "object",
          "properties": {
            "action": {
              "description": "Explicit decision.",
              "$ref": "#/$defs/ExecutionAction"
            },
            "execution_id": {
              "description": "Selected execution.",
              "type": "string",
              "format": "uuid"
            },
            "instance": {
              "description": "Expected instance; unavailable metadata permits only Leave.",
              "type": [
                "string",
                "null"
              ],
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "resolve_execution"
            }
          },
          "required": [
            "type",
            "execution_id",
            "action"
          ]
        },
        {
          "description": "Ask the Harness to end the turn it is working on, leaving the Run\nable to accept the next one. It is not a transport cancellation and\nnever withdraws queued input (ADR-0083, ADR-0095).",
          "type": "object",
          "properties": {
            "run_id": {
              "description": "The managed Run whose current turn ends.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "interrupt_turn"
            }
          },
          "required": [
            "type",
            "run_id"
          ]
        },
        {
          "description": "End one managed Run, including the native processes it owns.",
          "type": "object",
          "properties": {
            "run_id": {
              "description": "The managed Run to end.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "stop_run"
            }
          },
          "required": [
            "type",
            "run_id"
          ]
        },
        {
          "description": "Start native execution on the selected Conversation Home Plane (1.25).",
          "type": "object",
          "properties": {
            "account_binding_id": {
              "description": "Account binding on that Plane; never a credential copied from the client.",
              "type": "string",
              "format": "uuid"
            },
            "conversation_id": {
              "description": "Conversation already owned by the selected destination.",
              "type": "string",
              "format": "uuid"
            },
            "craft": {
              "description": "Installed Craft identity, never an executable path.",
              "type": "string"
            },
            "destination_plane_id": {
              "description": "Expected Home Plane; prevents launching against the wrong connection.",
              "type": "string",
              "format": "uuid"
            },
            "prompt": {
              "description": "Initial Harness input.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "start_visa_run"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "conversation_id",
            "destination_plane_id",
            "account_binding_id",
            "craft",
            "prompt"
          ]
        },
        {
          "description": "Desktop-only origin execution through paired remote tools (1.26).",
          "type": "object",
          "properties": {
            "account_binding_id": {
              "description": "Origin-local native Account binding.",
              "type": "string",
              "format": "uuid"
            },
            "conversation_id": {
              "description": "Origin-owned Conversation.",
              "type": "string",
              "format": "uuid"
            },
            "craft": {
              "description": "Accepted installed Craft with remote_tools support.",
              "type": "string"
            },
            "destinations": {
              "description": "One to eight explicit destinations, pinned for this Run.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/NoVisaDestination"
              }
            },
            "origin_plane_id": {
              "description": "Expected Home Plane, where credentials and native processes stay.",
              "type": "string",
              "format": "uuid"
            },
            "prompt": {
              "description": "Initial input.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "start_no_visa_run"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "conversation_id",
            "origin_plane_id",
            "account_binding_id",
            "craft",
            "prompt",
            "destinations"
          ]
        },
        {
          "description": "Start a managed Run with an installed Craft and initial input.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "Conversation whose working tree is used.",
              "type": "string",
              "format": "uuid"
            },
            "craft": {
              "description": "Installed Craft identity.",
              "type": "string"
            },
            "prompt": {
              "description": "Initial Harness input.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "start_run"
            }
          },
          "required": [
            "type",
            "conversation_id",
            "craft",
            "prompt"
          ]
        },
        {
          "description": "Create a Conversation with no Runs. Retained unless told otherwise,\nand in no Project unless a working tree is asked for.",
          "type": "object",
          "properties": {
            "retention": {
              "description": "Retention choice.",
              "$ref": "#/$defs/RetentionPolicy",
              "default": "retain"
            },
            "type": {
              "type": "string",
              "const": "create_conversation"
            },
            "working_tree": {
              "description": "Where the Conversation does its work. A Workspace request\ncreates the Workspace with the Conversation.",
              "$ref": "#/$defs/WorkingTreeRequest"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "Continue through another Harness with a new Conversation and Workspace.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "handoff_conversation"
            }
          },
          "$ref": "#/$defs/HandoffRequest",
          "required": [
            "type"
          ]
        },
        {
          "description": "Fork from an immutable checkpoint rather than a current Handoff package.",
          "type": "object",
          "properties": {
            "checkpoint_turn": {
              "description": "One-based turn boundary to fork from.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "source_run_id": {
              "description": "Run that owns the selected checkpoint.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "fork_conversation"
            }
          },
          "required": [
            "type",
            "source_run_id",
            "checkpoint_turn"
          ]
        },
        {
          "description": "Record a new Run of a Conversation that has no live Run.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "The Conversation to execute.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "create_run"
            }
          },
          "required": [
            "type",
            "conversation_id"
          ]
        },
        {
          "description": "Store a Setting value at one scope.",
          "type": "object",
          "properties": {
            "key": {
              "description": "The Setting to store.",
              "$ref": "#/$defs/SettingKey"
            },
            "scope": {
              "description": "The scope that stores the value.",
              "$ref": "#/$defs/SettingScope"
            },
            "type": {
              "type": "string",
              "const": "set_setting"
            },
            "value": {
              "description": "The value to store.",
              "$ref": "#/$defs/SettingValue"
            }
          },
          "required": [
            "type",
            "key",
            "scope",
            "value"
          ]
        },
        {
          "description": "Remove whatever value one scope stores for a Setting, leaving the\nscopes above it untouched.",
          "type": "object",
          "properties": {
            "key": {
              "description": "The Setting to clear.",
              "$ref": "#/$defs/SettingKey"
            },
            "scope": {
              "description": "The scope that stops storing a value.",
              "$ref": "#/$defs/SettingScope"
            },
            "type": {
              "type": "string",
              "const": "clear_setting"
            }
          },
          "required": [
            "type",
            "key",
            "scope"
          ]
        },
        {
          "description": "Bind a Provider account to this Plane. The request carries non-secret\nmetadata only; the Credential itself never crosses this protocol.",
          "type": "object",
          "properties": {
            "credential_source": {
              "description": "The backend that resolves the binding's Credential.",
              "$ref": "#/$defs/CredentialSource"
            },
            "label": {
              "description": "The user-facing name of the binding.",
              "type": "string"
            },
            "provider": {
              "description": "The Provider the binding authenticates to, such as `anthropic`.",
              "type": "string"
            },
            "provider_account": {
              "description": "The Provider's own account identity, when it supplies one.",
              "type": [
                "string",
                "null"
              ],
              "default": null
            },
            "type": {
              "type": "string",
              "const": "bind_account"
            }
          },
          "required": [
            "type",
            "provider",
            "label",
            "credential_source"
          ]
        },
        {
          "description": "Remove an Account binding from this Plane.",
          "type": "object",
          "properties": {
            "binding_id": {
              "description": "The binding to remove.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "unbind_account"
            }
          },
          "required": [
            "type",
            "binding_id"
          ]
        },
        {
          "description": "Begin a new authority epoch of the Security audit, carrying on past\nan integrity failure and recording the gap it leaves behind.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "begin_audit_epoch"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "Open or close the Plane's Pairing gate, which decides whether a new\nGUI client may begin Pairing at all. It does not alter the clients\nthat are already Paired.",
          "type": "object",
          "properties": {
            "gate": {
              "description": "Where to leave the gate.",
              "$ref": "#/$defs/PairingGate"
            },
            "type": {
              "type": "string",
              "const": "set_pairing_gate"
            }
          },
          "required": [
            "type",
            "gate"
          ]
        },
        {
          "description": "Issue the Plane's one Pairing offer, replacing whatever it had open,\nand disclose its one-time secret to the owner who asked for it.",
          "type": "object",
          "properties": {
            "method": {
              "description": "How the secret reaches the person pairing.",
              "$ref": "#/$defs/PairingMethod"
            },
            "type": {
              "type": "string",
              "const": "open_pairing"
            }
          },
          "required": [
            "type",
            "method"
          ]
        },
        {
          "description": "Claim the open Pairing offer with the secret a person presented and\nthe public key of the Client identity presenting it.",
          "type": "object",
          "properties": {
            "key": {
              "description": "The durable public key that becomes the credential once Pairing\ncompletes.",
              "$ref": "#/$defs/ClientPublicKey"
            },
            "secret": {
              "description": "The secret as it was presented.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "claim_pairing"
            }
          },
          "required": [
            "type",
            "secret",
            "key"
          ]
        },
        {
          "description": "Confirm, on the Plane being Paired with, that both screens show the\nsame authentication string. The client being Paired cannot confirm\nits own Pairing.",
          "type": "object",
          "properties": {
            "authentication_string": {
              "description": "The string as the person confirming reads it.",
              "type": "string"
            },
            "offer_id": {
              "description": "The offer being confirmed, which must be the one the Plane has\nopen.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "confirm_pairing"
            }
          },
          "required": [
            "type",
            "offer_id",
            "authentication_string"
          ]
        },
        {
          "description": "Complete the Pairing by signing the transcript of the claim with the\nClient identity that made it.",
          "type": "object",
          "properties": {
            "offer_id": {
              "description": "The offer being completed, which must be the one the Plane has\nopen.",
              "type": "string",
              "format": "uuid"
            },
            "signature": {
              "description": "The signature over the claim's transcript, as 128 lowercase\nhexadecimal characters.",
              "type": "string",
              "pattern": "^[0-9a-f]{128}$"
            },
            "type": {
              "type": "string",
              "const": "complete_pairing"
            }
          },
          "required": [
            "type",
            "offer_id",
            "signature"
          ]
        },
        {
          "description": "Stop a Paired client controlling the Plane, or let it control the\nPlane again. The Plane keeps its key either way.",
          "type": "object",
          "properties": {
            "access": {
              "description": "What it may do from now on.",
              "$ref": "#/$defs/PairedClientAccess"
            },
            "client_id": {
              "description": "The Paired client to decide about.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "set_paired_client_access"
            }
          },
          "required": [
            "type",
            "client_id",
            "access"
          ]
        },
        {
          "description": "Forget a Paired client and the key it was Paired with. The\ninstallation has to be Paired again to control the Plane.",
          "type": "object",
          "properties": {
            "client_id": {
              "description": "The client to forget.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "revoke_paired_client"
            }
          },
          "required": [
            "type",
            "client_id"
          ]
        },
        {
          "description": "Move a Run forward through its lifecycle.",
          "type": "object",
          "properties": {
            "expected_revision": {
              "description": "Revision observed when the Command was prepared, carried as a\ndecimal string (ADR-0089).",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "lifecycle": {
              "description": "The state to enter.",
              "$ref": "#/$defs/RunLifecycle"
            },
            "run_id": {
              "description": "The Run to move.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "transition_run"
            }
          },
          "required": [
            "type",
            "run_id",
            "expected_revision",
            "lifecycle"
          ]
        },
        {
          "description": "Register the Git working tree at an absolute path as a Project. This\nis a Path grant: the interactive user's explicit authorization for\nthe Plane to work inside that directory. The Plane resolves the path\nand refuses anything that is not an ordinary working tree.",
          "type": "object",
          "properties": {
            "path": {
              "description": "The absolute path the user granted.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "register_project"
            }
          },
          "required": [
            "type",
            "path"
          ]
        },
        {
          "description": "Promote a Workspace exactly as a preview showed. The Plane computes\nthe preview again and refuses a binding the Workspace or the\ndestination has moved past.",
          "type": "object",
          "properties": {
            "binding": {
              "description": "What the preview bound and the user confirmed.",
              "$ref": "#/$defs/PromotionBinding"
            },
            "type": {
              "type": "string",
              "const": "promote_workspace"
            }
          },
          "required": [
            "type",
            "binding"
          ]
        },
        {
          "description": "Register a Harness-native Conversation the Plane can see outside its\nmanagement, so a managed Run may later continue it. The Plane looks\nfor the identity again and refuses one no supported Harness reports.",
          "type": "object",
          "properties": {
            "harness": {
              "description": "The Harness whose identity it is, such as `codex`.",
              "type": "string"
            },
            "native_conversation": {
              "description": "The identity as the Harness spells it.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "import_conversation"
            }
          },
          "required": [
            "type",
            "harness",
            "native_conversation"
          ]
        },
        {
          "description": "Continue an Imported conversation as a new Conversation in a\nWorkspace or the Local checkout of a registered Project. A request\nwith no Project is refused: the user registers or maps one first.",
          "type": "object",
          "properties": {
            "import_id": {
              "description": "The import to continue.",
              "type": "string",
              "format": "uuid"
            },
            "retention": {
              "description": "Retention choice.",
              "$ref": "#/$defs/RetentionPolicy",
              "default": "retain"
            },
            "type": {
              "type": "string",
              "const": "resume_imported_conversation"
            },
            "working_tree": {
              "description": "Where the Conversation does its work.",
              "$ref": "#/$defs/WorkingTreeRequest"
            }
          },
          "required": [
            "type",
            "import_id",
            "working_tree"
          ]
        }
      ]
    },
    "CommandResponse": {
      "description": "Durable Command outcomes.",
      "oneOf": [
        {
          "description": "Native lifecycle request was durably staged.",
          "type": "object",
          "properties": {
            "change_id": {
              "description": "Identity for reading progress.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "extension_change_queued"
            }
          },
          "required": [
            "type",
            "change_id"
          ]
        },
        {
          "description": "The exact-action destination decision was recorded.",
          "type": "object",
          "properties": {
            "operation_id": {
              "description": "Reviewed operation identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "remote_tool_reviewed"
            }
          },
          "required": [
            "type",
            "operation_id"
          ]
        },
        {
          "description": "Durable Utility admission; query for its result.",
          "type": "object",
          "properties": {
            "job_id": {
              "description": "Plane-assigned job identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "utility_queued"
            }
          },
          "required": [
            "type",
            "job_id"
          ]
        },
        {
          "description": "Durable publication work accepted for one verified Craft Artifact.",
          "type": "object",
          "properties": {
            "craft_id": {
              "description": "Disabled Craft identity.",
              "type": "string"
            },
            "mode": {
              "description": "Applied disable behavior.",
              "$ref": "#/$defs/CraftDisableMode"
            },
            "type": {
              "type": "string",
              "const": "craft_disabled"
            }
          },
          "required": [
            "type",
            "craft_id",
            "mode"
          ]
        },
        {
          "description": "A verified Artifact was accepted for publication.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "craft_installation_queued"
            }
          },
          "$ref": "#/$defs/CraftInstallationQueued",
          "required": [
            "type"
          ]
        },
        {
          "description": "Enabled daily schedule.",
          "type": "object",
          "properties": {
            "task": {
              "description": "Enabled schedule and next firing.",
              "$ref": "#/$defs/ScheduledTask"
            },
            "type": {
              "type": "string",
              "const": "schedule_created"
            }
          },
          "required": [
            "type",
            "task"
          ]
        },
        {
          "description": "Canceled immutable schedule identity.",
          "type": "object",
          "properties": {
            "schedule_id": {
              "description": "Immutable schedule identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "schedule_canceled"
            }
          },
          "required": [
            "type",
            "schedule_id"
          ]
        },
        {
          "description": "A direct edit committed to the registered root.",
          "type": "object",
          "properties": {
            "path": {
              "description": "Validated path relative to the root.",
              "type": "string"
            },
            "revision": {
              "description": "Exact resulting Git content and mode.",
              "$ref": "#/$defs/FileRevision"
            },
            "target": {
              "description": "Registered root that was edited.",
              "$ref": "#/$defs/FileTarget"
            },
            "type": {
              "type": "string",
              "const": "user_edit_applied"
            }
          },
          "required": [
            "type",
            "target",
            "path",
            "revision"
          ]
        },
        {
          "description": "A Conversation's manual name committed.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "conversation_named"
            }
          },
          "$ref": "#/$defs/Conversation",
          "required": [
            "type"
          ]
        },
        {
          "description": "A Run's manual name committed.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "run_named"
            }
          },
          "$ref": "#/$defs/Run",
          "required": [
            "type"
          ]
        },
        {
          "description": "Terminal admission or close was committed.",
          "type": "object",
          "properties": {
            "terminal": {
              "description": "Current terminal state.",
              "$ref": "#/$defs/WorkspaceTerminal"
            },
            "type": {
              "type": "string",
              "const": "terminal"
            }
          },
          "required": [
            "type",
            "terminal"
          ]
        },
        {
          "description": "The original durable withdrawal result.",
          "type": "object",
          "properties": {
            "turn": {
              "description": "Input with its final outcome.",
              "$ref": "#/$defs/Turn"
            },
            "type": {
              "type": "string",
              "const": "turn_withdrawn"
            }
          },
          "required": [
            "type",
            "turn"
          ]
        },
        {
          "description": "The original durable admission, unchanged by later queue progress.",
          "type": "object",
          "properties": {
            "turn": {
              "description": "Plane-assigned input identity and sequence.",
              "$ref": "#/$defs/Turn"
            },
            "type": {
              "type": "string",
              "const": "turn_admitted"
            }
          },
          "required": [
            "type",
            "turn"
          ]
        },
        {
          "description": "The control request was durably accepted. Its outcome follows in\nthe Run's Events; acceptance alone never claims the work stopped.",
          "type": "object",
          "properties": {
            "control": {
              "description": "What was asked of it.",
              "$ref": "#/$defs/RunControl"
            },
            "run": {
              "description": "The Run as it stands after accepting the request.",
              "$ref": "#/$defs/Run"
            },
            "type": {
              "type": "string",
              "const": "run_control_accepted"
            }
          },
          "required": [
            "type",
            "run",
            "control"
          ]
        },
        {
          "description": "The interactive decision was durably queued.",
          "type": "object",
          "properties": {
            "action": {
              "description": "Explicit decision.",
              "$ref": "#/$defs/ExecutionAction"
            },
            "execution_id": {
              "description": "Selected execution.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "execution_resolution_recorded"
            }
          },
          "required": [
            "type",
            "execution_id",
            "action"
          ]
        },
        {
          "description": "The Conversation as created.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "conversation_created"
            }
          },
          "$ref": "#/$defs/Conversation",
          "required": [
            "type"
          ]
        },
        {
          "description": "The Run as created.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "run_created"
            }
          },
          "$ref": "#/$defs/Run",
          "required": [
            "type"
          ]
        },
        {
          "description": "The Run after its transition.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "run_transitioned"
            }
          },
          "$ref": "#/$defs/Run",
          "required": [
            "type"
          ]
        },
        {
          "description": "The Setting value the named scope now stores.",
          "type": "object",
          "properties": {
            "key": {
              "description": "The Setting that was stored.",
              "$ref": "#/$defs/SettingKey"
            },
            "scope": {
              "description": "The scope that stores it.",
              "$ref": "#/$defs/SettingScope"
            },
            "type": {
              "type": "string",
              "const": "setting_set"
            },
            "value": {
              "description": "The stored value.",
              "$ref": "#/$defs/SettingValue"
            }
          },
          "required": [
            "type",
            "key",
            "scope",
            "value"
          ]
        },
        {
          "description": "The named scope no longer stores its own value for the Setting.",
          "type": "object",
          "properties": {
            "key": {
              "description": "The Setting that was cleared.",
              "$ref": "#/$defs/SettingKey"
            },
            "scope": {
              "description": "The scope that no longer stores a value.",
              "$ref": "#/$defs/SettingScope"
            },
            "type": {
              "type": "string",
              "const": "setting_cleared"
            }
          },
          "required": [
            "type",
            "key",
            "scope"
          ]
        },
        {
          "description": "The Account binding as established.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "account_bound"
            }
          },
          "$ref": "#/$defs/AccountBinding",
          "required": [
            "type"
          ]
        },
        {
          "description": "The Plane no longer has the binding, and the reference whose secret\nits owner may now remove from the backend.",
          "type": "object",
          "properties": {
            "binding_id": {
              "description": "The binding that was removed.",
              "type": "string",
              "format": "uuid"
            },
            "credential_reference": {
              "description": "The reference it resolved through.",
              "$ref": "#/$defs/CredentialReference"
            },
            "type": {
              "type": "string",
              "const": "account_unbound"
            }
          },
          "required": [
            "type",
            "binding_id",
            "credential_reference"
          ]
        },
        {
          "description": "Where the Plane's Pairing gate now stands.",
          "type": "object",
          "properties": {
            "gate": {
              "description": "The gate as the Plane now records it.",
              "$ref": "#/$defs/PairingGate"
            },
            "type": {
              "type": "string",
              "const": "pairing_gate_set"
            }
          },
          "required": [
            "type",
            "gate"
          ]
        },
        {
          "description": "The Pairing offer the Plane now has open, and its one-time secret as\nit is disclosed once.",
          "type": "object",
          "properties": {
            "disclosure": {
              "description": "The secret, in the form the owner hands it over in.",
              "$ref": "#/$defs/PairingDisclosure"
            },
            "pending": {
              "description": "The offer, without the secret it was issued with.",
              "$ref": "#/$defs/PendingPairing"
            },
            "type": {
              "type": "string",
              "const": "pairing_opened"
            }
          },
          "required": [
            "type",
            "pending",
            "disclosure"
          ]
        },
        {
          "description": "The Pairing offer after a client claimed it, and the fresh challenge\nthat client's key signs to complete the Pairing.",
          "type": "object",
          "properties": {
            "challenge": {
              "description": "The challenge to sign, as 64 lowercase hexadecimal characters.",
              "type": "string",
              "pattern": "^[0-9a-f]{64}$"
            },
            "pending": {
              "description": "The offer, now waiting for the people at both ends.",
              "$ref": "#/$defs/PendingPairing"
            },
            "type": {
              "type": "string",
              "const": "pairing_claimed"
            }
          },
          "required": [
            "type",
            "pending",
            "challenge"
          ]
        },
        {
          "description": "The Pairing offer after the person at the target confirmed it.",
          "type": "object",
          "properties": {
            "pending": {
              "description": "The offer, now waiting for the client to prove its key.",
              "$ref": "#/$defs/PendingPairing"
            },
            "type": {
              "type": "string",
              "const": "pairing_confirmed"
            }
          },
          "required": [
            "type",
            "pending"
          ]
        },
        {
          "description": "The client the Plane is now Paired with.",
          "type": "object",
          "properties": {
            "client": {
              "description": "The Paired client the Pairing left behind.",
              "$ref": "#/$defs/PairedClient"
            },
            "type": {
              "type": "string",
              "const": "pairing_completed"
            }
          },
          "required": [
            "type",
            "client"
          ]
        },
        {
          "description": "The Paired client as the Plane now records it.",
          "type": "object",
          "properties": {
            "client": {
              "description": "The client, with the access it now has.",
              "$ref": "#/$defs/PairedClient"
            },
            "type": {
              "type": "string",
              "const": "paired_client_access_set"
            }
          },
          "required": [
            "type",
            "client"
          ]
        },
        {
          "description": "The Plane no longer holds that client or its key.",
          "type": "object",
          "properties": {
            "client_id": {
              "description": "The client that is no longer Paired.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "paired_client_revoked"
            }
          },
          "required": [
            "type",
            "client_id"
          ]
        },
        {
          "description": "The authority epoch the Security audit now records in.",
          "type": "object",
          "properties": {
            "epoch": {
              "description": "The epoch that holds the chain the Plane vouches for, carried as\na decimal string (ADR-0089).",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "type": {
              "type": "string",
              "const": "audit_epoch_begun"
            }
          },
          "required": [
            "type",
            "epoch"
          ]
        },
        {
          "description": "The Project as registered.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "project_registered"
            }
          },
          "$ref": "#/$defs/Project",
          "required": [
            "type"
          ]
        },
        {
          "description": "The promotion as recorded: applying, with its Effect committed, or\nconflicted, with the paths that keep it from being applied.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "workspace_promotion_recorded"
            }
          },
          "$ref": "#/$defs/WorkspacePromotion",
          "required": [
            "type"
          ]
        },
        {
          "description": "The Imported conversation as registered, with no Conversation\ncontinuing it yet.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "conversation_imported"
            }
          },
          "$ref": "#/$defs/ImportedConversation",
          "required": [
            "type"
          ]
        }
      ]
    },
    "ConflictKind": {
      "description": "Why a path cannot be promoted as it stands.",
      "oneOf": [
        {
          "description": "The Workspace and the destination both changed the path since the\nbase, in ways Git cannot combine.",
          "type": "string",
          "const": "diverged"
        },
        {
          "description": "The Workspace adds the path and the destination already holds an\nignored file there, which the merge never saw.",
          "type": "string",
          "const": "untracked"
        },
        {
          "description": "The destination's index holds a version of the path that differs\nfrom its file and from HEAD alike, which the merge never saw.",
          "type": "string",
          "const": "staged"
        }
      ]
    },
    "ConflictState": {
      "description": "Safe resource state attached to a Revision conflict.",
      "oneOf": [
        {
          "description": "The current Conversation.",
          "type": "object",
          "properties": {
            "conversation": {
              "description": "Complete safe Conversation state.",
              "$ref": "#/$defs/Conversation"
            },
            "type": {
              "type": "string",
              "const": "conversation"
            }
          },
          "required": [
            "type",
            "conversation"
          ]
        },
        {
          "description": "The current Run.",
          "type": "object",
          "properties": {
            "run": {
              "description": "Complete safe Run state.",
              "$ref": "#/$defs/Run"
            },
            "type": {
              "type": "string",
              "const": "run"
            }
          },
          "required": [
            "type",
            "run"
          ]
        }
      ]
    },
    "ConnectionProof": {
      "description": "The only message accepted in response to a remote challenge.",
      "type": "object",
      "properties": {
        "signature": {
          "description": "Signature of [`connection_signing_bytes`] by the Paired client's key.",
          "type": "string",
          "pattern": "^[0-9a-f]{128}$"
        }
      },
      "additionalProperties": false,
      "required": [
        "signature"
      ]
    },
    "Conversation": {
      "description": "One Conversation.",
      "type": "object",
      "properties": {
        "conversation_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        },
        "created_at_unix_ms": {
          "description": "When it was created, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "name": {
          "description": "Resolved name and its authority. Absent before protocol minor 20.",
          "anyOf": [
            {
              "$ref": "#/$defs/Name"
            },
            {
              "type": "null"
            }
          ]
        },
        "origin": {
          "description": "Where it came from. Absent before protocol minor 13.",
          "anyOf": [
            {
              "$ref": "#/$defs/ConversationOrigin"
            },
            {
              "type": "null"
            }
          ]
        },
        "retention": {
          "description": "Retention choice.",
          "$ref": "#/$defs/RetentionPolicy"
        },
        "revision": {
          "description": "Current version for conflict-sensitive Commands. Absent before minor 20.",
          "type": [
            "string",
            "null"
          ],
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "working_tree": {
          "description": "Where it does its work. Absent before protocol minor 9.",
          "anyOf": [
            {
              "$ref": "#/$defs/WorkingTree"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "conversation_id",
        "retention",
        "created_at_unix_ms"
      ]
    },
    "ConversationList": {
      "description": "One bounded page of Conversations, fenced by a journal cursor.",
      "type": "object",
      "properties": {
        "conversations": {
          "description": "Conversations in creation order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Conversation"
          }
        },
        "cursor": {
          "description": "Newest Event sequence visible when the list was read, carried as a\ndecimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "next_page": {
          "description": "Opaque continuation token when another page belongs to this snapshot.",
          "anyOf": [
            {
              "$ref": "#/$defs/PageCursor"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "cursor",
        "conversations"
      ]
    },
    "ConversationOrigin": {
      "description": "Where a Conversation came from.",
      "oneOf": [
        {
          "description": "Created in Jet.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "new"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "Created by a managed Resume to continue an Imported conversation.",
          "type": "object",
          "properties": {
            "import_id": {
              "description": "The import it continues.",
              "type": "string",
              "format": "uuid"
            },
            "kind": {
              "type": "string",
              "const": "imported"
            }
          },
          "required": [
            "kind",
            "import_id"
          ]
        },
        {
          "description": "Created from one immutable Change checkpoint.",
          "type": "object",
          "properties": {
            "checkpoint_turn": {
              "description": "One-based turn boundary selected from that Run.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "forked"
            },
            "source_conversation_id": {
              "description": "Conversation that owns the selected Run.",
              "type": "string",
              "format": "uuid"
            },
            "source_run_id": {
              "description": "Run that owns the selected checkpoint.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "source_conversation_id",
            "source_run_id",
            "checkpoint_turn"
          ]
        }
      ]
    },
    "ConversationSnapshot": {
      "description": "One Conversation with all of its Runs, fenced by a journal cursor.",
      "type": "object",
      "properties": {
        "conversation": {
          "description": "The Conversation itself.",
          "$ref": "#/$defs/Conversation"
        },
        "cursor": {
          "description": "Newest Event sequence visible when the snapshot was read, carried as\na decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "runs": {
          "description": "Its Runs in creation order, terminal ones included.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Run"
          }
        },
        "workspace": {
          "description": "The Workspace it owns, when it works in one. Absent before protocol\nminor 9.",
          "anyOf": [
            {
              "$ref": "#/$defs/Workspace"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "cursor",
        "conversation",
        "runs"
      ]
    },
    "CraftDisableMode": {
      "description": "How an interactive disable handles already accepted Runs.",
      "oneOf": [
        {
          "description": "Block new Runs and let pinned Runs finish.",
          "type": "string",
          "const": "wait"
        },
        {
          "description": "Stop the Craft while leaving its Harnesses under their helpers.",
          "type": "string",
          "const": "force"
        }
      ]
    },
    "CraftHostAccess": {
      "description": "Disclosed same-user host access; not a portable sandbox guarantee.",
      "oneOf": [
        {
          "description": "An executable the Craft expects to launch.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "executable"
            },
            "name": {
              "description": "Executable name or absolute path.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ]
        },
        {
          "description": "A filesystem area the Craft expects to use.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "filesystem"
            },
            "path": {
              "description": "Disclosed filesystem path.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "path"
          ]
        },
        {
          "description": "An environment input, never its secret value.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "environment"
            },
            "name": {
              "description": "Environment variable name.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ]
        },
        {
          "description": "An expected network destination.",
          "type": "object",
          "properties": {
            "destination": {
              "description": "Destination description shown during installation.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "network"
            }
          },
          "required": [
            "kind",
            "destination"
          ]
        }
      ]
    },
    "CraftInstallationConfirmation": {
      "description": "Every security-sensitive value copied from preview into confirmation.",
      "type": "object",
      "properties": {
        "artifact_sha256": {
          "description": "SHA-256 of the exact executable Artifact.",
          "type": "string"
        },
        "broker_permissions": {
          "description": "Jet-enforced broker authority the user accepts.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/BrokerPermission"
          }
        },
        "commit": {
          "description": "Immutable Git commit, or `developer-source` for a local build.",
          "type": "string"
        },
        "host_access": {
          "description": "Same-user host access the user accepts.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CraftHostAccess"
          }
        },
        "publisher_claim": {
          "description": "Publisher's unendorsed claim from the specification.",
          "type": "string"
        },
        "repository": {
          "description": "Canonical repository identity, or the local specification identity.",
          "type": "string"
        },
        "source": {
          "description": "Exact source the user previewed.",
          "$ref": "#/$defs/CraftSource"
        },
        "trust": {
          "description": "Jet's explicit trust boundary for this executable.",
          "$ref": "#/$defs/CraftTrust"
        }
      },
      "required": [
        "source",
        "repository",
        "publisher_claim",
        "commit",
        "artifact_sha256",
        "broker_permissions",
        "host_access",
        "trust"
      ]
    },
    "CraftInstallationPreview": {
      "description": "A verified immutable proposal that performs no installation by itself.",
      "type": "object",
      "properties": {
        "confirmation": {
          "description": "Exact consent object a subsequent installation Command must return.",
          "$ref": "#/$defs/CraftInstallationConfirmation"
        },
        "craft_id": {
          "description": "Stable Craft identity.",
          "type": "string"
        },
        "enabled_features": {
          "description": "Known optional features that remain enabled.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "version": {
          "description": "Publisher-declared release version.",
          "type": "string"
        }
      },
      "required": [
        "craft_id",
        "version",
        "enabled_features",
        "confirmation"
      ]
    },
    "CraftInstallationQueued": {
      "description": "Durable publication work accepted for one verified Craft Artifact.",
      "type": "object",
      "properties": {
        "artifact_sha256": {
          "description": "SHA-256 of the exact Artifact being published.",
          "type": "string"
        },
        "craft_id": {
          "description": "Stable Craft identity.",
          "type": "string"
        },
        "version": {
          "description": "Publisher-declared release version.",
          "type": "string"
        }
      },
      "required": [
        "craft_id",
        "version",
        "artifact_sha256"
      ]
    },
    "CraftSource": {
      "description": "An explicit source from which a third-party Craft may be considered.",
      "oneOf": [
        {
          "description": "A public GitHub Release from a qualifying `jet-craft-*` repository.",
          "type": "object",
          "properties": {
            "repository": {
              "description": "GitHub repository in `owner/name` form.",
              "type": "string"
            },
            "tag": {
              "description": "Exact release tag to resolve and pin.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "github_release"
            }
          },
          "required": [
            "type",
            "repository",
            "tag"
          ]
        },
        {
          "description": "Local files accepted only while Developer Mode is enabled.",
          "type": "object",
          "properties": {
            "artifact": {
              "description": "Path to the locally built executable Artifact.",
              "type": "string"
            },
            "specification": {
              "description": "Absolute or Plane-local path to `.jet/craft-spec.toml`.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "local"
            }
          },
          "required": [
            "type",
            "specification",
            "artifact"
          ]
        }
      ]
    },
    "CraftTrust": {
      "description": "The executable trust implication an interactive user must accept.",
      "oneOf": [
        {
          "description": "A verified release still executes with the user's OS authority.",
          "type": "string",
          "const": "same_user_executable"
        },
        {
          "description": "A local or source build has no verified release provenance.",
          "type": "string",
          "const": "developer_source"
        }
      ]
    },
    "CredentialItem": {
      "description": "One platform credential-store item. The Plane names it after the binding\nalone, so no client-supplied text enters Plane-owned state.",
      "type": "object",
      "properties": {
        "account": {
          "description": "The item within that service.",
          "type": "string"
        },
        "service": {
          "description": "The service every Jet Credential item lives under.",
          "type": "string"
        }
      },
      "required": [
        "service",
        "account"
      ]
    },
    "CredentialReference": {
      "description": "The opaque reference a Plane keeps for one binding's Credential, and the\nplace the secret it resolves to belongs.",
      "oneOf": [
        {
          "description": "An item of the platform credential store.",
          "type": "object",
          "properties": {
            "item": {
              "description": "The item the backend resolves.",
              "$ref": "#/$defs/CredentialItem"
            },
            "source": {
              "type": "string",
              "const": "platform_store"
            }
          },
          "required": [
            "source",
            "item"
          ]
        },
        {
          "description": "An external authentication helper.",
          "type": "object",
          "properties": {
            "helper": {
              "description": "The helper's non-secret name.",
              "type": "string"
            },
            "source": {
              "type": "string",
              "const": "external_helper"
            }
          },
          "required": [
            "source",
            "helper"
          ]
        },
        {
          "description": "Native Harness authentication; the Plane references nothing.",
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "const": "harness_native"
            }
          },
          "required": [
            "source"
          ]
        },
        {
          "description": "Memory of the daemon start that established it.",
          "type": "object",
          "properties": {
            "established_at_daemon_start": {
              "description": "The daemon start whose memory holds the Credential. A later\nstart holds nothing, so the binding must be established again.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "source": {
              "type": "string",
              "const": "session_only"
            }
          },
          "required": [
            "source",
            "established_at_daemon_start"
          ]
        }
      ]
    },
    "CredentialSource": {
      "description": "Which backend a client asks a new binding to resolve its Credential\nthrough.",
      "oneOf": [
        {
          "description": "The platform credential store, under an item the Plane names.",
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "const": "platform_store"
            }
          },
          "required": [
            "source"
          ]
        },
        {
          "description": "An explicitly configured external authentication helper, invoked at\nthe moment of use.",
          "type": "object",
          "properties": {
            "helper": {
              "description": "The helper's non-secret name.",
              "type": "string"
            },
            "source": {
              "type": "string",
              "const": "external_helper"
            }
          },
          "required": [
            "source",
            "helper"
          ]
        },
        {
          "description": "Native Harness authentication supplied by the environment the\nHarness is launched in. Jet references nothing of its own.",
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "const": "harness_native"
            }
          },
          "required": [
            "source"
          ]
        },
        {
          "description": "Memory of one daemon process, which a restart invalidates.",
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "const": "session_only"
            }
          },
          "required": [
            "source"
          ]
        }
      ]
    },
    "CredentialState": {
      "description": "Whether one binding's Credential can be resolved right now, and what\nhas to happen when it cannot. `jetd` never asks a person for a secret,\nso a backend that will not answer becomes one of these and the GUI\nstarts the operating system's own unlock flow.",
      "oneOf": [
        {
          "description": "The Plane can see the backend, so the Credential resolves at the\nmoment of use.",
          "type": "object",
          "properties": {
            "state": {
              "type": "string",
              "const": "resolvable"
            }
          },
          "required": [
            "state"
          ]
        },
        {
          "description": "The Plane holds no evidence either way. An external helper and\nnative Harness authentication answer only when they are invoked, and\ninvoking one early would run somebody's helper for no reason, so\nwork that needs the Credential finds out when it asks.",
          "type": "object",
          "properties": {
            "state": {
              "type": "string",
              "const": "resolved_at_use"
            }
          },
          "required": [
            "state"
          ]
        },
        {
          "description": "The backend is present but locked. Work that needs the Credential\nwaits until the user unlocks it.",
          "type": "object",
          "properties": {
            "kind": {
              "description": "The store that is locked.",
              "$ref": "#/$defs/CredentialStoreKind"
            },
            "state": {
              "type": "string",
              "const": "waiting_for_unlock"
            }
          },
          "required": [
            "state",
            "kind"
          ]
        },
        {
          "description": "The backend cannot be reached on this Plane at all, so this binding\ncannot be used until secure storage is set up.",
          "type": "object",
          "properties": {
            "kind": {
              "description": "The store that was expected.",
              "$ref": "#/$defs/CredentialStoreKind"
            },
            "state": {
              "type": "string",
              "const": "unavailable"
            }
          },
          "required": [
            "state",
            "kind"
          ]
        },
        {
          "description": "A session-only Credential that an earlier daemon start established.\nThis one holds nothing, so it must be established again.",
          "type": "object",
          "properties": {
            "state": {
              "type": "string",
              "const": "invalidated_by_restart"
            }
          },
          "required": [
            "state"
          ]
        }
      ]
    },
    "CredentialStoreKind": {
      "description": "The platform facility that resolves Credential references.",
      "oneOf": [
        {
          "description": "The macOS Keychain.",
          "type": "string",
          "const": "apple_keychain"
        },
        {
          "description": "The freedesktop Secret Service, reached over the session bus.",
          "type": "string",
          "const": "secret_service"
        }
      ]
    },
    "CredentialStoreStatus": {
      "description": "Whether the platform credential store can be reached, and whether it\nwill answer. Jet never falls back to plaintext, so each of these is\nreported rather than worked around.",
      "oneOf": [
        {
          "description": "The store is present and can be asked for credentials.",
          "type": "object",
          "properties": {
            "kind": {
              "description": "Which store the Plane resolves through.",
              "$ref": "#/$defs/CredentialStoreKind"
            },
            "status": {
              "type": "string",
              "const": "available"
            }
          },
          "required": [
            "status",
            "kind"
          ]
        },
        {
          "description": "The store is present but locked: it answers nothing until the user\nunlocks it through the operating system. `jetd` never asks for that\nitself, so work that needs a Credential waits instead.",
          "type": "object",
          "properties": {
            "kind": {
              "description": "Which store is locked.",
              "$ref": "#/$defs/CredentialStoreKind"
            },
            "status": {
              "type": "string",
              "const": "locked"
            }
          },
          "required": [
            "status",
            "kind"
          ]
        },
        {
          "description": "The store cannot be reached on this Plane right now.",
          "type": "object",
          "properties": {
            "kind": {
              "description": "Which store was expected.",
              "$ref": "#/$defs/CredentialStoreKind"
            },
            "status": {
              "type": "string",
              "const": "unavailable"
            }
          },
          "required": [
            "status",
            "kind"
          ]
        }
      ]
    },
    "DegradedCondition": {
      "description": "Something a Plane cannot do in its current state.",
      "oneOf": [
        {
          "description": "An external tool ordinary work needs is not installed.",
          "type": "object",
          "properties": {
            "condition": {
              "type": "string",
              "const": "missing_external_tool"
            },
            "tool": {
              "description": "The tool the core could not run.",
              "$ref": "#/$defs/ExternalTool"
            }
          },
          "required": [
            "condition",
            "tool"
          ]
        },
        {
          "description": "No Craft is installed, so the Plane can orchestrate no Harness.",
          "type": "object",
          "properties": {
            "condition": {
              "type": "string",
              "const": "no_harness_available"
            }
          },
          "required": [
            "condition"
          ]
        },
        {
          "description": "Credentials cannot be resolved, so no Account binding can be used.",
          "type": "object",
          "properties": {
            "condition": {
              "type": "string",
              "const": "credential_store_unavailable"
            },
            "kind": {
              "description": "The store that was expected.",
              "$ref": "#/$defs/CredentialStoreKind"
            }
          },
          "required": [
            "condition",
            "kind"
          ]
        },
        {
          "description": "The credential store is locked, so Credentials resolve only after\nthe user unlocks it through the operating system.",
          "type": "object",
          "properties": {
            "condition": {
              "type": "string",
              "const": "credential_store_locked"
            },
            "kind": {
              "description": "The store that is locked.",
              "$ref": "#/$defs/CredentialStoreKind"
            }
          },
          "required": [
            "condition",
            "kind"
          ]
        }
      ]
    },
    "DiffScope": {
      "description": "Which immutable boundaries a diff compares.",
      "oneOf": [
        {
          "description": "Run baseline compared with the working tree now.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "current"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "Run baseline compared with its final captured boundary; terminal Runs only.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "final"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "Compare two retained boundaries; zero selects the Run baseline.",
          "type": "object",
          "properties": {
            "from_turn": {
              "description": "Earlier boundary.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "historical"
            },
            "to_turn": {
              "description": "Later boundary.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "from_turn",
            "to_turn"
          ]
        },
        {
          "description": "Changes in one completed or interrupted turn, numbered from one.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "turn"
            },
            "turn": {
              "description": "Turn number within the Run.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "turn"
          ]
        }
      ]
    },
    "EditableFile": {
      "description": "Bounded UTF-8 content read through a registered root.",
      "type": "object",
      "properties": {
        "content": {
          "description": "UTF-8 content, or `None` when the path does not exist yet.",
          "type": [
            "string",
            "null"
          ]
        },
        "cursor": {
          "description": "Journal fence observed with the registered target.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "path": {
          "description": "Validated path relative to that root.",
          "type": "string"
        },
        "revision": {
          "description": "Exact optimistic-concurrency precondition for an edit.",
          "$ref": "#/$defs/FileRevision"
        },
        "target": {
          "description": "Registered root that was read.",
          "$ref": "#/$defs/FileTarget"
        }
      },
      "required": [
        "cursor",
        "target",
        "path",
        "revision"
      ]
    },
    "EntryKind": {
      "description": "What one path inside a Project names.",
      "oneOf": [
        {
          "description": "A regular file.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "Its length in bytes.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "type": {
              "type": "string",
              "const": "file"
            }
          },
          "required": [
            "type",
            "bytes"
          ]
        },
        {
          "description": "A directory.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "directory"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "Something else the filesystem holds, such as a socket.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "other"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "Nothing yet.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "missing"
            }
          },
          "required": [
            "type"
          ]
        }
      ]
    },
    "ErrorCategory": {
      "description": "Stable error categories exposed to clients (ADR-0068).",
      "oneOf": [
        {
          "description": "The request was malformed or violated a precondition.",
          "type": "string",
          "const": "invalid_input"
        },
        {
          "description": "The Actor may not perform the request.",
          "type": "string",
          "const": "unauthorized"
        },
        {
          "description": "The request conflicts with current state.",
          "type": "string",
          "const": "conflict"
        },
        {
          "description": "A required resource is temporarily unavailable.",
          "type": "string",
          "const": "unavailable"
        },
        {
          "description": "The peers cannot agree on a protocol, codec, or version.",
          "type": "string",
          "const": "incompatible"
        },
        {
          "description": "The request was throttled.",
          "type": "string",
          "const": "rate_limited"
        },
        {
          "description": "The addressed resource does not exist.",
          "type": "string",
          "const": "not_found"
        },
        {
          "description": "The result of external work could not be established.",
          "type": "string",
          "const": "outcome_unknown"
        },
        {
          "description": "An unexpected internal failure.",
          "type": "string",
          "const": "internal"
        }
      ]
    },
    "Event": {
      "description": "One journal entry.",
      "type": "object",
      "properties": {
        "actor": {
          "description": "Legacy client authorization attribution. Use `origin` when present\nto identify who supplied an execution observation.",
          "$ref": "#/$defs/Actor"
        },
        "conversation_id": {
          "description": "The Conversation it concerns, if any.",
          "type": [
            "string",
            "null"
          ],
          "format": "uuid"
        },
        "event_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        },
        "kind": {
          "description": "Indexed kind name such as `run.lifecycle_changed`.",
          "type": "string"
        },
        "origin": {
          "description": "Actual origin of an execution observation; additive for older readers.",
          "anyOf": [
            {
              "$ref": "#/$defs/EventOrigin"
            },
            {
              "type": "null"
            }
          ]
        },
        "payload": {
          "description": "Kind-specific JSON payload."
        },
        "payload_version": {
          "description": "Schema version of `payload` for this `kind` (ADR-0096).",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "recorded_at_unix_ms": {
          "description": "When it was recorded, in signed Unix milliseconds. Display metadata\nonly; never an ordering authority.",
          "type": "integer",
          "format": "int64"
        },
        "run_id": {
          "description": "The Run it concerns, if any.",
          "type": [
            "string",
            "null"
          ],
          "format": "uuid"
        },
        "sequence": {
          "description": "Plane-local monotonic position, carried as a decimal string\n(ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        }
      },
      "required": [
        "sequence",
        "event_id",
        "actor",
        "recorded_at_unix_ms",
        "kind",
        "payload_version",
        "payload"
      ]
    },
    "EventOrigin": {
      "description": "Responsible execution origin, when legacy client authorization is insufficient.",
      "oneOf": [
        {
          "description": "Input originated from a durable Scheduled task.",
          "type": "object",
          "properties": {
            "schedule_id": {
              "description": "Responsible schedule.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "scheduled_task"
            }
          },
          "required": [
            "type",
            "schedule_id"
          ]
        },
        {
          "description": "Semantic observations from the pinned Harness.",
          "type": "object",
          "properties": {
            "run_id": {
              "description": "Owning Run identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "harness"
            }
          },
          "required": [
            "type",
            "run_id"
          ]
        },
        {
          "description": "Internal process supervision.",
          "type": "object",
          "properties": {
            "run_id": {
              "description": "Owning Run identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "run_supervisor"
            }
          },
          "required": [
            "type",
            "run_id"
          ]
        }
      ]
    },
    "EventPage": {
      "description": "One page of journal Events, fenced by the journal position it was read\nat (ADR-0092). The page is the last one when its final Event's sequence\nequals `cursor`.",
      "type": "object",
      "properties": {
        "cursor": {
          "description": "Newest Event sequence visible to this peer when the page was read,\ncarried as a decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "events": {
          "description": "The Events strictly after the requested position, in sequence order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Event"
          }
        }
      },
      "required": [
        "cursor",
        "events"
      ]
    },
    "ExecutionAction": {
      "description": "Unknown control decisions fail closed.",
      "oneOf": [
        {
          "description": "Revalidate authority, roots, identity, and offsets before attaching.",
          "type": "string",
          "const": "adopt"
        },
        {
          "description": "Preserve the execution without acknowledging source.",
          "type": "string",
          "const": "leave"
        },
        {
          "description": "Stop only the inspected helper instance and its native process.",
          "type": "string",
          "const": "terminate"
        }
      ]
    },
    "ExecutionMetadata": {
      "description": "Read-only, non-secret metadata; inspection does not authorize adoption.",
      "type": "object",
      "properties": {
        "helper_pid": {
          "description": "Helper OS identity.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "instance": {
          "description": "Fresh helper instance identity.",
          "type": "string",
          "format": "uuid"
        },
        "project_root": {
          "description": "Declared Project root.",
          "type": "string"
        },
        "root": {
          "description": "Declared canonical working root.",
          "type": "string"
        },
        "version": {
          "description": "Helper product version.",
          "type": "string"
        }
      },
      "required": [
        "instance",
        "helper_pid",
        "root",
        "project_root",
        "version"
      ]
    },
    "ExecutionRole": {
      "description": "Role of an independently preserved execution.",
      "oneOf": [
        {
          "description": "A Harness Run (also the role of older descriptors).",
          "type": "string",
          "const": "run"
        },
        {
          "description": "A Workspace terminal.",
          "type": "string",
          "const": "terminal"
        }
      ]
    },
    "ExtensionAction": {
      "description": "Operations supported by the responsible native adapter.",
      "oneOf": [
        {
          "description": "Install a native extension.",
          "type": "string",
          "const": "install"
        },
        {
          "description": "Update a native extension.",
          "type": "string",
          "const": "update"
        },
        {
          "description": "Stop loading a native extension.",
          "type": "string",
          "const": "disable"
        },
        {
          "description": "Remove a native extension.",
          "type": "string",
          "const": "remove"
        }
      ]
    },
    "ExtensionCatalog": {
      "description": "Native catalog data, interpreted by its Craft and rendered as data by clients.",
      "type": "object",
      "properties": {
        "craft_id": {
          "description": "Installed Craft identity.",
          "type": "string"
        },
        "harness": {
          "description": "Harness whose native formats this catalog describes.",
          "type": "string"
        },
        "native_metadata": {
          "description": "Bounded native JSON containing sources, versions, publishers and components.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "craft_id",
        "harness",
        "native_metadata"
      ]
    },
    "ExtensionChange": {
      "description": "Inspectable, content-free lifecycle progress.",
      "type": "object",
      "properties": {
        "action": {
          "description": "Requested operation.",
          "$ref": "#/$defs/ExtensionAction"
        },
        "change_id": {
          "description": "Durable operation identity.",
          "type": "string",
          "format": "uuid"
        },
        "craft_id": {
          "description": "Responsible Craft.",
          "type": "string"
        },
        "extension_id": {
          "description": "Native extension identity.",
          "type": "string"
        },
        "state": {
          "description": "Execution outcome.",
          "$ref": "#/$defs/ExtensionChangeState"
        }
      },
      "required": [
        "change_id",
        "craft_id",
        "extension_id",
        "action",
        "state"
      ]
    },
    "ExtensionChangeState": {
      "description": "Durable outcome, separate from acceptance of the initiating Command.",
      "oneOf": [
        {
          "description": "Waiting for existing managed Runs to finish.",
          "type": "string",
          "const": "staged"
        },
        {
          "description": "Native lifecycle operation completed for subsequent Runs.",
          "type": "string",
          "const": "applied"
        },
        {
          "description": "Validation refused the mutation before execution.",
          "type": "string",
          "const": "refused"
        },
        {
          "description": "Execution began but its complete outcome cannot be established. Never retried.",
          "type": "string",
          "const": "outcome_unknown"
        }
      ]
    },
    "ExtensionConfirmation": {
      "description": "Exact native metadata and authority shown before any lifecycle mutation.",
      "type": "object",
      "properties": {
        "action": {
          "description": "Exactly one lifecycle operation.",
          "$ref": "#/$defs/ExtensionAction"
        },
        "catalog": {
          "description": "Catalog snapshot which the Craft must revalidate before execution.",
          "$ref": "#/$defs/ExtensionCatalog"
        },
        "extension_id": {
          "description": "Native identity, for example plugin@marketplace.",
          "type": "string"
        },
        "scope": {
          "description": "Native installation scope.",
          "$ref": "#/$defs/ExtensionScope"
        },
        "trust": {
          "description": "Same-user executable access accepted by the interactive client.",
          "$ref": "#/$defs/ExtensionTrust"
        }
      },
      "additionalProperties": false,
      "required": [
        "catalog",
        "extension_id",
        "action",
        "scope",
        "trust"
      ]
    },
    "ExtensionScope": {
      "description": "Native scopes currently admitted by the Plane.",
      "oneOf": [
        {
          "description": "The native Harness's user configuration on the selected Plane.",
          "type": "string",
          "const": "user"
        }
      ]
    },
    "ExtensionTrust": {
      "description": "Explicit consent to the native executable boundary, never GUI code.",
      "oneOf": [
        {
          "description": "Skills may direct tools; hooks, MCP servers and plugins execute as the user.",
          "type": "string",
          "const": "same_user_executable"
        }
      ]
    },
    "ExternalConversation": {
      "description": "One Harness-native Conversation the Plane can see outside its\nmanagement.",
      "type": "object",
      "properties": {
        "harness": {
          "description": "The Harness whose identity it is, such as `codex`.",
          "type": "string"
        },
        "import_id": {
          "description": "The import that already registered it, if one has.",
          "type": [
            "string",
            "null"
          ],
          "format": "uuid"
        },
        "native_conversation": {
          "description": "The identity as the Harness spells it.",
          "type": "string"
        },
        "origin": {
          "description": "Where it did its work.",
          "$ref": "#/$defs/ExternalOrigin"
        },
        "process": {
          "description": "The live process holding it, if any.",
          "$ref": "#/$defs/ExternalProcess"
        }
      },
      "required": [
        "harness",
        "native_conversation",
        "origin",
        "process"
      ]
    },
    "ExternalConversationList": {
      "description": "The external Conversations the Plane can see and the imports it holds,\nfenced by the journal position the imports were read at.",
      "type": "object",
      "properties": {
        "cursor": {
          "description": "Newest Event sequence visible when the imports were read, carried as\na decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "discovered": {
          "description": "Every identity the Plane can see right now.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ExternalConversation"
          }
        },
        "imported": {
          "description": "Every import the Plane holds, in the order they were made.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ImportedConversation"
          }
        }
      },
      "required": [
        "cursor",
        "discovered",
        "imported"
      ]
    },
    "ExternalOrigin": {
      "description": "Where an external Conversation did its work, as it relates to the\nPlane's Projects.",
      "oneOf": [
        {
          "description": "Inside a registered Project, which a Resume may select directly.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "project"
            },
            "project_id": {
              "description": "The Project whose root holds the directory.",
              "type": "string",
              "format": "uuid"
            },
            "working_directory": {
              "description": "The directory the Harness reported.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "project_id",
            "working_directory"
          ]
        },
        {
          "description": "In a directory no Project covers. The user registers it, or maps\nanother Project, before a Resume.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "unregistered"
            },
            "working_directory": {
              "description": "The directory the Harness reported.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "working_directory"
          ]
        },
        {
          "description": "The Harness did not say where it worked.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "unknown"
            }
          },
          "required": [
            "kind"
          ]
        }
      ]
    },
    "ExternalProcess": {
      "description": "A process outside the Plane's management that holds an external\nConversation live, and what the Plane can do about it.",
      "oneOf": [
        {
          "description": "No live process was observed. The identity can be continued only by\na new managed Run.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "none"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "A live process the Plane can see only through a terminal. It stays\nexternal.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "external"
            },
            "pid": {
              "description": "The process as the operating system numbers it.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "pid"
          ]
        },
        {
          "description": "A live process whose Harness advertises a cooperating structured\nendpoint, so live takeover is available there.",
          "type": "object",
          "properties": {
            "endpoint": {
              "description": "The endpoint the Harness advertises.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "cooperating"
            },
            "pid": {
              "description": "The process as the operating system numbers it.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "pid",
            "endpoint"
          ]
        }
      ]
    },
    "ExternalTool": {
      "description": "A command-line tool the core invokes but never bundles or installs.",
      "oneOf": [
        {
          "description": "Git, which every Workspace and Change checkpoint rests on.",
          "type": "string",
          "const": "git"
        },
        {
          "description": "Git LFS, reported so a Project that tracks large files knows whether\nthe Plane can handle them. Named from protocol minor 8, spelled as\nthe program the Plane looks for.",
          "type": "string",
          "const": "git-lfs"
        },
        {
          "description": "OpenSSH, which No-Visa Runs reach paired Planes through.",
          "type": "string",
          "const": "ssh"
        },
        {
          "description": "Tailscale, which discovers and reaches Planes across networks.",
          "type": "string",
          "const": "tailscale"
        }
      ]
    },
    "ExternalToolStatus": {
      "description": "Whether one external tool answered, and with which version.",
      "type": "object",
      "properties": {
        "availability": {
          "description": "What looking for it found.",
          "$ref": "#/$defs/ToolAvailability"
        },
        "tool": {
          "description": "The tool that was looked for.",
          "$ref": "#/$defs/ExternalTool"
        }
      },
      "required": [
        "tool",
        "availability"
      ]
    },
    "FileRevision": {
      "description": "Exact Git content and mode observed for one file.",
      "type": "object",
      "properties": {
        "mode": {
          "description": "Git mode, or `000000` when the file is missing.",
          "type": "string"
        },
        "object": {
          "description": "Git blob object, or all zeroes when the file is missing.",
          "type": "string"
        }
      },
      "required": [
        "object",
        "mode"
      ]
    },
    "FileTarget": {
      "description": "A registered root through which an ordinary file operation is addressed.",
      "oneOf": [
        {
          "description": "A registered Project's Local checkout.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "project"
            },
            "project_id": {
              "description": "Project identity.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "project_id"
          ]
        },
        {
          "description": "A Conversation-owned Workspace.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "workspace"
            },
            "workspace_id": {
              "description": "Workspace identity.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "workspace_id"
          ]
        }
      ]
    },
    "GitLink": {
      "description": "One submodule as the index records it: a path holding a commit of\nanother repository.",
      "type": "object",
      "properties": {
        "commit": {
          "description": "The commit the link points at, as Git spells it.",
          "type": "string"
        },
        "path": {
          "description": "The path inside the working tree, as Git spells it.",
          "type": "string"
        }
      },
      "required": [
        "path",
        "commit"
      ]
    },
    "HandoffRequest": {
      "description": "Selected context; the daemon captures files, current diff, and provenance.",
      "type": "object",
      "properties": {
        "craft": {
          "description": "Installed destination Craft for a different Harness.",
          "type": "string"
        },
        "files": {
          "description": "At most 16 distinct relative regular-file paths, each at most 8 KiB.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "plan": {
          "description": "Selected plan; empty selects none. Maximum 8 KiB.",
          "type": "string"
        },
        "source_run_id": {
          "description": "Managed source Run.",
          "type": "string",
          "format": "uuid"
        },
        "summary": {
          "description": "Selected summary; empty selects none. Maximum 8 KiB.",
          "type": "string"
        }
      },
      "required": [
        "source_run_id",
        "craft",
        "summary",
        "plan",
        "files"
      ]
    },
    "ImportedConversation": {
      "description": "One Imported conversation.",
      "type": "object",
      "properties": {
        "harness": {
          "description": "The Harness whose identity it is.",
          "type": "string"
        },
        "import_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        },
        "imported_at_unix_ms": {
          "description": "When it was imported, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "imported_by": {
          "description": "The interactive user who imported it.",
          "$ref": "#/$defs/Actor"
        },
        "native_conversation": {
          "description": "The identity as the Harness spells it.",
          "type": "string"
        },
        "resumed_as": {
          "description": "The Conversation that continues it, once a Resume has made one.",
          "type": [
            "string",
            "null"
          ],
          "format": "uuid"
        },
        "working_directory": {
          "description": "The directory the Harness reported working in when it was imported.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "import_id",
        "harness",
        "native_conversation",
        "imported_by",
        "imported_at_unix_ms"
      ]
    },
    "InstalledCraft": {
      "description": "One Craft installed on the Plane and the Harnesses it adapts.",
      "type": "object",
      "properties": {
        "craft_id": {
          "description": "The Craft's identity.",
          "type": "string"
        },
        "harnesses": {
          "description": "The Harnesses it teaches this Plane to orchestrate.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "version": {
          "description": "The version its specification declares.",
          "type": "string"
        }
      },
      "required": [
        "craft_id",
        "version",
        "harnesses"
      ]
    },
    "ManagedProcess": {
      "description": "Observable process identity; distinct from a Conversation or Run identity.",
      "type": "object",
      "properties": {
        "label": {
          "description": "Live native title for this process alone, never an entity name (1.20).",
          "type": [
            "string",
            "null"
          ]
        },
        "pid": {
          "description": "OS process identifier, meaningful only on the Home Plane.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "role": {
          "description": "Process responsibility.",
          "$ref": "#/$defs/ManagedProcessRole"
        },
        "running": {
          "description": "Whether this process is still participating in the Run.",
          "type": "boolean"
        }
      },
      "required": [
        "pid",
        "role",
        "running"
      ]
    },
    "ManagedProcessRole": {
      "description": "The role of a process owned by a Run.",
      "oneOf": [
        {
          "description": "Generic per-Run supervisor.",
          "type": "string",
          "const": "helper"
        },
        {
          "description": "Native coding Harness.",
          "type": "string",
          "const": "harness"
        }
      ]
    },
    "ModelConsumption": {
      "description": "Deduplicated consumption for one Model.",
      "type": "object",
      "properties": {
        "estimated": {
          "description": "How many of them Jet estimated rather than measured.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "interim": {
          "description": "How many of them can still change.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "last_observed_at_unix_ms": {
          "description": "When the newest contributing measurement was observed.",
          "type": "integer",
          "format": "int64"
        },
        "measurements": {
          "description": "How many deduplicated measurements contributed.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "model": {
          "description": "The Model, absent where the Harness named none.",
          "type": [
            "string",
            "null"
          ]
        },
        "tokens": {
          "description": "The counts.",
          "$ref": "#/$defs/UsageTokens"
        }
      },
      "required": [
        "tokens",
        "measurements",
        "estimated",
        "interim",
        "last_observed_at_unix_ms"
      ]
    },
    "Name": {
      "description": "One resolved user-facing name and its authority.",
      "type": "object",
      "properties": {
        "source": {
          "description": "Authority that supplied `value`.",
          "$ref": "#/$defs/NameSource"
        },
        "value": {
          "description": "Original validated text; clients encode it for their render context.",
          "type": "string"
        }
      },
      "required": [
        "value",
        "source"
      ]
    },
    "NameSource": {
      "description": "Authority that supplied a current Conversation or Run name.",
      "oneOf": [
        {
          "description": "An interactive user's authoritative choice.",
          "type": "string",
          "const": "manual"
        },
        {
          "description": "A validated Utility-model result.",
          "type": "string",
          "const": "utility"
        },
        {
          "description": "A structured title supplied by the Harness through its Craft.",
          "type": "string",
          "const": "harness_native"
        },
        {
          "description": "Stable local text used without Utility work.",
          "type": "string",
          "const": "deterministic"
        }
      ]
    },
    "NoVisaDestination": {
      "description": "One destination selected by the desktop, never by the Craft.",
      "type": "object",
      "properties": {
        "plane_id": {
          "description": "Expected paired Plane identity.",
          "type": "string",
          "format": "uuid"
        },
        "ssh_endpoint": {
          "description": "System SSH destination, resolved through the user's SSH configuration.",
          "type": "string"
        },
        "workspace_id": {
          "description": "Registered destination Workspace.",
          "type": "string",
          "format": "uuid"
        }
      },
      "additionalProperties": false,
      "required": [
        "plane_id",
        "workspace_id",
        "ssh_endpoint"
      ]
    },
    "NoVisaOrigin": {
      "description": "Origin retained separately from the paired installation's authentication.",
      "type": "object",
      "properties": {
        "conversation_id": {
          "description": "Originating Conversation.",
          "type": "string",
          "format": "uuid"
        },
        "plane_id": {
          "description": "Home Plane, which continues to own the Conversation.",
          "type": "string",
          "format": "uuid"
        },
        "run_id": {
          "description": "Originating Harness Run.",
          "type": "string",
          "format": "uuid"
        }
      },
      "additionalProperties": false,
      "required": [
        "plane_id",
        "conversation_id",
        "run_id"
      ]
    },
    "NoVisaSelection": {
      "description": "Immutable origin and destination selection retained with the execution.",
      "type": "object",
      "properties": {
        "account_binding_id": {
          "description": "Native authentication stays on this Plane.",
          "type": "string",
          "format": "uuid"
        },
        "conversation_id": {
          "description": "Authoritative Conversation on the origin.",
          "type": "string",
          "format": "uuid"
        },
        "destinations": {
          "description": "Destinations the broker may contact.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/NoVisaDestination"
          }
        },
        "jet_equivalent": {
          "description": "Jet-provided equivalents, subject to destination authority and bounds.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "native_unavailable": {
          "description": "Native remote behavior that this mode cannot promise.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "origin_plane_id": {
          "description": "Native execution's Home Plane.",
          "type": "string",
          "format": "uuid"
        }
      },
      "required": [
        "jet_equivalent",
        "native_unavailable",
        "origin_plane_id",
        "account_binding_id",
        "conversation_id",
        "destinations"
      ]
    },
    "ObservedConsumption": {
      "description": "Deduplicated Jet-observed consumption, with the uncertainty in it left\nvisible rather than folded into the total.",
      "type": "object",
      "properties": {
        "estimated": {
          "description": "How many of them Jet estimated rather than measured.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "interim": {
          "description": "How many of them can still change.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "last_observed_at_unix_ms": {
          "description": "When the newest contributing measurement was observed.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        },
        "measurements": {
          "description": "How many deduplicated measurements contributed.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "models": {
          "description": "The same consumption per Model.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ModelConsumption"
          }
        },
        "tokens": {
          "description": "The counts across every Model in the selection.",
          "$ref": "#/$defs/UsageTokens"
        }
      },
      "required": [
        "tokens",
        "measurements",
        "estimated",
        "interim",
        "models"
      ]
    },
    "OrphanedExecution": {
      "description": "A live execution recovery could not match safely.",
      "type": "object",
      "properties": {
        "execution_id": {
          "description": "Execution selected by the user.",
          "type": "string",
          "format": "uuid"
        },
        "metadata": {
          "description": "Owner-only metadata when safely readable.",
          "anyOf": [
            {
              "$ref": "#/$defs/ExecutionMetadata"
            },
            {
              "type": "null"
            }
          ]
        },
        "role": {
          "description": "Execution role; absent on pre-terminal protocol snapshots.",
          "$ref": "#/$defs/ExecutionRole"
        }
      },
      "required": [
        "execution_id"
      ]
    },
    "OrphanedExecutions": {
      "description": "Bounded page of interactive recovery candidates.",
      "type": "object",
      "properties": {
        "executions": {
          "description": "Read-only metadata; never native output.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/OrphanedExecution"
          }
        },
        "next": {
          "description": "Continue after this identity, or finish when absent.",
          "type": [
            "string",
            "null"
          ],
          "format": "uuid"
        }
      },
      "required": [
        "executions"
      ]
    },
    "PageCursor": {
      "description": "Opaque token for continuing one fenced keyset snapshot page.",
      "type": "string",
      "format": "uuid"
    },
    "PairedClient": {
      "description": "One GUI client a Plane has Paired with.",
      "type": "object",
      "properties": {
        "access": {
          "description": "Whether it may control the Plane right now.",
          "$ref": "#/$defs/PairedClientAccess"
        },
        "client_id": {
          "description": "The Client identity that was Paired.",
          "type": "string",
          "format": "uuid"
        },
        "key": {
          "description": "The durable public key that is now its credential.",
          "$ref": "#/$defs/ClientPublicKey"
        },
        "paired_at_unix_ms": {
          "description": "When the Pairing completed, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "pairing_protocol": {
          "description": "The Pairing protocol the key was established under, such as\n`jet.pairing.v1`.",
          "type": "string"
        }
      },
      "required": [
        "client_id",
        "key",
        "pairing_protocol",
        "access",
        "paired_at_unix_ms"
      ]
    },
    "PairedClientAccess": {
      "description": "Whether a Paired client may control the Plane right now.",
      "oneOf": [
        {
          "description": "It may.",
          "type": "string",
          "const": "enabled"
        },
        {
          "description": "It may not. The Plane keeps its key, so enabling it again needs no\nnew pairing.",
          "type": "string",
          "const": "disabled"
        }
      ]
    },
    "PairingDisclosure": {
      "description": "The one-time secret as the Plane hands it to the owner who opened the\noffer. It is disclosed once: a retry of the Command that opened the\noffer is answered without it.",
      "oneOf": [
        {
          "description": "The eight-digit code, grouped as `xxxx-yyyy`.",
          "type": "object",
          "properties": {
            "code": {
              "description": "The code to read out.",
              "type": "string"
            },
            "disclosure": {
              "type": "string",
              "const": "manual_code"
            }
          },
          "required": [
            "disclosure",
            "code"
          ]
        },
        {
          "description": "The versioned payload to render as a QR code.",
          "type": "object",
          "properties": {
            "disclosure": {
              "type": "string",
              "const": "qr_payload"
            },
            "payload": {
              "description": "The payload to render.",
              "type": "string"
            }
          },
          "required": [
            "disclosure",
            "payload"
          ]
        },
        {
          "description": "The Plane already disclosed this offer's secret. Open another offer\nto be given one.",
          "type": "object",
          "properties": {
            "disclosure": {
              "type": "string",
              "const": "already_disclosed"
            }
          },
          "required": [
            "disclosure"
          ]
        }
      ]
    },
    "PairingEnd": {
      "description": "Why a Pairing offer is over.",
      "oneOf": [
        {
          "description": "Its window passed.",
          "type": "string",
          "const": "expired"
        },
        {
          "description": "Too many wrong secrets were presented against it.",
          "type": "string",
          "const": "too_many_attempts"
        },
        {
          "description": "The owner closed the Plane's Pairing gate while it was open.",
          "type": "string",
          "const": "gate_closed"
        }
      ]
    },
    "PairingGate": {
      "description": "Whether a Plane accepts new Pairings right now.",
      "oneOf": [
        {
          "description": "A new GUI client may begin Pairing.",
          "type": "string",
          "const": "open"
        },
        {
          "description": "It may not. A Plane starts here, so an owner opens the gate for as\nlong as a pairing takes rather than leaving it open.",
          "type": "string",
          "const": "closed"
        }
      ]
    },
    "PairingKeyAlgorithm": {
      "description": "The signature algorithm one Client identity signs with, retained beside\nthe key so a Plane that later speaks a second one can tell which key is\nwhich.",
      "oneOf": [
        {
          "description": "Ed25519, the only algorithm a v1 Client identity uses.",
          "type": "string",
          "const": "ed25519"
        }
      ]
    },
    "PairingMethod": {
      "description": "How the Plane hands one offer's one-time secret to the person pairing.",
      "oneOf": [
        {
          "description": "An eight-digit numeric code, read off the target and typed into the\nGUI client.",
          "type": "object",
          "properties": {
            "method": {
              "type": "string",
              "const": "manual_code"
            }
          },
          "required": [
            "method"
          ]
        },
        {
          "description": "A versioned payload the GUI client scans.",
          "type": "object",
          "properties": {
            "endpoint": {
              "description": "The reachable endpoint the payload advertises.",
              "type": "string"
            },
            "method": {
              "type": "string",
              "const": "qr_payload"
            }
          },
          "required": [
            "method",
            "endpoint"
          ]
        }
      ]
    },
    "PairingProgress": {
      "description": "How far the Plane's one Pairing offer has got.",
      "oneOf": [
        {
          "description": "Issued, and waiting for a client to present its secret.",
          "type": "object",
          "properties": {
            "progress": {
              "type": "string",
              "const": "offered"
            }
          },
          "required": [
            "progress"
          ]
        },
        {
          "description": "A client presented the secret. Both sides now display the\nauthentication string for the people at each end to compare.",
          "type": "object",
          "properties": {
            "authentication_string": {
              "description": "What both sides display.",
              "type": "string"
            },
            "client_id": {
              "description": "The Client identity that claimed the offer.",
              "type": "string",
              "format": "uuid"
            },
            "progress": {
              "type": "string",
              "const": "awaiting_confirmation"
            }
          },
          "required": [
            "progress",
            "client_id",
            "authentication_string"
          ]
        },
        {
          "description": "The person at the target agreed that both screens show the same\nstring. The Pairing now waits for the client to prove that it holds\nthe identity it presented.",
          "type": "object",
          "properties": {
            "authentication_string": {
              "description": "What both sides displayed.",
              "type": "string"
            },
            "client_id": {
              "description": "The Client identity that claimed the offer.",
              "type": "string",
              "format": "uuid"
            },
            "progress": {
              "type": "string",
              "const": "confirmed"
            }
          },
          "required": [
            "progress",
            "client_id",
            "authentication_string"
          ]
        },
        {
          "description": "Over. It can only be replaced by a new offer.",
          "type": "object",
          "properties": {
            "progress": {
              "type": "string",
              "const": "ended"
            },
            "reason": {
              "description": "Why it is over.",
              "$ref": "#/$defs/PairingEnd"
            }
          },
          "required": [
            "progress",
            "reason"
          ]
        }
      ]
    },
    "PairingSnapshot": {
      "description": "One Plane's Pairing as it stands, fenced by a journal cursor\n(ADR-0092).",
      "type": "object",
      "properties": {
        "clients": {
          "description": "The clients this Plane has Paired with, oldest pairing first.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PairedClient"
          }
        },
        "cursor": {
          "description": "Newest Event sequence visible when the snapshot was read, carried as\na decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "gate": {
          "description": "Whether a new GUI client may begin Pairing.",
          "$ref": "#/$defs/PairingGate"
        },
        "pending": {
          "description": "The offer the Plane has open, if any.",
          "anyOf": [
            {
              "$ref": "#/$defs/PendingPairing"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "cursor",
        "gate"
      ]
    },
    "PendingPairing": {
      "description": "The Plane's one Pairing offer, without the secret it was issued with.",
      "type": "object",
      "properties": {
        "attempts_remaining": {
          "description": "How many wrong secrets it still survives.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "expires_at_unix_ms": {
          "description": "When its current step stops being accepted, in signed Unix\nmilliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "method": {
          "description": "How its secret reached the person pairing.",
          "$ref": "#/$defs/PairingMethod"
        },
        "offer_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        },
        "opened_at_unix_ms": {
          "description": "When it was opened, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "progress": {
          "description": "How far it has got.",
          "$ref": "#/$defs/PairingProgress"
        }
      },
      "required": [
        "offer_id",
        "method",
        "progress",
        "attempts_remaining",
        "opened_at_unix_ms",
        "expires_at_unix_ms"
      ]
    },
    "PlaneStatus": {
      "description": "Wire form of the Plane status snapshot.",
      "type": "object",
      "properties": {
        "core_version": {
          "description": "Version of the running core.",
          "type": "string"
        },
        "cursor": {
          "description": "Newest Event sequence visible when the status was read, carried as a\ndecimal string (ADR-0089). Absent only on negotiated minor zero.",
          "type": [
            "string",
            "null"
          ],
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "daemon_starts": {
          "description": "How many times an authoritative `jetd` has started on this Plane.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "plane_id": {
          "description": "Durable identity of the Plane, created when its store was created.",
          "type": "string",
          "format": "uuid"
        },
        "security": {
          "description": "Whether the Plane can vouch for its own Security audit. Absent on a\nminor that does not name the Security audit.",
          "anyOf": [
            {
              "$ref": "#/$defs/SecurityState"
            },
            {
              "type": "null"
            }
          ]
        },
        "started_at_unix_ms": {
          "description": "When the current `jetd` started, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "plane_id",
        "daemon_starts",
        "started_at_unix_ms",
        "core_version"
      ]
    },
    "PlaneUsage": {
      "description": "What one Plane knows about Usage for the selected scope.",
      "type": "object",
      "properties": {
        "consumption": {
          "description": "Deduplicated Jet-observed consumption for the selection.",
          "$ref": "#/$defs/ObservedConsumption"
        },
        "cursor": {
          "description": "Newest Event sequence visible when the snapshot was read, carried\nas a decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "plane_id": {
          "description": "The Plane every record here was observed on. A total covers this\nPlane alone (ADR-0016).",
          "type": "string",
          "format": "uuid"
        },
        "quota_windows": {
          "description": "The freshest Provider response about each window of each selected\nAccount binding.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/QuotaWindow"
          }
        }
      },
      "required": [
        "cursor",
        "plane_id",
        "quota_windows",
        "consumption"
      ]
    },
    "Platform": {
      "description": "The operating system and processor a Plane runs on.",
      "type": "object",
      "properties": {
        "architecture": {
          "description": "Processor architecture, such as `aarch64`.",
          "type": "string"
        },
        "operating_system": {
          "description": "Operating system name, such as `linux` or `macos`.",
          "type": "string"
        }
      },
      "required": [
        "operating_system",
        "architecture"
      ]
    },
    "Project": {
      "description": "One registered Project.",
      "type": "object",
      "properties": {
        "project_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        },
        "registered_at_unix_ms": {
          "description": "When it was registered, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "registered_by": {
          "description": "The interactive Actor whose Path grant registered it.",
          "$ref": "#/$defs/Actor"
        },
        "root": {
          "description": "The canonical absolute root of its working tree, as the Plane\nresolved the granted path.",
          "type": "string"
        }
      },
      "required": [
        "project_id",
        "root",
        "registered_by",
        "registered_at_unix_ms"
      ]
    },
    "ProjectEntry": {
      "description": "One entry inside a registered Project, addressed by the Project and a\npath relative to its root. It carries metadata and never content.",
      "type": "object",
      "properties": {
        "cursor": {
          "description": "Newest Event sequence visible when the Project was read, carried as\na decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "kind": {
          "description": "What the path names right now.",
          "$ref": "#/$defs/EntryKind"
        },
        "path": {
          "description": "The path as it was asked for.",
          "type": "string"
        },
        "project_id": {
          "description": "The Project the path was resolved in.",
          "type": "string",
          "format": "uuid"
        }
      },
      "required": [
        "cursor",
        "project_id",
        "path",
        "kind"
      ]
    },
    "ProjectList": {
      "description": "Every registered Project on one Plane, fenced by a journal cursor\n(ADR-0092).",
      "type": "object",
      "properties": {
        "cursor": {
          "description": "Newest Event sequence visible when the snapshot was read, carried as\na decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "projects": {
          "description": "The Projects in the order they were registered.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Project"
          }
        }
      },
      "required": [
        "cursor",
        "projects"
      ]
    },
    "ProjectPreview": {
      "description": "What a Path grant would register, shown before anything is recorded.",
      "type": "object",
      "properties": {
        "registrability": {
          "description": "Whether that directory can be a Project, and what it is if so.",
          "$ref": "#/$defs/Registrability"
        },
        "root": {
          "description": "The canonical directory the granted path resolves to.",
          "type": "string"
        }
      },
      "required": [
        "root",
        "registrability"
      ]
    },
    "PromotedChange": {
      "description": "One path a promotion changes in the destination.",
      "type": "object",
      "properties": {
        "kind": {
          "description": "What happens to it.",
          "$ref": "#/$defs/ChangeKind"
        },
        "path": {
          "description": "The path, as Git spells it.",
          "type": "string"
        }
      },
      "required": [
        "path",
        "kind"
      ]
    },
    "PromotionBinding": {
      "description": "What a preview showed and a promotion carries back: the Workspace and\ndestination as they stood, the result and the risk the user looked\nat, and the client it was shown to. The Plane refuses a promotion when\nany of it has changed since.",
      "type": "object",
      "properties": {
        "actor": {
          "description": "The Client identity the preview was shown to.",
          "type": "string",
          "format": "uuid"
        },
        "base_commit": {
          "description": "The commit the Workspace started from, which the merge is against.",
          "type": "string"
        },
        "conflicts": {
          "description": "The paths the promotion cannot settle. A preview with any is shown\nand never applied.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PromotionConflict"
          }
        },
        "destination": {
          "description": "Where its changes go.",
          "$ref": "#/$defs/PromotionDestination"
        },
        "destination_commit": {
          "description": "The commit the destination had checked out or pointed at.",
          "type": "string"
        },
        "destination_dirty": {
          "description": "Whether the destination holds uncommitted changes of its own, which\nthe promotion is merged over and never discards.",
          "type": "boolean"
        },
        "destination_tree": {
          "description": "The destination's content as one Git tree: its working tree for the\nLocal checkout, the tip's tree for a branch.",
          "type": "string"
        },
        "result_tree": {
          "description": "The tree the three-way merge produced.",
          "type": "string"
        },
        "workspace_id": {
          "description": "The Workspace being promoted.",
          "type": "string",
          "format": "uuid"
        },
        "workspace_tree": {
          "description": "The Workspace's working tree, captured as one Git tree.",
          "type": "string"
        }
      },
      "required": [
        "workspace_id",
        "destination",
        "base_commit",
        "workspace_tree",
        "destination_commit",
        "destination_tree",
        "result_tree",
        "destination_dirty",
        "conflicts",
        "actor"
      ]
    },
    "PromotionConflict": {
      "description": "One path a promotion cannot settle without a person.",
      "type": "object",
      "properties": {
        "kind": {
          "description": "Why it cannot be settled.",
          "$ref": "#/$defs/ConflictKind"
        },
        "path": {
          "description": "The path, as Git spells it.",
          "type": "string"
        }
      },
      "required": [
        "path",
        "kind"
      ]
    },
    "PromotionDestination": {
      "description": "Where a promotion applies a Workspace's changes.",
      "oneOf": [
        {
          "description": "The Project's own Local checkout. The changes arrive staged in its\nindex and written to its files, merged over whatever it holds.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "local_checkout"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "A branch of the Project that no working tree has checked out. The\nchanges arrive as one commit on top of the branch.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "branch"
            },
            "name": {
              "description": "The branch name, as Git spells it without `refs/heads/`.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ]
        }
      ]
    },
    "PromotionPreview": {
      "description": "What promoting a Workspace would do, shown before it is done.",
      "type": "object",
      "properties": {
        "binding": {
          "description": "What the promotion is bound to, risk included.",
          "$ref": "#/$defs/PromotionBinding"
        },
        "changed_paths": {
          "description": "How many paths the promotion changes in the destination.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "changes": {
          "description": "The changes, up to the first 4096 of them, in Git's order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PromotedChange"
          }
        },
        "cursor": {
          "description": "Newest Event sequence visible when the Workspace was read, carried\nas a decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        }
      },
      "required": [
        "cursor",
        "binding",
        "changed_paths",
        "changes"
      ]
    },
    "PromotionState": {
      "description": "Where a promotion stands.",
      "oneOf": [
        {
          "description": "Recorded, with the Effect that applies it not yet settled.",
          "type": "string",
          "const": "applying"
        },
        {
          "description": "Applied, and the destination verified to hold the result.",
          "type": "string",
          "const": "promoted"
        },
        {
          "description": "Never applied: the preview could not settle every path, and the\npaths are kept with the promotion for the user to resolve in the\nWorkspace.",
          "type": "string",
          "const": "conflicted"
        },
        {
          "description": "Its Effect reported a definite failure before changing anything;\nthe destination is as it was.",
          "type": "string",
          "const": "failed"
        },
        {
          "description": "Its Effect's outcome could not be established. The Plane neither\nrepeats it nor calls it failed; the destination is the user's to\ninspect.",
          "type": "string",
          "const": "outcome_unknown"
        }
      ]
    },
    "QueryRequest": {
      "description": "Queries a client may run.",
      "oneOf": [
        {
          "description": "Inspect native source, components and requested access before a mutation.",
          "type": "object",
          "properties": {
            "craft_id": {
              "description": "Accepted Craft identity.",
              "type": "string"
            },
            "extension_id": {
              "description": "Exact native target.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "inspect_extension"
            }
          },
          "required": [
            "type",
            "craft_id",
            "extension_id"
          ]
        },
        {
          "description": "Discover extensions in native sources through the responsible Craft.",
          "type": "object",
          "properties": {
            "craft_id": {
              "description": "Accepted Craft identity.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "extension_catalog"
            }
          },
          "required": [
            "type",
            "craft_id"
          ]
        },
        {
          "description": "Inspect a staged extension mutation.",
          "type": "object",
          "properties": {
            "change_id": {
              "description": "Durable operation identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "extension_change"
            }
          },
          "required": [
            "type",
            "change_id"
          ]
        },
        {
          "description": "Inspect the exact remote action awaiting a destination review.",
          "type": "object",
          "properties": {
            "client_id": {
              "description": "Paired installation that submitted it.",
              "type": "string",
              "format": "uuid"
            },
            "operation_id": {
              "description": "Remote operation identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "remote_tool_review"
            }
          },
          "required": [
            "type",
            "client_id",
            "operation_id"
          ]
        },
        {
          "description": "Read a durable Utility result.",
          "type": "object",
          "properties": {
            "job_id": {
              "description": "Plane-assigned job identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "utility"
            }
          },
          "required": [
            "type",
            "job_id"
          ]
        },
        {
          "description": "Verify one third-party Craft source and return its exact consent surface.",
          "type": "object",
          "properties": {
            "source": {
              "description": "Repository release or explicit Developer Mode source.",
              "$ref": "#/$defs/CraftSource"
            },
            "type": {
              "type": "string",
              "const": "discover_craft"
            }
          },
          "required": [
            "type",
            "source"
          ]
        },
        {
          "description": "Enabled schedules in one Conversation.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "Owning Conversation.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "scheduled_tasks"
            }
          },
          "required": [
            "type",
            "conversation_id"
          ]
        },
        {
          "description": "Read bounded UTF-8 file content through a registered root.",
          "type": "object",
          "properties": {
            "path": {
              "description": "Path relative to that root.",
              "type": "string"
            },
            "target": {
              "description": "Registered Project or Workspace root.",
              "$ref": "#/$defs/FileTarget"
            },
            "type": {
              "type": "string",
              "const": "editable_file"
            }
          },
          "required": [
            "type",
            "target",
            "path"
          ]
        },
        {
          "description": "All retained terminals of one Workspace (at most 64).",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "workspace_terminals"
            },
            "workspace_id": {
              "description": "Registered Workspace identity.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "type",
            "workspace_id"
          ]
        },
        {
          "description": "Read a bounded chunk of a checkpoint patch Artifact.",
          "type": "object",
          "properties": {
            "offset": {
              "description": "Byte offset in decimal-string form.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "sha256": {
              "description": "Canonical SHA-256 content address.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "change_artifact"
            }
          },
          "required": [
            "type",
            "sha256",
            "offset"
          ]
        },
        {
          "description": "Compare observed Change checkpoints.",
          "type": "object",
          "properties": {
            "run_id": {
              "description": "Owning Run.",
              "type": "string",
              "format": "uuid"
            },
            "scope": {
              "description": "Boundaries to compare.",
              "$ref": "#/$defs/DiffScope"
            },
            "type": {
              "type": "string",
              "const": "change_diff"
            }
          },
          "required": [
            "type",
            "run_id",
            "scope"
          ]
        },
        {
          "description": "Continue changed-file metadata at an opaque, expiring snapshot cursor.",
          "type": "object",
          "properties": {
            "cursor": {
              "description": "Cursor supplied by the preceding diff page.",
              "$ref": "#/$defs/PageCursor"
            },
            "type": {
              "type": "string",
              "const": "next_change_diff"
            }
          },
          "required": [
            "type",
            "cursor"
          ]
        },
        {
          "description": "Current queue, with a position given by vector order.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "Conversation whose queue is read.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "turn_queue"
            }
          },
          "required": [
            "type",
            "conversation_id"
          ]
        },
        {
          "description": "Bounded Orphaned-execution metadata for interactive decisions.",
          "type": "object",
          "properties": {
            "after": {
              "description": "Continue after a previous page.",
              "type": [
                "string",
                "null"
              ],
              "format": "uuid",
              "default": null
            },
            "type": {
              "type": "string",
              "const": "orphaned_executions"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "Read the durable execution state of a managed Run.",
          "type": "object",
          "properties": {
            "run_id": {
              "description": "Run identity.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "run_execution"
            }
          },
          "required": [
            "type",
            "run_id"
          ]
        },
        {
          "description": "Snapshot of the Plane's daemon status.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "status"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "First bounded page of Conversations on the Plane.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "conversations"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "Continue a fenced Conversation keyset snapshot.",
          "type": "object",
          "properties": {
            "cursor": {
              "description": "Opaque token returned by the previous page.",
              "$ref": "#/$defs/PageCursor"
            },
            "type": {
              "type": "string",
              "const": "next_conversations"
            }
          },
          "required": [
            "type",
            "cursor"
          ]
        },
        {
          "description": "One Conversation with all of its Runs.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "The Conversation to read.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "conversation"
            }
          },
          "required": [
            "type",
            "conversation_id"
          ]
        },
        {
          "description": "What the Plane can do.",
          "type": "object",
          "properties": {
            "observation": {
              "description": "Whether to report the last observation or take a new one.",
              "$ref": "#/$defs/CapabilityObservation"
            },
            "type": {
              "type": "string",
              "const": "capabilities"
            }
          },
          "required": [
            "type",
            "observation"
          ]
        },
        {
          "description": "Every Account binding on the Plane, with the state of the Credential\neach one resolves.",
          "type": "object",
          "properties": {
            "observation": {
              "description": "Whether the Credential states follow the last observation of the\nPlane or a new one, taken now.",
              "$ref": "#/$defs/CapabilityObservation"
            },
            "type": {
              "type": "string",
              "const": "account_bindings"
            }
          },
          "required": [
            "type",
            "observation"
          ]
        },
        {
          "description": "The Usage records the Plane holds for one scope, with the freshness\nand estimation each of them carries.",
          "type": "object",
          "properties": {
            "selection": {
              "description": "What the answer covers.",
              "$ref": "#/$defs/UsageSelection"
            },
            "type": {
              "type": "string",
              "const": "usage"
            }
          },
          "required": [
            "type",
            "selection"
          ]
        },
        {
          "description": "Settings resolved for one scope.",
          "type": "object",
          "properties": {
            "scope": {
              "description": "The scope to resolve for; its own values win over the Plane's.",
              "$ref": "#/$defs/SettingScope"
            },
            "selection": {
              "description": "Which Settings to resolve.",
              "$ref": "#/$defs/SettingSelection"
            },
            "type": {
              "type": "string",
              "const": "settings"
            }
          },
          "required": [
            "type",
            "scope",
            "selection"
          ]
        },
        {
          "description": "A page of journal Events strictly after a sequence.",
          "type": "object",
          "properties": {
            "after": {
              "description": "The sequence to resume after, carried as a decimal string\n(ADR-0089); `\"0\"` for the whole journal.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "type": {
              "type": "string",
              "const": "events"
            }
          },
          "required": [
            "type",
            "after"
          ]
        },
        {
          "description": "The Plane's Pairing: whether it accepts new GUI clients.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "pairing"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "A page of the owner-only Security audit strictly after a position.",
          "type": "object",
          "properties": {
            "after": {
              "description": "The position to resume after, carried as a decimal string\n(ADR-0089); `\"0\"` for the whole audit.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "type": {
              "type": "string",
              "const": "security_audit"
            }
          },
          "required": [
            "type",
            "after"
          ]
        },
        {
          "description": "Every registered Project on the Plane.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "projects"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "What registering the Git working tree at an absolute path would\nrecord, before the Path grant is made: the directory it resolves to\nand what the Plane's Git says about it.",
          "type": "object",
          "properties": {
            "observation": {
              "description": "Whether Git LFS is reported from the last observation of the\nPlane or a new one, taken now.",
              "$ref": "#/$defs/CapabilityObservation"
            },
            "path": {
              "description": "The absolute path the user is about to grant.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "preview_project"
            }
          },
          "required": [
            "type",
            "path",
            "observation"
          ]
        },
        {
          "description": "What one path inside a Project names. This is how every ordinary\nfile operation addresses a file: a Project and a path relative to\nits root, which the Plane validates before touching the filesystem.",
          "type": "object",
          "properties": {
            "path": {
              "description": "The path relative to the Project's root, with `/` between its\ncomponents.",
              "type": "string"
            },
            "project_id": {
              "description": "The Project to resolve the path in.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "project_entry"
            }
          },
          "required": [
            "type",
            "project_id",
            "path"
          ]
        },
        {
          "description": "What promoting a Workspace to a permanent checkout or branch of its\nProject would do, before it is done. The answer binds what it\ncompared, and a promotion carries that binding back.",
          "type": "object",
          "properties": {
            "destination": {
              "description": "Where its changes would go.",
              "$ref": "#/$defs/PromotionDestination"
            },
            "type": {
              "type": "string",
              "const": "preview_promotion"
            },
            "workspace_id": {
              "description": "The Workspace to promote.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "type",
            "workspace_id",
            "destination"
          ]
        },
        {
          "description": "Bounded ranked hits over this Plane's human-visible Conversation\ncontent. A GUI merges the answers of every Plane it is connected\nto.",
          "type": "object",
          "properties": {
            "text": {
              "description": "At most 256 characters, whose whitespace-separated terms every\nhit must contain, sixteen at most.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "search"
            }
          },
          "required": [
            "type",
            "text"
          ]
        },
        {
          "description": "The Harness-native Conversations the Plane can see outside its\nmanagement, placed against its Projects, and the imports it holds.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "external_conversations"
            }
          },
          "required": [
            "type"
          ]
        }
      ]
    },
    "QueryResponse": {
      "description": "Query snapshots.",
      "oneOf": [
        {
          "description": "Unconverted native catalog and metadata.",
          "type": "object",
          "properties": {
            "craft_id": {
              "description": "Installed Craft identity.",
              "type": "string"
            },
            "harness": {
              "description": "Harness whose native formats this catalog describes.",
              "type": "string"
            },
            "native_metadata": {
              "description": "Bounded native JSON containing sources, versions, publishers and components.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "extension_catalog"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "craft_id",
            "harness",
            "native_metadata"
          ]
        },
        {
          "description": "Deferred native mutation outcome.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "extension_change"
            }
          },
          "$ref": "#/$defs/ExtensionChange",
          "required": [
            "type"
          ]
        },
        {
          "description": "Exact remote action awaiting a destination review.",
          "type": "object",
          "properties": {
            "action": {
              "description": "Closed operation vocabulary; arbitrary Commands cannot be forwarded.",
              "$ref": "#/$defs/RemoteToolAction"
            },
            "destination_plane_id": {
              "description": "Expected destination, checked before accessing its state.",
              "type": "string",
              "format": "uuid"
            },
            "operation_id": {
              "description": "Stable identity; an uncertain mutation must never get a new identity automatically.",
              "type": "string",
              "format": "uuid"
            },
            "origin": {
              "description": "Actor provenance derived by the Home daemon from the accepted Run.",
              "$ref": "#/$defs/NoVisaOrigin"
            },
            "permissions": {
              "description": "Accepted Craft permissions, supplied by the Home daemon, never the Craft.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/BrokerPermission"
              }
            },
            "type": {
              "type": "string",
              "const": "remote_tool_review"
            },
            "workspace_id": {
              "description": "Registered destination Workspace.",
              "type": "string",
              "format": "uuid"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "operation_id",
            "origin",
            "destination_plane_id",
            "workspace_id",
            "permissions",
            "action"
          ]
        },
        {
          "description": "Attributed Utility result, with no execution authority.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "utility"
            }
          },
          "$ref": "#/$defs/UtilityJob",
          "required": [
            "type"
          ]
        },
        {
          "description": "Verified immutable Craft installation proposal.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "craft_installation_preview"
            }
          },
          "$ref": "#/$defs/CraftInstallationPreview",
          "required": [
            "type"
          ]
        },
        {
          "description": "Fenced schedule snapshot.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "scheduled_tasks"
            }
          },
          "$ref": "#/$defs/ScheduledTasks",
          "required": [
            "type"
          ]
        },
        {
          "description": "Bounded editable content and its exact file Revision.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "editable_file"
            }
          },
          "$ref": "#/$defs/EditableFile",
          "required": [
            "type"
          ]
        },
        {
          "description": "Terminal lifecycle snapshots, separate from Runs.",
          "type": "object",
          "properties": {
            "cursor": {
              "description": "Snapshot Event cursor.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "terminals": {
              "description": "Retained Workspace terminals.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/WorkspaceTerminal"
              }
            },
            "type": {
              "type": "string",
              "const": "workspace_terminals"
            }
          },
          "required": [
            "type",
            "cursor",
            "terminals"
          ]
        },
        {
          "description": "Bounded Artifact bytes.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "change_artifact"
            }
          },
          "$ref": "#/$defs/ChangeArtifactChunk",
          "required": [
            "type"
          ]
        },
        {
          "description": "Immutable boundaries and patch preview.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "change_diff"
            }
          },
          "$ref": "#/$defs/ChangeDiff",
          "required": [
            "type"
          ]
        },
        {
          "description": "Fenced Turn queue snapshot.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "turn_queue"
            }
          },
          "$ref": "#/$defs/TurnQueue",
          "required": [
            "type"
          ]
        },
        {
          "description": "Read-only recovery candidates.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "orphaned_executions"
            }
          },
          "$ref": "#/$defs/OrphanedExecutions",
          "required": [
            "type"
          ]
        },
        {
          "description": "Durable lifecycle, activity, and Managed processes of a Run.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "run_execution"
            }
          },
          "$ref": "#/$defs/RunExecution",
          "required": [
            "type"
          ]
        },
        {
          "description": "Snapshot of the Plane's daemon status.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "status"
            }
          },
          "$ref": "#/$defs/PlaneStatus",
          "required": [
            "type"
          ]
        },
        {
          "description": "One page of Conversations on the Plane.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "conversations"
            }
          },
          "$ref": "#/$defs/ConversationList",
          "required": [
            "type"
          ]
        },
        {
          "description": "One Conversation with all of its Runs. Boxed: the Workspace it\ncarries, with its promotion, outweighs every other snapshot.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "conversation"
            }
          },
          "$ref": "#/$defs/ConversationSnapshot",
          "required": [
            "type"
          ]
        },
        {
          "description": "What the Plane can do.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "capabilities"
            }
          },
          "$ref": "#/$defs/CapabilitySnapshot",
          "required": [
            "type"
          ]
        },
        {
          "description": "Every Account binding on the Plane.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "account_bindings"
            }
          },
          "$ref": "#/$defs/AccountBindingList",
          "required": [
            "type"
          ]
        },
        {
          "description": "What one Plane knows about Usage for the selected scope. Boxed: it\ncarries the Plane's quota windows beside its per-Model totals.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "usage"
            }
          },
          "$ref": "#/$defs/PlaneUsage",
          "required": [
            "type"
          ]
        },
        {
          "description": "Settings resolved for one scope.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "settings"
            }
          },
          "$ref": "#/$defs/SettingSnapshot",
          "required": [
            "type"
          ]
        },
        {
          "description": "One page of journal Events in sequence order.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "events"
            }
          },
          "$ref": "#/$defs/EventPage",
          "required": [
            "type"
          ]
        },
        {
          "description": "The Plane's Pairing as it stands.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "pairing"
            }
          },
          "$ref": "#/$defs/PairingSnapshot",
          "required": [
            "type"
          ]
        },
        {
          "description": "One page of the Security audit, oldest first.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "security_audit"
            }
          },
          "$ref": "#/$defs/SecurityAudit",
          "required": [
            "type"
          ]
        },
        {
          "description": "Every registered Project on the Plane.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "projects"
            }
          },
          "$ref": "#/$defs/ProjectList",
          "required": [
            "type"
          ]
        },
        {
          "description": "What a Path grant would register.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "project_preview"
            }
          },
          "$ref": "#/$defs/ProjectPreview",
          "required": [
            "type"
          ]
        },
        {
          "description": "What one path inside a Project names.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "project_entry"
            }
          },
          "$ref": "#/$defs/ProjectEntry",
          "required": [
            "type"
          ]
        },
        {
          "description": "What promoting a Workspace would do. Boxed: the preview carries\ntwo lists and six object names, far more than any other snapshot.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "promotion_preview"
            }
          },
          "$ref": "#/$defs/PromotionPreview",
          "required": [
            "type"
          ]
        },
        {
          "description": "Bounded ranked hits, best match first.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "search"
            }
          },
          "$ref": "#/$defs/SearchResult",
          "required": [
            "type"
          ]
        },
        {
          "description": "What the Plane can see outside its management, and what it holds.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "external_conversations"
            }
          },
          "$ref": "#/$defs/ExternalConversationList",
          "required": [
            "type"
          ]
        }
      ]
    },
    "QuotaMeasure": {
      "description": "How full a Provider says one window is.",
      "type": "object",
      "properties": {
        "limit": {
          "description": "The limit it stated, where it stated one.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "unit": {
          "description": "The unit the Provider stated it in.",
          "$ref": "#/$defs/QuotaUnit"
        },
        "used": {
          "description": "How much of the window it reported as consumed.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "unit",
        "used"
      ]
    },
    "QuotaScope": {
      "description": "What one Provider-reported quota window covers.",
      "oneOf": [
        {
          "description": "The Provider account as a whole.",
          "type": "object",
          "properties": {
            "covers": {
              "type": "string",
              "const": "provider_account"
            }
          },
          "required": [
            "covers"
          ]
        },
        {
          "description": "One Model of that account.",
          "type": "object",
          "properties": {
            "covers": {
              "type": "string",
              "const": "model"
            },
            "model": {
              "description": "The Model the window limits.",
              "type": "string"
            }
          },
          "required": [
            "covers",
            "model"
          ]
        }
      ]
    },
    "QuotaUnit": {
      "description": "The unit a Provider stated one window in.",
      "oneOf": [
        {
          "description": "Inference tokens.",
          "type": "string",
          "const": "tokens"
        },
        {
          "description": "Requests.",
          "type": "string",
          "const": "requests"
        },
        {
          "description": "Provider-defined credits.",
          "type": "string",
          "const": "credits"
        },
        {
          "description": "Hundredths of a percent of the window, out of 10,000.",
          "type": "string",
          "const": "share"
        }
      ]
    },
    "QuotaWindow": {
      "description": "The freshest Provider response about one quota window. Windows are\nreported one by one and are never added together.",
      "type": "object",
      "properties": {
        "binding_id": {
          "description": "The Plane-local Account binding it belongs to.",
          "type": "string",
          "format": "uuid"
        },
        "conversation_id": {
          "description": "The Conversation the response was observed in.",
          "type": [
            "string",
            "null"
          ],
          "format": "uuid"
        },
        "estimation": {
          "description": "Whether the Provider measured it or Jet derived it.",
          "$ref": "#/$defs/UsageEstimation"
        },
        "finality": {
          "description": "Whether the window has closed.",
          "$ref": "#/$defs/UsageFinality"
        },
        "freshness": {
          "description": "Whether it still stands for what the Provider would say now.",
          "$ref": "#/$defs/UsageFreshness"
        },
        "measure": {
          "description": "How full the Provider said it was.",
          "$ref": "#/$defs/QuotaMeasure"
        },
        "observed_at_unix_ms": {
          "description": "When the Plane observed the response.",
          "type": "integer",
          "format": "int64"
        },
        "provider": {
          "description": "The Provider that reported it.",
          "type": "string"
        },
        "resets_at_unix_ms": {
          "description": "When it refills, where the Provider stated it.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        },
        "run_id": {
          "description": "The Run the response was observed in.",
          "type": [
            "string",
            "null"
          ],
          "format": "uuid"
        },
        "scope": {
          "description": "What the window covers.",
          "$ref": "#/$defs/QuotaScope"
        },
        "window": {
          "description": "The Provider's own name for the window.",
          "type": "string"
        },
        "window_seconds": {
          "description": "How long the window lasts, where the Provider stated it.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "binding_id",
        "provider",
        "window",
        "scope",
        "measure",
        "estimation",
        "finality",
        "observed_at_unix_ms",
        "freshness"
      ]
    },
    "RecoveryAction": {
      "description": "Structured action a client may take to recover from an error.",
      "oneOf": [
        {
          "description": "Refresh a file before preparing another direct edit.",
          "type": "object",
          "properties": {
            "current_revision": {
              "description": "Exact file state now authoritative.",
              "$ref": "#/$defs/FileRevision"
            },
            "path": {
              "description": "Validated path relative to that root.",
              "type": "string"
            },
            "target": {
              "description": "Registered root to refresh.",
              "$ref": "#/$defs/FileTarget"
            },
            "type": {
              "type": "string",
              "const": "refresh_file"
            }
          },
          "required": [
            "type",
            "target",
            "path",
            "current_revision"
          ]
        },
        {
          "description": "Refresh current Conversation state before preparing another Command.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "Conversation whose current state should be queried.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "refresh_conversation"
            }
          },
          "required": [
            "type",
            "conversation_id"
          ]
        },
        {
          "description": "Refresh current Run state before preparing another Command.",
          "type": "object",
          "properties": {
            "run_id": {
              "description": "Run whose current state should be queried.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "refresh_run"
            }
          },
          "required": [
            "type",
            "run_id"
          ]
        },
        {
          "description": "Reconnect and resume the semantic Event stream after this cursor.",
          "type": "object",
          "properties": {
            "after": {
              "description": "Last Event cursor the disconnected client received completely.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "type": {
              "type": "string",
              "const": "resume_events"
            }
          },
          "required": [
            "type",
            "after"
          ]
        }
      ]
    },
    "Registrability": {
      "description": "Whether a granted directory can be a Project. What keeps it from\nregistering is data a client acts on, never a message it parses.",
      "oneOf": [
        {
          "description": "An ordinary working tree, described.",
          "type": "object",
          "properties": {
            "repository": {
              "description": "The working tree as the Plane's Git describes it.",
              "$ref": "#/$defs/Repository"
            },
            "verdict": {
              "type": "string",
              "const": "registrable"
            }
          },
          "required": [
            "verdict",
            "repository"
          ]
        },
        {
          "description": "Git finds no repository at the directory or above it.",
          "type": "object",
          "properties": {
            "verdict": {
              "type": "string",
              "const": "not_a_repository"
            }
          },
          "required": [
            "verdict"
          ]
        },
        {
          "description": "The directory carries a `.git` entry that Git cannot open, such as\na linked worktree whose repository is gone.",
          "type": "object",
          "properties": {
            "verdict": {
              "type": "string",
              "const": "broken_repository"
            }
          },
          "required": [
            "verdict"
          ]
        },
        {
          "description": "A bare repository, which has no working tree for Runs to use.",
          "type": "object",
          "properties": {
            "verdict": {
              "type": "string",
              "const": "bare_repository"
            }
          },
          "required": [
            "verdict"
          ]
        },
        {
          "description": "The directory lies inside a repository's own `.git` directory.",
          "type": "object",
          "properties": {
            "verdict": {
              "type": "string",
              "const": "inside_git_dir"
            }
          },
          "required": [
            "verdict"
          ]
        },
        {
          "description": "The directory lies inside a working tree without being its top. The\ngrant is for the directory named; the user may grant the top\ninstead.",
          "type": "object",
          "properties": {
            "toplevel": {
              "description": "The top of the working tree the directory lies in.",
              "type": "string"
            },
            "verdict": {
              "type": "string",
              "const": "inside_working_tree"
            }
          },
          "required": [
            "verdict",
            "toplevel"
          ]
        }
      ]
    },
    "RemoteEnvironment": {
      "description": "Explicit destination environment change; absent values remove a variable.",
      "type": "object",
      "properties": {
        "name": {
          "description": "Variable name, without equals signs or NULs.",
          "type": "string"
        },
        "value": {
          "description": "Explicit value or removal.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "RemoteGitOperation": {
      "description": "Read-only Git operations with bounded output.",
      "oneOf": [
        {
          "description": "Porcelain v1 status, including untracked paths.",
          "type": "string",
          "const": "status"
        },
        {
          "description": "Working-tree diff without external diff or text conversion commands.",
          "type": "string",
          "const": "diff"
        }
      ]
    },
    "RemotePairingRequest": {
      "description": "The two enrollment operations available before remote authentication.",
      "oneOf": [
        {
          "description": "Present the out-of-band one-time secret and this installation's key.",
          "type": "object",
          "properties": {
            "command_id": {
              "description": "Stable Command identity, retained across retries.",
              "type": "string",
              "format": "uuid"
            },
            "key": {
              "description": "The installation's public key.",
              "$ref": "#/$defs/ClientPublicKey"
            },
            "pairing": {
              "type": "string",
              "const": "claim"
            },
            "secret": {
              "description": "The manual code or QR payload, never retained in diagnostics.",
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "pairing",
            "command_id",
            "secret",
            "key"
          ]
        },
        {
          "description": "Prove the Pairing transcript after confirmation on the target.",
          "type": "object",
          "properties": {
            "command_id": {
              "description": "Stable Command identity.",
              "type": "string",
              "format": "uuid"
            },
            "offer_id": {
              "description": "The previously claimed offer.",
              "type": "string",
              "format": "uuid"
            },
            "pairing": {
              "type": "string",
              "const": "complete"
            },
            "signature": {
              "description": "Signature of the claim's signing bytes.",
              "type": "string",
              "pattern": "^[0-9a-f]{128}$"
            }
          },
          "additionalProperties": false,
          "required": [
            "pairing",
            "command_id",
            "offer_id",
            "signature"
          ]
        }
      ]
    },
    "RemotePairingResponse": {
      "description": "Enrollment-only reply; it never authorizes application streams.",
      "oneOf": [
        {
          "description": "The token was accepted; compare the string on both screens.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "claimed"
            },
            "pending": {
              "description": "The claim and its confirmation string.",
              "$ref": "#/$defs/PendingPairing"
            },
            "signing_bytes": {
              "description": "The bound Pairing transcript, avoiding any protected status Query.",
              "type": "array",
              "items": {
                "type": "integer",
                "format": "uint8",
                "maximum": 255,
                "minimum": 0
              }
            }
          },
          "required": [
            "kind",
            "pending",
            "signing_bytes"
          ]
        },
        {
          "description": "Pairing completed; reconnect with a fresh connection signature.",
          "type": "object",
          "properties": {
            "client": {
              "description": "The newly Paired client.",
              "$ref": "#/$defs/PairedClient"
            },
            "kind": {
              "type": "string",
              "const": "completed"
            }
          },
          "required": [
            "kind",
            "client"
          ]
        },
        {
          "description": "Enrollment was refused.",
          "type": "object",
          "properties": {
            "error": {
              "description": "Stable explanation.",
              "$ref": "#/$defs/WireError"
            },
            "kind": {
              "type": "string",
              "const": "rejected"
            }
          },
          "required": [
            "kind",
            "error"
          ]
        }
      ]
    },
    "RemoteToolAction": {
      "description": "Operations available through the remote broker.",
      "oneOf": [
        {
          "description": "One reviewed input batch in a real PTY, closed after the batch exits.",
          "type": "object",
          "properties": {
            "columns": {
              "description": "Fixed terminal dimensions.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "directory": {
              "description": "Workspace-relative cwd; empty selects its root.",
              "type": "string"
            },
            "input": {
              "description": "Exact terminal input, shown in destination review.",
              "type": "string"
            },
            "rows": {
              "description": "Fixed terminal dimensions.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "type": {
              "type": "string",
              "const": "terminal"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "directory",
            "input",
            "rows",
            "columns"
          ]
        },
        {
          "description": "Argument-array process invocation, reviewed exactly once at destination.",
          "type": "object",
          "properties": {
            "arguments": {
              "description": "Executable followed by literal arguments; never a shell command string.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "directory": {
              "description": "Workspace-relative cwd; empty selects its root.",
              "type": "string"
            },
            "environment": {
              "description": "Explicit changes to a minimal environment.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/RemoteEnvironment"
              }
            },
            "type": {
              "type": "string",
              "const": "process"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "arguments",
            "directory",
            "environment"
          ]
        },
        {
          "description": "Read-only Git inspection with fixed arguments and hooks disabled.",
          "type": "object",
          "properties": {
            "operation": {
              "description": "Closed inspection vocabulary.",
              "$ref": "#/$defs/RemoteGitOperation"
            },
            "type": {
              "type": "string",
              "const": "git"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "operation"
          ]
        },
        {
          "description": "Visibly labelled shell syntax, requiring exact destination review.",
          "type": "object",
          "properties": {
            "directory": {
              "description": "Workspace-relative directory; empty selects the Workspace root.",
              "type": "string"
            },
            "environment": {
              "description": "Explicit environment changes, applied to a minimal environment.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/RemoteEnvironment"
              }
            },
            "script": {
              "description": "Shell source, never interpolated into SSH or a process argument array.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "shell"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "directory",
            "environment",
            "script"
          ]
        },
        {
          "description": "Atomically replace at most 64 KiB inside the Workspace.",
          "type": "object",
          "properties": {
            "content": {
              "description": "New UTF-8 content.",
              "type": "string"
            },
            "path": {
              "description": "Workspace-relative file path.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "write_file"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "path",
            "content"
          ]
        },
        {
          "description": "Read at most 64 KiB of UTF-8 file content.",
          "type": "object",
          "properties": {
            "path": {
              "description": "Validated Workspace-relative path.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "read_file"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "path"
          ]
        }
      ]
    },
    "RemoteToolDecision": {
      "description": "A user decision on one immutable remote action.",
      "oneOf": [
        {
          "description": "Permit exactly this stored action once.",
          "type": "string",
          "const": "allow_once"
        },
        {
          "description": "Refuse this action permanently.",
          "type": "string",
          "const": "deny"
        }
      ]
    },
    "RemoteToolRequest": {
      "description": "One request from a trusted Home daemon over its paired connection.",
      "type": "object",
      "properties": {
        "action": {
          "description": "Closed operation vocabulary; arbitrary Commands cannot be forwarded.",
          "$ref": "#/$defs/RemoteToolAction"
        },
        "destination_plane_id": {
          "description": "Expected destination, checked before accessing its state.",
          "type": "string",
          "format": "uuid"
        },
        "operation_id": {
          "description": "Stable identity; an uncertain mutation must never get a new identity automatically.",
          "type": "string",
          "format": "uuid"
        },
        "origin": {
          "description": "Actor provenance derived by the Home daemon from the accepted Run.",
          "$ref": "#/$defs/NoVisaOrigin"
        },
        "permissions": {
          "description": "Accepted Craft permissions, supplied by the Home daemon, never the Craft.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/BrokerPermission"
          }
        },
        "workspace_id": {
          "description": "Registered destination Workspace.",
          "type": "string",
          "format": "uuid"
        }
      },
      "additionalProperties": false,
      "required": [
        "operation_id",
        "origin",
        "destination_plane_id",
        "workspace_id",
        "permissions",
        "action"
      ]
    },
    "RemoteToolResult": {
      "description": "Bounded response from one destination operation.",
      "oneOf": [
        {
          "description": "This action has not executed and needs a destination user decision.",
          "type": "object",
          "properties": {
            "operation_id": {
              "description": "Stable identity of the action to inspect and review.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "approval_required"
            }
          },
          "required": [
            "type",
            "operation_id"
          ]
        },
        {
          "description": "Bounded process output after native exit.",
          "type": "object",
          "properties": {
            "exit_code": {
              "description": "Native exit status, absent when terminated by a signal.",
              "type": [
                "integer",
                "null"
              ],
              "format": "int32"
            },
            "stderr": {
              "description": "At most 64 KiB, rendered with replacement for invalid UTF-8.",
              "type": "string"
            },
            "stdout": {
              "description": "At most 64 KiB, rendered with replacement for invalid UTF-8.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "process"
            }
          },
          "required": [
            "type",
            "stdout",
            "stderr"
          ]
        },
        {
          "description": "The file replacement and directory sync completed.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "written"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "UTF-8 content inside the selected Workspace.",
          "type": "object",
          "properties": {
            "content": {
              "description": "At most 64 KiB.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "file"
            }
          },
          "required": [
            "type",
            "content"
          ]
        }
      ]
    },
    "Repository": {
      "description": "A registrable working tree as the Plane's Git describes it.",
      "type": "object",
      "properties": {
        "checkout": {
          "description": "Whether a sparse checkout narrows it; reported, never changed.",
          "$ref": "#/$defs/Checkout"
        },
        "lfs": {
          "description": "Whether the Plane has Git LFS; the Plane never bundles it.",
          "$ref": "#/$defs/ToolAvailability"
        },
        "submodules": {
          "description": "The submodules its index holds, each as its Git link alone.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/GitLink"
          }
        },
        "worktree": {
          "description": "Whether the working tree is the repository's own or a linked one.",
          "$ref": "#/$defs/Worktree"
        }
      },
      "required": [
        "worktree",
        "checkout",
        "submodules",
        "lfs"
      ]
    },
    "ResolvedSetting": {
      "description": "One Setting as it applies to the scope a Query addressed.",
      "type": "object",
      "properties": {
        "key": {
          "description": "The Setting.",
          "$ref": "#/$defs/SettingKey"
        },
        "source": {
          "description": "The scope the value came from.",
          "$ref": "#/$defs/SettingSource"
        },
        "value": {
          "description": "Its value after precedence.",
          "$ref": "#/$defs/SettingValue"
        }
      },
      "required": [
        "key",
        "value",
        "source"
      ]
    },
    "RestartMetadata": {
      "description": "Stable metadata explaining why a snapshot must be restarted.",
      "oneOf": [
        {
          "description": "Required Event replay is no longer retained.",
          "type": "object",
          "properties": {
            "current_snapshot_revision": {
              "description": "Current Event high-water cursor for a replacement snapshot.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "minimum_available_cursor": {
              "description": "Oldest cursor from which continuous replay remains possible.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "reason": {
              "type": "string",
              "const": "cursor_expired"
            }
          },
          "required": [
            "reason",
            "minimum_available_cursor",
            "current_snapshot_revision"
          ]
        },
        {
          "description": "The supplied Event cursor belongs to a later or different Plane.",
          "type": "object",
          "properties": {
            "current_snapshot_revision": {
              "description": "Current Event high-water cursor for the replacement snapshot.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "reason": {
              "type": "string",
              "const": "cursor_ahead"
            }
          },
          "required": [
            "reason",
            "current_snapshot_revision"
          ]
        },
        {
          "description": "A later page no longer belongs to the current projection state.",
          "type": "object",
          "properties": {
            "current_snapshot_revision": {
              "description": "Current Event high-water cursor for the replacement first page.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "reason": {
              "type": "string",
              "const": "pagination_stale"
            }
          },
          "required": [
            "reason",
            "current_snapshot_revision"
          ]
        }
      ]
    },
    "RetentionPolicy": {
      "description": "Whether Jet keeps a Conversation after its final Run.",
      "oneOf": [
        {
          "description": "Keep the Conversation and its history. The default.",
          "type": "string",
          "const": "retain"
        },
        {
          "description": "Forget the Conversation once it has no live Run and no other\nprotected state.",
          "type": "string",
          "const": "forget_after_final_run"
        }
      ]
    },
    "ReviewComment": {
      "description": "One submitted inline review comment.",
      "type": "object",
      "properties": {
        "comment": {
          "description": "User-authored comment text.",
          "type": "string"
        },
        "line": {
          "description": "One-based line number in the reviewed file.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "path": {
          "description": "Validated path relative to the Conversation's working tree.",
          "type": "string"
        }
      },
      "required": [
        "path",
        "line",
        "comment"
      ]
    },
    "RevisionConflict": {
      "description": "Structured state returned when a Revision precondition is stale.",
      "type": "object",
      "properties": {
        "current_revision": {
          "description": "Revision that is authoritative now, carried as a decimal string\n(ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "safe_state": {
          "description": "Safe current state with which the caller can refresh.",
          "$ref": "#/$defs/ConflictState"
        }
      },
      "required": [
        "current_revision",
        "safe_state"
      ]
    },
    "Run": {
      "description": "One Run of a Conversation.",
      "type": "object",
      "properties": {
        "conversation_id": {
          "description": "The Conversation it executes.",
          "type": "string",
          "format": "uuid"
        },
        "created_at_unix_ms": {
          "description": "When it was created, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "ended_at_unix_ms": {
          "description": "When it reached a terminal state, if it has.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        },
        "lifecycle": {
          "description": "Current lifecycle state.",
          "$ref": "#/$defs/RunLifecycle"
        },
        "name": {
          "description": "Resolved name and its authority. Absent before protocol minor 20.",
          "anyOf": [
            {
              "$ref": "#/$defs/Name"
            },
            {
              "type": "null"
            }
          ]
        },
        "revision": {
          "description": "Monotonic version used by conflict-sensitive Commands, carried as a\ndecimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "run_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        }
      },
      "required": [
        "run_id",
        "conversation_id",
        "revision",
        "lifecycle",
        "created_at_unix_ms"
      ]
    },
    "RunActivity": {
      "description": "Why an active Run is working or waiting.",
      "oneOf": [
        {
          "description": "Performing work.",
          "type": "string",
          "const": "working"
        },
        {
          "description": "Needs user input.",
          "type": "string",
          "const": "waiting_for_user"
        },
        {
          "description": "Needs an approval decision.",
          "type": "string",
          "const": "waiting_for_approval"
        },
        {
          "description": "Needs authentication.",
          "type": "string",
          "const": "waiting_for_auth"
        },
        {
          "description": "Needs available quota.",
          "type": "string",
          "const": "waiting_for_quota"
        },
        {
          "description": "Reconnecting to its execution.",
          "type": "string",
          "const": "reconnecting"
        }
      ]
    },
    "RunControl": {
      "description": "What an interactive control request asks of one managed execution.",
      "oneOf": [
        {
          "description": "End the current turn and leave the Run able to accept the next one.",
          "type": "string",
          "const": "interrupt_turn"
        },
        {
          "description": "End the whole execution, including its native processes.",
          "type": "string",
          "const": "stop_run"
        }
      ]
    },
    "RunExecution": {
      "description": "Durable execution projection, fenced with the Event journal.",
      "type": "object",
      "properties": {
        "activity": {
          "description": "Present only while the Run is active.",
          "anyOf": [
            {
              "$ref": "#/$defs/RunActivity"
            },
            {
              "type": "null"
            }
          ]
        },
        "cursor": {
          "description": "Snapshot cursor.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "exit_code": {
          "description": "Native exit status when the OS supplied one.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int32"
        },
        "native_conversation": {
          "description": "Last native Conversation identity reported by its Craft.",
          "type": [
            "string",
            "null"
          ]
        },
        "needs_attention": {
          "description": "The live execution requires an interactive recovery decision.",
          "type": "boolean"
        },
        "no_visa": {
          "description": "Explicit No-Visa execution selection, absent for Visa Runs.",
          "anyOf": [
            {
              "$ref": "#/$defs/NoVisaSelection"
            },
            {
              "type": "null"
            }
          ]
        },
        "processes": {
          "description": "Processes retained as historical identities after completion.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ManagedProcess"
          }
        },
        "run": {
          "description": "Authoritative lifecycle and revision.",
          "$ref": "#/$defs/Run"
        },
        "termination": {
          "description": "How an interactive control request ended this execution. Absent\nbefore protocol minor 19, and while no request has settled.",
          "anyOf": [
            {
              "$ref": "#/$defs/RunTermination"
            },
            {
              "type": "null"
            }
          ]
        },
        "visa": {
          "description": "Explicit Visa selection; absent on legacy Runs and before minor 25.",
          "anyOf": [
            {
              "$ref": "#/$defs/VisaSelection"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "cursor",
        "run",
        "processes"
      ]
    },
    "RunLifecycle": {
      "description": "Mutually exclusive lifecycle state of one Run.",
      "oneOf": [
        {
          "description": "Recorded but not yet launching.",
          "type": "string",
          "const": "created"
        },
        {
          "description": "Launching its Harness.",
          "type": "string",
          "const": "starting"
        },
        {
          "description": "Executing.",
          "type": "string",
          "const": "active"
        },
        {
          "description": "Ending gracefully.",
          "type": "string",
          "const": "stopping"
        },
        {
          "description": "Terminal: finished its work.",
          "type": "string",
          "const": "completed"
        },
        {
          "description": "Terminal: ended with an error.",
          "type": "string",
          "const": "failed"
        },
        {
          "description": "Terminal: ended on request.",
          "type": "string",
          "const": "canceled"
        },
        {
          "description": "Terminal: its execution can no longer be observed.",
          "type": "string",
          "const": "lost"
        }
      ]
    },
    "RunTermination": {
      "description": "The exact terminal outcome of one control request, recorded once.",
      "type": "object",
      "properties": {
        "control": {
          "description": "The request this outcome answers.",
          "$ref": "#/$defs/RunControl"
        },
        "stage": {
          "description": "The step that actually ended the work.",
          "$ref": "#/$defs/TerminationStage"
        }
      },
      "required": [
        "control",
        "stage"
      ]
    },
    "ScheduleFiring": {
      "description": "One intended local occurrence, resolved once and persisted before delivery.",
      "type": "object",
      "properties": {
        "due_at_unix_ms": {
          "description": "Selected UTC instant in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "firing_id": {
          "description": "Deterministic identity, also the admitted Turn identity.",
          "type": "string",
          "format": "uuid"
        },
        "intended_local": {
          "description": "Original local date and time, including a nonexistent or repeated time.",
          "type": "string"
        }
      },
      "required": [
        "firing_id",
        "intended_local",
        "due_at_unix_ms"
      ]
    },
    "ScheduledTask": {
      "description": "An enabled daily Scheduled task. Cancel and create anew to change its rule.",
      "type": "object",
      "properties": {
        "authorized_by": {
          "description": "Client that authorized scheduled input.",
          "type": "string",
          "format": "uuid"
        },
        "conversation_id": {
          "description": "Durable owning Conversation.",
          "type": "string",
          "format": "uuid"
        },
        "local_time": {
          "description": "Daily local time in HH:MM:SS form.",
          "type": "string"
        },
        "next": {
          "description": "Next intended occurrence, retained unchanged across restarts.",
          "$ref": "#/$defs/ScheduleFiring"
        },
        "prompt": {
          "description": "Turn input, bounded to 8192 UTF-8 bytes.",
          "type": "string"
        },
        "schedule_id": {
          "description": "Immutable schedule identity.",
          "type": "string",
          "format": "uuid"
        },
        "time_zone": {
          "description": "Original IANA zone, independent of the Plane's current zone.",
          "type": "string"
        }
      },
      "required": [
        "schedule_id",
        "conversation_id",
        "authorized_by",
        "time_zone",
        "local_time",
        "prompt",
        "next"
      ]
    },
    "ScheduledTasks": {
      "description": "Enabled schedules and their snapshot fence.",
      "type": "object",
      "properties": {
        "cursor": {
          "description": "Plane Event high-water cursor in this read transaction.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "tasks": {
          "description": "At most 32 schedules in the addressed Conversation.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ScheduledTask"
          }
        }
      },
      "required": [
        "cursor",
        "tasks"
      ]
    },
    "SearchField": {
      "description": "What kind of human-visible content a hit matched.",
      "oneOf": [
        {
          "description": "A Conversation or Run name.",
          "type": "string",
          "const": "name"
        },
        {
          "description": "A file path the Conversation touched: its Workspace root or a path\nits promotion could not settle.",
          "type": "string",
          "const": "path"
        },
        {
          "description": "A Git branch the Conversation promoted its Workspace to.",
          "type": "string",
          "const": "branch"
        }
      ]
    },
    "SearchHit": {
      "description": "One ranked match, with the stable reference a client follows to it.",
      "type": "object",
      "properties": {
        "conversation_id": {
          "description": "The Conversation the content belongs to.",
          "type": "string",
          "format": "uuid"
        },
        "excerpt": {
          "description": "A bounded excerpt of the matched content.",
          "type": "string"
        },
        "field": {
          "description": "What kind of content matched.",
          "$ref": "#/$defs/SearchField"
        },
        "sequence": {
          "description": "The Event that carried the content, carried as a decimal string\n(ADR-0089). A client reads it from the journal or scrolls the\nConversation to it.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        }
      },
      "required": [
        "conversation_id",
        "sequence",
        "field",
        "excerpt"
      ]
    },
    "SearchResult": {
      "description": "One bounded page of hits, best match first, fenced by the journal\nposition it was read at (ADR-0092).",
      "type": "object",
      "properties": {
        "cursor": {
          "description": "Newest Event sequence visible when the index was read, carried as a\ndecimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "hits": {
          "description": "At most 64 hits, best match first.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/SearchHit"
          }
        },
        "indexed_through": {
          "description": "The journal position the index had been derived through, carried\nas a decimal string. It equals `cursor` unless indexing was\ninterrupted since the last Command.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        }
      },
      "required": [
        "cursor",
        "indexed_through",
        "hits"
      ]
    },
    "SecurityAudit": {
      "description": "One page of the Security audit, fenced by the position the audit had\nreached when the page was read. The page is the last one when its final\nrecord's sequence equals `cursor`.",
      "type": "object",
      "properties": {
        "cursor": {
          "description": "Newest audit position when the page was read, carried as a decimal\nstring (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "entries": {
          "description": "The records strictly after the requested position, oldest first.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/AuditEntry"
          }
        }
      },
      "required": [
        "cursor",
        "entries"
      ]
    },
    "SecurityState": {
      "description": "Whether a Plane can vouch for its own Security audit.",
      "oneOf": [
        {
          "description": "The audit chain folds through the head kept outside the store.",
          "type": "object",
          "properties": {
            "state": {
              "type": "string",
              "const": "trusted"
            }
          },
          "required": [
            "state"
          ]
        },
        {
          "description": "It does not, and the Plane is in Security-degraded mode: reads,\nexports and Runs already under way continue, while changes to\ntrust, policy, Craft, and anything destructive wait for an owner to\nexport the evidence and begin a new audit epoch.",
          "type": "object",
          "properties": {
            "breach": {
              "description": "What validation found.",
              "$ref": "#/$defs/AuditBreach"
            },
            "epoch": {
              "description": "The authority epoch that failed to validate, carried as a\ndecimal string (ADR-0089).",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "head": {
              "description": "The head published outside the store, when there still is one.",
              "anyOf": [
                {
                  "$ref": "#/$defs/AuditHead"
                },
                {
                  "type": "null"
                }
              ]
            },
            "state": {
              "type": "string",
              "const": "degraded"
            },
            "store_sequence": {
              "description": "The newest position the store itself holds, carried as a\ndecimal string.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            }
          },
          "required": [
            "state",
            "breach",
            "epoch",
            "store_sequence"
          ]
        }
      ]
    },
    "SeedSelection": {
      "description": "Which Local-checkout changes a new Workspace starts with.",
      "oneOf": [
        {
          "description": "No changes: the Workspace starts at its base alone.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "none"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "Every eligible change: modifications and deletions of tracked paths,\nuntracked paths that are not ignored, and the commit each submodule\nhas checked out. Ignored paths and nested repositories are left out.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "all_eligible"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "The named paths and whatever they hold. An ignored path is included\nbecause it was named; a named directory brings its unignored content.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "paths"
            },
            "paths": {
              "description": "Paths relative to the Project root, with `/` between components.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "kind",
            "paths"
          ]
        }
      ]
    },
    "ServerHello": {
      "description": "Server reply to a [`ClientHello`].",
      "oneOf": [
        {
          "description": "Remote endpoint access is established; Jet authorization is still pending.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "challenge"
            },
            "nonce": {
              "description": "A fresh 256-bit nonce, used once on this connection.",
              "type": "string",
              "pattern": "^[0-9a-f]{64}$"
            }
          },
          "required": [
            "kind",
            "nonce"
          ]
        },
        {
          "description": "The connection is authenticated and negotiated.",
          "type": "object",
          "properties": {
            "capabilities": {
              "description": "Protocol capability flags the server supports.",
              "type": "array",
              "default": [],
              "items": {
                "type": "string"
              }
            },
            "codec": {
              "description": "Selected codec.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "welcome"
            },
            "max_control_frame": {
              "description": "Negotiated control frame limit both peers honor when sending.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "max_data_frame": {
              "description": "Negotiated data frame limit both peers honor when sending.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "minor": {
              "description": "Selected minor of that major: the smaller of the two peers' newest\nminors, so neither side sends fields the other cannot read.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "protocol": {
              "description": "Selected protocol major.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "protocol",
            "minor",
            "codec",
            "max_control_frame",
            "max_data_frame"
          ]
        },
        {
          "description": "The server refuses the connection; it closes the stream afterwards.",
          "type": "object",
          "properties": {
            "error": {
              "description": "Why the handshake failed.",
              "$ref": "#/$defs/WireError"
            },
            "kind": {
              "type": "string",
              "const": "rejected"
            }
          },
          "required": [
            "kind",
            "error"
          ]
        }
      ]
    },
    "ServerMessage": {
      "description": "Control message sent by `jetd`.",
      "oneOf": [
        {
          "description": "Outcome of one No-Visa operation.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Request correlation.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "remote_tool_result"
            },
            "result": {
              "description": "Bounded destination result.",
              "$ref": "#/$defs/RemoteToolResult"
            }
          },
          "required": [
            "kind",
            "id",
            "result"
          ]
        },
        {
          "description": "The numbered stream now accepts terminal input and receives output.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Request correlation.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "terminal_attached"
            }
          },
          "required": [
            "kind",
            "id"
          ]
        },
        {
          "description": "The attached PTY accepted its new dimensions.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Request correlation.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "terminal_resized"
            }
          },
          "required": [
            "kind",
            "id"
          ]
        },
        {
          "description": "Successful Query snapshot.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Identifier of the request being answered.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "query_result"
            },
            "result": {
              "description": "The snapshot.",
              "$ref": "#/$defs/QueryResponse"
            }
          },
          "required": [
            "kind",
            "id",
            "result"
          ]
        },
        {
          "description": "Durable Command outcome.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Identifier of the request being answered.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "command_result"
            },
            "result": {
              "description": "The outcome.",
              "$ref": "#/$defs/CommandResponse"
            }
          },
          "required": [
            "kind",
            "id",
            "result"
          ]
        },
        {
          "description": "A request failed, or the connection is being refused.",
          "type": "object",
          "properties": {
            "error": {
              "description": "Stable error body.",
              "$ref": "#/$defs/WireError"
            },
            "id": {
              "description": "Identifier of the request that failed, if any.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "error"
            }
          },
          "required": [
            "kind",
            "error"
          ]
        }
      ]
    },
    "SettingKey": {
      "description": "A Setting this protocol minor names. A spelling it does not name is\nrefused rather than guessed (ADR-0094).",
      "oneOf": [
        {
          "description": "Plane-wide utility.account_binding policy (Utility minor).",
          "type": "string",
          "const": "utility.account_binding"
        },
        {
          "description": "Plane-wide utility.content_consent policy (Utility minor).",
          "type": "string",
          "const": "utility.content_consent"
        },
        {
          "description": "Plane-wide utility.autodelete_compilation policy (Utility minor).",
          "type": "string",
          "const": "utility.autodelete_compilation"
        },
        {
          "description": "Plane-wide utility.git_text policy (Utility minor).",
          "type": "string",
          "const": "utility.git_text"
        },
        {
          "description": "Whether the Utility model names Conversations automatically.",
          "type": "string",
          "const": "utility.automatic_naming"
        },
        {
          "description": "Whether Jet commits Harness changes without being asked.",
          "type": "string",
          "const": "git.auto_commit"
        },
        {
          "description": "Plane-wide guidance for generated commit messages and pull-request\ntext.",
          "type": "string",
          "const": "git.message_instructions"
        },
        {
          "description": "How many days the Plane keeps its Security audit.",
          "type": "string",
          "const": "security.audit_retention_days"
        },
        {
          "description": "Whether local and source-built third-party Crafts may be installed.",
          "type": "string",
          "const": "craft.developer_mode"
        },
        {
          "description": "Whether this Plane reviews eligible approval requests automatically.",
          "type": "string",
          "const": "review.automatic"
        },
        {
          "description": "Plane-wide review.account_binding reviewer selection.",
          "type": "string",
          "const": "review.account_binding"
        },
        {
          "description": "Persistent consent to review through a binding outside the Run's own\nProvider.",
          "type": "string",
          "const": "review.cross_provider_consent"
        }
      ]
    },
    "SettingScope": {
      "description": "Where a Setting value lives. A Command writes exactly the scope it\nnames; a Query resolves the Plane's values and then the values of the\nscope it names.",
      "oneOf": [
        {
          "description": "Everything on the Plane.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "plane"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "One registered Project.",
          "type": "object",
          "properties": {
            "project_id": {
              "description": "The Project the values apply to.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "project"
            }
          },
          "required": [
            "type",
            "project_id"
          ]
        },
        {
          "description": "One Conversation.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "The Conversation the values apply to.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "conversation"
            }
          },
          "required": [
            "type",
            "conversation_id"
          ]
        }
      ]
    },
    "SettingSelection": {
      "description": "Which Settings one Query resolves.",
      "oneOf": [
        {
          "description": "Every Setting the addressed scope may store.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "all"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "One Setting, refused when the addressed scope may not store it.",
          "type": "object",
          "properties": {
            "key": {
              "description": "The Setting to resolve.",
              "$ref": "#/$defs/SettingKey"
            },
            "type": {
              "type": "string",
              "const": "key"
            }
          },
          "required": [
            "type",
            "key"
          ]
        }
      ]
    },
    "SettingSnapshot": {
      "description": "Settings resolved for one scope, fenced by a journal cursor (ADR-0092).",
      "type": "object",
      "properties": {
        "cursor": {
          "description": "Newest Event sequence visible when the snapshot was read, carried as\na decimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "scope": {
          "description": "The scope the Settings were resolved for.",
          "$ref": "#/$defs/SettingScope"
        },
        "settings": {
          "description": "The resolved Settings.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ResolvedSetting"
          }
        }
      },
      "required": [
        "cursor",
        "scope",
        "settings"
      ]
    },
    "SettingSource": {
      "description": "Where a resolved value came from.",
      "oneOf": [
        {
          "description": "The Plane's built-in default; no scope stores a value.",
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "const": "built_in"
            }
          },
          "required": [
            "source"
          ]
        },
        {
          "description": "The value one scope stores.",
          "type": "object",
          "properties": {
            "scope": {
              "description": "The scope that stores it.",
              "$ref": "#/$defs/SettingScope"
            },
            "source": {
              "type": "string",
              "const": "scope"
            }
          },
          "required": [
            "source",
            "scope"
          ]
        }
      ]
    },
    "SettingValue": {
      "description": "One Setting's value. Each key holds exactly one of these shapes.",
      "oneOf": [
        {
          "description": "A yes-or-no choice.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "flag"
            },
            "value": {
              "type": "boolean"
            }
          },
          "required": [
            "type",
            "value"
          ]
        },
        {
          "description": "Bounded free text.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "text"
            },
            "value": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "value"
          ]
        },
        {
          "description": "A whole number of something, such as days.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "count"
            },
            "value": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "type",
            "value"
          ]
        }
      ]
    },
    "Sha256Digest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "StreamControl": {
      "description": "Stream-local control carried as strict JSON on a numbered stream.",
      "oneOf": [
        {
          "description": "Receiver permits this many additional raw bytes.",
          "type": "object",
          "properties": {
            "bytes": {
              "description": "Additional byte credit.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "type": {
              "type": "string",
              "const": "credit"
            }
          },
          "required": [
            "type",
            "bytes"
          ]
        },
        {
          "description": "Rolling terminal output omitted a contiguous byte range.",
          "type": "object",
          "properties": {
            "first_missing_offset": {
              "description": "Offset of the first omitted byte.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "missing_bytes": {
              "description": "Number of omitted bytes.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "type": {
              "type": "string",
              "const": "terminal_gap"
            }
          },
          "required": [
            "type",
            "first_missing_offset",
            "missing_bytes"
          ]
        },
        {
          "description": "A terminal stream ended at this source offset.",
          "type": "object",
          "properties": {
            "total_bytes": {
              "description": "Total source bytes, including any explicitly reported gaps.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "type": {
              "type": "string",
              "const": "terminal_finished"
            }
          },
          "required": [
            "type",
            "total_bytes"
          ]
        },
        {
          "description": "An Artifact stream ended with a declared size and integrity digest.",
          "type": "object",
          "properties": {
            "sha256": {
              "description": "SHA-256 digest computed across the complete content.",
              "$ref": "#/$defs/Sha256Digest"
            },
            "total_bytes": {
              "description": "Total bytes in the completed Artifact.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "type": {
              "type": "string",
              "const": "artifact_finished"
            }
          },
          "required": [
            "type",
            "total_bytes",
            "sha256"
          ]
        }
      ]
    },
    "TerminalState": {
      "description": "Durable terminal lifecycle, independent from Runs.",
      "oneOf": [
        {
          "description": "The durable launch Effect is pending.",
          "type": "string",
          "const": "opening"
        },
        {
          "description": "A live helper owns the PTY.",
          "type": "string",
          "const": "open"
        },
        {
          "description": "An explicit close is pending.",
          "type": "string",
          "const": "closing"
        },
        {
          "description": "The PTY ended; reconnect never launches another shell.",
          "type": "string",
          "const": "closed"
        },
        {
          "description": "Identity or transport could not be established safely.",
          "type": "string",
          "const": "unavailable"
        }
      ]
    },
    "TerminationStage": {
      "description": "How far Jet had to go before the execution actually stopped. Everything\npast `Interrupt` is a forced termination: the Harness was given the\nchance to end its own work and did not take it.",
      "oneOf": [
        {
          "description": "The Harness cancelled its own turn; no signal was sent and the Run\nremained available for later input.",
          "type": "string",
          "const": "native_cancellation"
        },
        {
          "description": "The native process ended after an interrupt signal.",
          "type": "string",
          "const": "interrupt"
        },
        {
          "description": "It ignored the interrupt and ended after a terminate signal.",
          "type": "string",
          "const": "terminate"
        },
        {
          "description": "It ignored both and was killed.",
          "type": "string",
          "const": "kill"
        },
        {
          "description": "Every signal was delivered and the execution still could not be\nobserved ending; later work requires a new Run.",
          "type": "string",
          "const": "unobserved"
        }
      ]
    },
    "ToolAvailability": {
      "description": "The result of looking for one external tool.",
      "oneOf": [
        {
          "description": "The tool answered with the version line it reports.",
          "type": "object",
          "properties": {
            "status": {
              "type": "string",
              "const": "present"
            },
            "version": {
              "description": "The tool's own version line, bounded and unparsed.",
              "type": "string"
            }
          },
          "required": [
            "status",
            "version"
          ]
        },
        {
          "description": "The tool could not be run at all.",
          "type": "object",
          "properties": {
            "status": {
              "type": "string",
              "const": "missing"
            }
          },
          "required": [
            "status"
          ]
        }
      ]
    },
    "Turn": {
      "description": "One durable input identity; the queue order is its sequence, never wall time.",
      "type": "object",
      "properties": {
        "client_id": {
          "description": "Authenticated client that authorized this input.",
          "type": "string",
          "format": "uuid"
        },
        "run_id": {
          "description": "Execution that claimed this input, when any.",
          "type": [
            "string",
            "null"
          ],
          "format": "uuid"
        },
        "sequence": {
          "description": "Monotonic within this Conversation, including replaced entries.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "source": {
          "description": "Slot selection, independent from authenticated attribution.",
          "$ref": "#/$defs/TurnSource"
        },
        "state": {
          "description": "Current state or recorded final outcome.",
          "$ref": "#/$defs/TurnState"
        },
        "turn_id": {
          "description": "Plane-assigned identity, also used for Craft correlation.",
          "type": "string",
          "format": "uuid"
        }
      },
      "required": [
        "turn_id",
        "sequence",
        "client_id",
        "source",
        "state"
      ]
    },
    "TurnOutcome": {
      "description": "Terminal outcome of one turn, independent of Run lifecycle.",
      "oneOf": [
        {
          "description": "The Harness completed its turn.",
          "type": "string",
          "const": "completed"
        },
        {
          "description": "The turn ended early with partial changes preserved.",
          "type": "string",
          "const": "interrupted"
        }
      ]
    },
    "TurnQueue": {
      "description": "A bounded queue snapshot. Vector order is queue position, active input first.",
      "type": "object",
      "properties": {
        "cursor": {
          "description": "Event cursor from the same read transaction.",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "turns": {
          "description": "At most 128 entries; prompts remain in the admission Events.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Turn"
          }
        }
      },
      "required": [
        "cursor",
        "turns"
      ]
    },
    "TurnSource": {
      "description": "The independently coalesced input class; it grants no Actor authority.",
      "oneOf": [
        {
          "description": "User input is never replaced.",
          "type": "string",
          "const": "user"
        },
        {
          "description": "The newest pending Scheduled-task input.",
          "type": "string",
          "const": "schedule"
        },
        {
          "description": "The newest pending Auto-continue input.",
          "type": "string",
          "const": "auto_continue"
        }
      ]
    },
    "TurnState": {
      "description": "A Turn's execution or final admission outcome.",
      "oneOf": [
        {
          "description": "Waiting in authoritative sequence order.",
          "type": "string",
          "const": "queued"
        },
        {
          "description": "Claimed durably before native delivery; never redelivered blindly.",
          "type": "string",
          "const": "active"
        },
        {
          "description": "Native completion was committed.",
          "type": "string",
          "const": "completed"
        },
        {
          "description": "A newer input took the same background slot.",
          "type": "string",
          "const": "superseded"
        },
        {
          "description": "User input canceled pending Auto-continue, or an Interrupt turn\nended this claimed work while its Run stayed alive.",
          "type": "string",
          "const": "canceled"
        },
        {
          "description": "Its own client withdrew queued user work.",
          "type": "string",
          "const": "withdrawn"
        },
        {
          "description": "The Run ended without a successful turn completion.",
          "type": "string",
          "const": "failed"
        },
        {
          "description": "Delivery or execution cannot currently be reconciled.",
          "type": "string",
          "const": "outcome_unknown"
        }
      ]
    },
    "UsageEstimation": {
      "description": "Whether Jet measured a record's numbers or derived them.",
      "oneOf": [
        {
          "description": "Reported by the Harness or the Provider.",
          "type": "string",
          "const": "measured"
        },
        {
          "description": "Derived by Jet, and never a Provider's own accounting.",
          "type": "string",
          "const": "estimated"
        }
      ]
    },
    "UsageFinality": {
      "description": "Whether a record's numbers can still change.",
      "oneOf": [
        {
          "description": "The work it covers had not finished when it was reported.",
          "type": "string",
          "const": "interim"
        },
        {
          "description": "The work it covers is over.",
          "type": "string",
          "const": "final"
        }
      ]
    },
    "UsageFreshness": {
      "description": "Whether a Provider-reported window still stands for what the Provider\nwould say now.",
      "oneOf": [
        {
          "description": "Read recently enough to stand for the Provider's current state.",
          "type": "object",
          "properties": {
            "state": {
              "type": "string",
              "const": "fresh"
            }
          },
          "required": [
            "state"
          ]
        },
        {
          "description": "Older than the interval a Plane refreshes on; it is history rather\nthan a current reading (ADR-0045).",
          "type": "object",
          "properties": {
            "state": {
              "type": "string",
              "const": "stale"
            }
          },
          "required": [
            "state"
          ]
        },
        {
          "description": "The Provider did not answer the last time the Plane asked.",
          "type": "object",
          "properties": {
            "reason": {
              "description": "Why it did not answer.",
              "type": "string"
            },
            "state": {
              "type": "string",
              "const": "unreachable"
            }
          },
          "required": [
            "state",
            "reason"
          ]
        }
      ]
    },
    "UsageSelection": {
      "description": "What a Usage Query covers.",
      "oneOf": [
        {
          "description": "Every Account binding and Conversation on the Plane.",
          "type": "object",
          "properties": {
            "scope": {
              "type": "string",
              "const": "plane"
            }
          },
          "required": [
            "scope"
          ]
        },
        {
          "description": "One Plane-local Account binding.",
          "type": "object",
          "properties": {
            "binding_id": {
              "description": "The binding.",
              "type": "string",
              "format": "uuid"
            },
            "scope": {
              "type": "string",
              "const": "binding"
            }
          },
          "required": [
            "scope",
            "binding_id"
          ]
        },
        {
          "description": "One Conversation. Quota windows belong to an Account binding rather\nthan to a Conversation, so this answers with consumption alone.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "The Conversation.",
              "type": "string",
              "format": "uuid"
            },
            "scope": {
              "type": "string",
              "const": "conversation"
            }
          },
          "required": [
            "scope",
            "conversation_id"
          ]
        },
        {
          "description": "One Run, answered the same way as its Conversation.",
          "type": "object",
          "properties": {
            "run_id": {
              "description": "The Run.",
              "type": "string",
              "format": "uuid"
            },
            "scope": {
              "type": "string",
              "const": "run"
            }
          },
          "required": [
            "scope",
            "run_id"
          ]
        }
      ]
    },
    "UsageTokens": {
      "description": "The token counts one total carries.",
      "type": "object",
      "properties": {
        "cached_input": {
          "description": "Tokens served from the Provider's cache.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "input": {
          "description": "Tokens sent.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "output": {
          "description": "Tokens generated.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "reasoning": {
          "description": "Tokens spent on reasoning, where the Provider counts them apart.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "input",
        "cached_input",
        "output",
        "reasoning"
      ]
    },
    "UtilityJob": {
      "description": "Durable Utility result and its complete routing and policy attribution.",
      "type": "object",
      "properties": {
        "binding_id": {
          "description": "Explicit selected binding, including one that later became unavailable.",
          "type": [
            "string",
            "null"
          ],
          "format": "uuid"
        },
        "job_id": {
          "description": "Plane-assigned request identity.",
          "type": "string",
          "format": "uuid"
        },
        "model": {
          "description": "Exact selected Model; absent when selection was unavailable.",
          "type": [
            "string",
            "null"
          ]
        },
        "outcome": {
          "description": "Validated data or a closed failure.",
          "$ref": "#/$defs/UtilityOutcome"
        },
        "plane_id": {
          "description": "The only Plane authorized to execute this request.",
          "type": "string",
          "format": "uuid"
        },
        "policy": {
          "description": "Policy at admission and the consent it recorded.",
          "$ref": "#/$defs/UtilityPolicy"
        },
        "provider": {
          "description": "Exact selected Provider; absent when no usable binding was configured.",
          "type": [
            "string",
            "null"
          ]
        },
        "purpose": {
          "description": "Purpose, independent of the generated data.",
          "$ref": "#/$defs/UtilityPurpose"
        }
      },
      "required": [
        "job_id",
        "plane_id",
        "purpose",
        "policy",
        "outcome"
      ]
    },
    "UtilityOutcome": {
      "description": "A Utility result carries no execution or deletion authority.",
      "oneOf": [
        {
          "description": "Durable admission, awaiting a single inference attempt.",
          "type": "object",
          "properties": {
            "status": {
              "type": "string",
              "const": "pending"
            }
          },
          "additionalProperties": false,
          "required": [
            "status"
          ]
        },
        {
          "description": "A draft inactivity predicate. It cannot execute until reviewed separately.",
          "type": "object",
          "properties": {
            "inactive_days": {
              "description": "Whole days of inactivity, from 1 through 36500.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "status": {
              "type": "string",
              "const": "draft"
            }
          },
          "additionalProperties": false,
          "required": [
            "status",
            "inactive_days"
          ]
        },
        {
          "description": "Validated text, or a deterministic local fallback. Never executed.",
          "type": "object",
          "properties": {
            "body": {
              "description": "Git body; empty for naming.",
              "type": "string"
            },
            "fallback_reason": {
              "description": "Content-free fallback explanation; absent for inference.",
              "type": [
                "string",
                "null"
              ]
            },
            "status": {
              "type": "string",
              "const": "text"
            },
            "text": {
              "description": "Name or Git subject.",
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "status",
            "text",
            "body"
          ]
        },
        {
          "description": "Compilation did not produce a usable draft.",
          "type": "object",
          "properties": {
            "reason": {
              "description": "Stable, content-free reason.",
              "type": "string"
            },
            "status": {
              "type": "string",
              "const": "refused"
            }
          },
          "additionalProperties": false,
          "required": [
            "status",
            "reason"
          ]
        }
      ]
    },
    "UtilityPolicy": {
      "description": "The exact policy used by a job. Version 1 means smallest suitable Model,\nminimum reasoning, one inference request, no tools, and bounded input/output.",
      "type": "object",
      "properties": {
        "cross_provider_consent": {
          "description": "Persistent consent to send content outside its originating Provider.",
          "type": "boolean"
        },
        "enabled": {
          "description": "Whether the purpose was enabled.",
          "type": "boolean"
        },
        "version": {
          "description": "Version of the allowlists and limits.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "version",
        "enabled",
        "cross_provider_consent"
      ]
    },
    "UtilityPurpose": {
      "description": "Closed set of Utility purposes, used for durable attribution.",
      "oneOf": [
        {
          "description": "Conversation or Run naming.",
          "type": "string",
          "const": "naming"
        },
        {
          "description": "Git commit or pull-request text.",
          "type": "string",
          "const": "git_text"
        },
        {
          "description": "Unapproved natural-language rule compilation.",
          "type": "string",
          "const": "autodelete"
        }
      ]
    },
    "UtilityRequest": {
      "description": "One purpose-specific request for Jet-owned inference.",
      "oneOf": [
        {
          "description": "Suggest a Run name using only its title and opening input.",
          "type": "object",
          "properties": {
            "purpose": {
              "type": "string",
              "const": "naming"
            },
            "run_id": {
              "description": "Owning Run on this Plane.",
              "type": "string",
              "format": "uuid"
            }
          },
          "additionalProperties": false,
          "required": [
            "purpose",
            "run_id"
          ]
        },
        {
          "description": "Draft Git commit or pull-request text from one retained turn.",
          "type": "object",
          "properties": {
            "purpose": {
              "type": "string",
              "const": "git_text"
            },
            "run_id": {
              "description": "Owning Run.",
              "type": "string",
              "format": "uuid"
            },
            "turn": {
              "description": "Completed or interrupted turn, starting at one.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "additionalProperties": false,
          "required": [
            "purpose",
            "run_id",
            "turn"
          ]
        },
        {
          "description": "Compile only this rule prompt into an unapproved draft.",
          "type": "object",
          "properties": {
            "prompt": {
              "description": "Rule prompt, bounded to 4096 UTF-8 bytes.",
              "type": "string"
            },
            "purpose": {
              "type": "string",
              "const": "autodelete"
            }
          },
          "additionalProperties": false,
          "required": [
            "purpose",
            "prompt"
          ]
        }
      ]
    },
    "VersionRange": {
      "description": "Inclusive range of protocol majors a client can speak.",
      "type": "object",
      "properties": {
        "max": {
          "description": "Highest supported major.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "min": {
          "description": "Lowest supported major.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "min",
        "max"
      ]
    },
    "VisaSelection": {
      "description": "The immutable selection used by one admitted Visa Run.",
      "type": "object",
      "properties": {
        "account_binding_id": {
          "description": "Binding in that Plane's own store.",
          "type": "string",
          "format": "uuid"
        },
        "plane_id": {
          "description": "Home Plane on which its native processes execute.",
          "type": "string",
          "format": "uuid"
        }
      },
      "required": [
        "plane_id",
        "account_binding_id"
      ]
    },
    "WireError": {
      "description": "Stable error body carried by every failed reply.",
      "type": "object",
      "properties": {
        "category": {
          "description": "Category the client may branch on.",
          "$ref": "#/$defs/ErrorCategory"
        },
        "code": {
          "description": "Domain-specific code such as `protocol.unsupported_version`.",
          "type": "string"
        },
        "message": {
          "description": "Safe human-readable description free of native error strings.",
          "type": "string"
        },
        "recovery_actions": {
          "description": "Structured actions that can safely recover from this error.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/RecoveryAction"
          }
        },
        "restart": {
          "description": "Structured metadata when a stale read must restart.",
          "anyOf": [
            {
              "$ref": "#/$defs/RestartMetadata"
            },
            {
              "type": "null"
            }
          ]
        },
        "retryable": {
          "description": "Whether repeating the same request may succeed.",
          "type": "boolean"
        },
        "revision_conflict": {
          "description": "Current resource state when an expected Revision was stale.",
          "anyOf": [
            {
              "$ref": "#/$defs/RevisionConflict"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "category",
        "code",
        "retryable",
        "message"
      ]
    },
    "WorkingTree": {
      "description": "Where a Conversation does its work.",
      "oneOf": [
        {
          "description": "In no Project. Nothing on disk belongs to the Conversation.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "no_project"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "In a managed Workspace of a Project, isolated from every other\nConversation. The Workspace itself comes with the Conversation\nsnapshot.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "workspace"
            },
            "project_id": {
              "description": "The Project the Workspace was created from.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "project_id"
          ]
        },
        {
          "description": "In the Project's own Local checkout, without isolation. The Plane\nadmits one live managed Run there at a time and cannot lock the\nprocesses outside its management.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "local_checkout"
            },
            "project_id": {
              "description": "The Project whose checkout it works in.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "project_id"
          ]
        }
      ]
    },
    "WorkingTreeRequest": {
      "description": "What a new Conversation asks for as its working tree. Left out, it asks\nfor no Project, which is what every request before protocol minor 9\nasked for.",
      "oneOf": [
        {
          "description": "No Project yet.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "no_project"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "A managed Workspace of a Project: the default for managed work.",
          "type": "object",
          "properties": {
            "base": {
              "description": "The base to start from; the Project's HEAD when left out.",
              "$ref": "#/$defs/BaseSelection",
              "default": {
                "kind": "head"
              }
            },
            "kind": {
              "type": "string",
              "const": "workspace"
            },
            "project_id": {
              "description": "The Project to create the Workspace from.",
              "type": "string",
              "format": "uuid"
            },
            "seed": {
              "description": "Which Local-checkout changes to start with; none when left out,\nwhich is what every request before protocol minor 10 asked for.",
              "$ref": "#/$defs/SeedSelection"
            }
          },
          "required": [
            "kind",
            "project_id"
          ]
        },
        {
          "description": "The Project's own Local checkout, chosen explicitly.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "local_checkout"
            },
            "project_id": {
              "description": "The Project whose checkout to work in.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "project_id"
          ]
        }
      ]
    },
    "Workspace": {
      "description": "One managed Workspace.",
      "type": "object",
      "properties": {
        "base": {
          "description": "What it started from.",
          "$ref": "#/$defs/WorkspaceBase"
        },
        "conversation_id": {
          "description": "The one Conversation that owns it.",
          "type": "string",
          "format": "uuid"
        },
        "created_at_unix_ms": {
          "description": "When it was created, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "project_id": {
          "description": "The Project it was created from.",
          "type": "string",
          "format": "uuid"
        },
        "promotion": {
          "description": "Its most recent promotion, if it has been promoted: where that\nstands, and the paths it could not settle when it is conflicted.\nAbsent before protocol minor 11, and when it was never promoted.",
          "anyOf": [
            {
              "$ref": "#/$defs/WorkspacePromotion"
            },
            {
              "type": "null"
            }
          ]
        },
        "root": {
          "description": "The absolute root of its worktree, under the Plane's Jet home.",
          "type": "string"
        },
        "seed": {
          "description": "The Local-checkout changes it started with, if any. Absent before\nprotocol minor 10, and when it was seeded with nothing.",
          "anyOf": [
            {
              "$ref": "#/$defs/WorkspaceSeed"
            },
            {
              "type": "null"
            }
          ]
        },
        "workspace_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        }
      },
      "required": [
        "workspace_id",
        "conversation_id",
        "project_id",
        "root",
        "base",
        "created_at_unix_ms"
      ]
    },
    "WorkspaceBase": {
      "description": "The immutable base of a Workspace.",
      "type": "object",
      "properties": {
        "commit": {
          "description": "The commit it resolved to when the Workspace was created, as Git\nspells it.",
          "type": "string"
        },
        "selection": {
          "description": "The base as the user selected it.",
          "$ref": "#/$defs/BaseSelection"
        }
      },
      "required": [
        "selection",
        "commit"
      ]
    },
    "WorkspacePromotion": {
      "description": "One recorded promotion of a Workspace: what the user confirmed and\nwhere it stands.",
      "type": "object",
      "properties": {
        "binding": {
          "description": "Exactly what the preview bound and the user confirmed.",
          "$ref": "#/$defs/PromotionBinding"
        },
        "changed_paths": {
          "description": "How many paths the result changes in the destination.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "promotion_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        },
        "recorded_at_unix_ms": {
          "description": "When it was recorded, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "settled_at_unix_ms": {
          "description": "When it reached a settled state, if it has.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        },
        "state": {
          "description": "Where the promotion stands.",
          "$ref": "#/$defs/PromotionState"
        }
      },
      "required": [
        "promotion_id",
        "binding",
        "changed_paths",
        "state",
        "recorded_at_unix_ms"
      ]
    },
    "WorkspaceSeed": {
      "description": "What a Workspace was seeded with from its Project's Local checkout.",
      "type": "object",
      "properties": {
        "changed_paths": {
          "description": "How many paths that tree changes against the base commit.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "tree": {
          "description": "The Git tree object the changes were captured as, as Git spells it.",
          "type": "string"
        }
      },
      "required": [
        "tree",
        "changed_paths"
      ]
    },
    "WorkspaceTerminal": {
      "description": "One Workspace-owned PTY; no Run identity is involved.",
      "type": "object",
      "properties": {
        "state": {
          "description": "Observed lifecycle.",
          "$ref": "#/$defs/TerminalState"
        },
        "terminal_id": {
          "description": "Stable terminal identity.",
          "type": "string",
          "format": "uuid"
        },
        "workspace_id": {
          "description": "Owning Workspace.",
          "type": "string",
          "format": "uuid"
        }
      },
      "required": [
        "terminal_id",
        "workspace_id",
        "state"
      ]
    },
    "Worktree": {
      "description": "Which working tree of its repository a Project is.",
      "oneOf": [
        {
          "description": "The repository's own working tree.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "main"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "A linked worktree, sharing the repository at `common_dir`.",
          "type": "object",
          "properties": {
            "common_dir": {
              "description": "The `.git` directory (or bare repository) the worktree shares.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "linked"
            }
          },
          "required": [
            "kind",
            "common_dir"
          ]
        }
      ]
    }
  }
}
