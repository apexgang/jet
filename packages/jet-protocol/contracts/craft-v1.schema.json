{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CraftContracts",
  "type": "object",
  "properties": {
    "command": {
      "$ref": "#/$defs/CraftCommand"
    },
    "event": {
      "$ref": "#/$defs/CraftEvent"
    },
    "extension_reply": {
      "$ref": "#/$defs/CraftExtensionReply"
    },
    "extension_request": {
      "$ref": "#/$defs/CraftExtensionRequest"
    },
    "hello": {
      "$ref": "#/$defs/CraftHello"
    },
    "presentation": {
      "$ref": "#/$defs/Presentation"
    },
    "ready": {
      "$ref": "#/$defs/CraftReady"
    },
    "review_model": {
      "$ref": "#/$defs/CraftReviewModel"
    },
    "review_reply": {
      "$ref": "#/$defs/CraftReviewReply"
    },
    "review_request": {
      "$ref": "#/$defs/CraftReviewRequest"
    },
    "specification": {
      "$ref": "#/$defs/CraftSpecification"
    },
    "utility_model": {
      "$ref": "#/$defs/CraftUtilityModel"
    },
    "utility_reply": {
      "$ref": "#/$defs/CraftUtilityReply"
    },
    "utility_request": {
      "$ref": "#/$defs/CraftUtilityRequest"
    }
  },
  "required": [
    "extension_request",
    "extension_reply",
    "utility_model",
    "utility_request",
    "utility_reply",
    "review_model",
    "review_request",
    "review_reply",
    "hello",
    "ready",
    "command",
    "event",
    "specification",
    "presentation"
  ],
  "$defs": {
    "BrokerPermission": {
      "description": "Jet-enforced broker permissions, all required declarations in v1.",
      "oneOf": [
        {
          "description": "Read authorized Artifacts through the broker.",
          "type": "string",
          "const": "artifact_read"
        },
        {
          "description": "Publish Artifacts through the broker.",
          "type": "string",
          "const": "artifact_write"
        },
        {
          "description": "Use authorized paired-Plane tools through the broker.",
          "type": "string",
          "const": "remote_tools"
        }
      ]
    },
    "ConflictState": {
      "description": "Safe resource state attached to a Revision conflict.",
      "oneOf": [
        {
          "description": "The current Conversation.",
          "type": "object",
          "properties": {
            "conversation": {
              "description": "Complete safe Conversation state.",
              "$ref": "#/$defs/Conversation"
            },
            "type": {
              "type": "string",
              "const": "conversation"
            }
          },
          "required": [
            "type",
            "conversation"
          ]
        },
        {
          "description": "The current Run.",
          "type": "object",
          "properties": {
            "run": {
              "description": "Complete safe Run state.",
              "$ref": "#/$defs/Run"
            },
            "type": {
              "type": "string",
              "const": "run"
            }
          },
          "required": [
            "type",
            "run"
          ]
        }
      ]
    },
    "Conversation": {
      "description": "One Conversation.",
      "type": "object",
      "properties": {
        "conversation_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        },
        "created_at_unix_ms": {
          "description": "When it was created, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "name": {
          "description": "Resolved name and its authority. Absent before protocol minor 20.",
          "anyOf": [
            {
              "$ref": "#/$defs/Name"
            },
            {
              "type": "null"
            }
          ]
        },
        "origin": {
          "description": "Where it came from. Absent before protocol minor 13.",
          "anyOf": [
            {
              "$ref": "#/$defs/ConversationOrigin"
            },
            {
              "type": "null"
            }
          ]
        },
        "retention": {
          "description": "Retention choice.",
          "$ref": "#/$defs/RetentionPolicy"
        },
        "revision": {
          "description": "Current version for conflict-sensitive Commands. Absent before minor 20.",
          "type": [
            "string",
            "null"
          ],
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "working_tree": {
          "description": "Where it does its work. Absent before protocol minor 9.",
          "anyOf": [
            {
              "$ref": "#/$defs/WorkingTree"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "conversation_id",
        "retention",
        "created_at_unix_ms"
      ]
    },
    "ConversationOrigin": {
      "description": "Where a Conversation came from.",
      "oneOf": [
        {
          "description": "Created in Jet.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "new"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "Created by a managed Resume to continue an Imported conversation.",
          "type": "object",
          "properties": {
            "import_id": {
              "description": "The import it continues.",
              "type": "string",
              "format": "uuid"
            },
            "kind": {
              "type": "string",
              "const": "imported"
            }
          },
          "required": [
            "kind",
            "import_id"
          ]
        },
        {
          "description": "Created from one immutable Change checkpoint.",
          "type": "object",
          "properties": {
            "checkpoint_turn": {
              "description": "One-based turn boundary selected from that Run.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "forked"
            },
            "source_conversation_id": {
              "description": "Conversation that owns the selected Run.",
              "type": "string",
              "format": "uuid"
            },
            "source_run_id": {
              "description": "Run that owns the selected checkpoint.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "source_conversation_id",
            "source_run_id",
            "checkpoint_turn"
          ]
        }
      ]
    },
    "CraftAction": {
      "description": "Structured action input; unknown security-sensitive variants fail closed.",
      "oneOf": [
        {
          "description": "Invoke an action offered by the Harness in this execution.",
          "type": "object",
          "properties": {
            "action_id": {
              "description": "Native action identity.",
              "type": "string"
            },
            "input": {
              "description": "Harness-native input, interpreted only by the Craft."
            },
            "kind": {
              "type": "string",
              "const": "invoke"
            }
          },
          "required": [
            "kind",
            "action_id",
            "input"
          ]
        },
        {
          "description": "Answer an approval after Jet has authorized this exact decision.",
          "type": "object",
          "properties": {
            "decision": {
              "description": "No implicit blanket or persistent approval exists.",
              "$ref": "#/$defs/CraftApprovalDecision"
            },
            "kind": {
              "type": "string",
              "const": "approval"
            },
            "request_id": {
              "description": "Native approval request identity.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "request_id",
            "decision"
          ]
        }
      ]
    },
    "CraftApprovalDecision": {
      "description": "Explicit decision on exactly one native approval request.",
      "oneOf": [
        {
          "description": "Allow only the identified request under existing policy.",
          "type": "string",
          "const": "allow_once"
        },
        {
          "description": "Deny the identified request.",
          "type": "string",
          "const": "deny"
        }
      ]
    },
    "CraftApprovalRequest": {
      "description": "One native approval request the Craft holds until Jet answers it. It\ndescribes exactly what the Harness asked to do, so a reviewer and a\nperson are shown the same action (Craft 1.7).",
      "type": "object",
      "properties": {
        "action": {
          "description": "The exact requested action as the Harness would carry it out, at\nmost 4096 UTF-8 bytes. It is content, never an instruction.",
          "type": "string"
        },
        "request_id": {
          "description": "Native approval identity, echoed back in exactly one decision.",
          "type": "string"
        },
        "tool": {
          "description": "Native tool or permission name, at most 128 UTF-8 bytes.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "request_id",
        "tool",
        "action"
      ]
    },
    "CraftCommand": {
      "description": "Host-to-Craft Commands, admitted by Jet before crossing this boundary.",
      "oneOf": [
        {
          "description": "Pause new native children under energy pressure, or restore native policy\n(Craft 1.9, subagents_limit). Never terminate an existing child.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "constrain_subagents"
            },
            "max_children": {
              "description": "Zero blocks new child work; null restores the native policy.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "Pinned remote destinations and honest capability report (Craft 1.6).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "configure_remote_tools"
            },
            "selection": {
              "description": "Origin and authorized destinations.",
              "$ref": "#/$defs/NoVisaSelection"
            }
          },
          "required": [
            "kind",
            "selection"
          ]
        },
        {
          "description": "Reply to one remote call, without disrupting other destination work (1.6).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "remote_tool_result"
            },
            "operation_id": {
              "description": "Original operation identity.",
              "type": "string",
              "format": "uuid"
            },
            "outcome": {
              "description": "Destination result or stable refusal.",
              "$ref": "#/$defs/RemoteToolOutcome"
            }
          },
          "required": [
            "kind",
            "operation_id",
            "outcome"
          ]
        },
        {
          "description": "Reconnect an existing helper without issuing native input (Craft 1.2).",
          "type": "object",
          "properties": {
            "checkpoint": {
              "description": "Opaque parser state committed with that boundary.",
              "type": "string"
            },
            "helper_socket": {
              "description": "Validated owner-only helper endpoint.",
              "type": "string"
            },
            "id": {
              "description": "Original Run identity.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "recover"
            },
            "source_offset": {
              "description": "Last atomically committed source boundary.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "id",
            "helper_socket",
            "source_offset",
            "checkpoint"
          ]
        },
        {
          "description": "Start a Run through its host-provisioned helper (Craft 1.1, runs).",
          "type": "object",
          "properties": {
            "helper_socket": {
              "description": "Owner-only endpoint of this Run's helper.",
              "type": "string"
            },
            "id": {
              "description": "Initial Command identity.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "start"
            },
            "text": {
              "description": "Initial input for the Harness.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "id",
            "text",
            "helper_socket"
          ]
        },
        {
          "description": "Confirm that preceding semantic output is durable (Craft 1.1, runs).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "acknowledge"
            },
            "source_offset": {
              "description": "Source record the Craft may now release through the helper.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "source_offset"
          ]
        },
        {
          "description": "Submit one admitted turn.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Correlation identity; delivery alone is not a durable receipt.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "turn"
            },
            "text": {
              "description": "Harness input.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "id",
            "text"
          ]
        },
        {
          "description": "Ask the Harness to cancel the identified turn without ending the\nRun (Craft 1.4). A Craft that negotiated 1.4 must answer every\nInterrupt with a `TurnEnded` boundary for that turn.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Correlation identity of the turn to cancel.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "interrupt"
            }
          },
          "required": [
            "kind",
            "id"
          ]
        },
        {
          "description": "Route an authenticated user action to its native handler.",
          "type": "object",
          "properties": {
            "action": {
              "description": "Structured native action.",
              "$ref": "#/$defs/CraftAction"
            },
            "id": {
              "description": "Correlation identity.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "action"
            }
          },
          "required": [
            "kind",
            "id",
            "action"
          ]
        },
        {
          "description": "Release this execution connection; never deletes a Conversation or\nterminates the Craft process while other execution connections exist.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "shutdown"
            }
          },
          "required": [
            "kind"
          ]
        }
      ]
    },
    "CraftEvent": {
      "description": "Craft-to-host events. Native content remains the authoritative payload.",
      "oneOf": [
        {
          "description": "Explicit Model selected by the native session (1.10), never inferred from usage.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "model"
            },
            "model": {
              "description": "Exact resolved Model identity, at most 128 bytes.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "model"
          ]
        },
        {
          "description": "Request a Jet remote tool; the host supplies Actor and permissions (1.6).",
          "type": "object",
          "properties": {
            "call": {
              "description": "Closed, destination-scoped request.",
              "$ref": "#/$defs/CraftRemoteTool"
            },
            "kind": {
              "type": "string",
              "const": "remote_tool"
            }
          },
          "required": [
            "kind",
            "call"
          ]
        },
        {
          "description": "Normalized Usage the Harness reported (1.7). It records what was\nconsumed or how full a Provider window is; it changes no Run state\nand grants nothing.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "usage"
            },
            "usage": {
              "description": "The report, in Jet's vocabulary rather than the Harness's.",
              "$ref": "#/$defs/CraftUsage"
            }
          },
          "required": [
            "kind",
            "usage"
          ]
        },
        {
          "description": "The Harness asked for something that needs a decision, and is waiting\nfor exactly one (Craft 1.8). Emitting this grants no permission and\nwidens no boundary; the Craft holds the native request until Jet\nanswers with [`CraftAction::Approval`].",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "approval_requested"
            },
            "request": {
              "description": "The held request and the exact action it asks for.",
              "$ref": "#/$defs/CraftApprovalRequest"
            }
          },
          "required": [
            "kind",
            "request"
          ]
        },
        {
          "description": "Capture before a subsequent turn (1.3). Hold native input until the\nhost acknowledges this marker's source record. Initial Start is pre-captured.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "turn_started"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "Capture completed or interrupted work without ending the Run (1.3).\nDo not begin another turn until this boundary is acknowledged.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "turn_ended"
            },
            "outcome": {
              "description": "Turn outcome.",
              "$ref": "#/$defs/TurnOutcome"
            }
          },
          "required": [
            "kind",
            "outcome"
          ]
        },
        {
          "description": "Correlated native file operation and exact content evidence (1.3).",
          "type": "object",
          "properties": {
            "change": {
              "description": "Origin is assigned by the host, never the Craft.",
              "$ref": "#/$defs/CraftFileChange"
            },
            "kind": {
              "type": "string",
              "const": "file_changed"
            }
          },
          "required": [
            "kind",
            "change"
          ]
        },
        {
          "description": "Structured native title candidate for the owning Conversation (1.5).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "conversation_title"
            },
            "title": {
              "description": "Unescaped title text.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "title"
          ]
        },
        {
          "description": "Structured native title candidate for the current Run (1.5).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "run_title"
            },
            "title": {
              "description": "Unescaped title text.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "title"
          ]
        },
        {
          "description": "Terminal/native title for exactly one Managed process (1.5).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "process_title"
            },
            "pid": {
              "description": "OS process identity reported by `run_started`.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "title": {
              "description": "Unescaped live label.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "pid",
            "title"
          ]
        },
        {
          "description": "The helper definitively failed to launch a native Harness.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "run_launch_failed"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "Native process identities supplied by the Run-role helper.",
          "type": "object",
          "properties": {
            "harness_pid": {
              "description": "Harness OS process identity.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "helper_pid": {
              "description": "Helper OS process identity.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "run_started"
            }
          },
          "required": [
            "kind",
            "helper_pid",
            "harness_pid"
          ]
        },
        {
          "description": "Current reason an active Run is working or waiting.",
          "type": "object",
          "properties": {
            "activity": {
              "description": "Orthogonal to the Run lifecycle.",
              "$ref": "#/$defs/RunActivity"
            },
            "kind": {
              "type": "string",
              "const": "activity"
            }
          },
          "required": [
            "kind",
            "activity"
          ]
        },
        {
          "description": "End of a native source record; earlier Events and parser state commit atomically.\nThe host commits bounded groups with replay-prefix digests while keeping\nthis record retained, then acknowledges after its final checkpoint.",
          "type": "object",
          "properties": {
            "checkpoint": {
              "description": "Bounded parser state required to interpret the next source record.",
              "type": "string",
              "default": ""
            },
            "kind": {
              "type": "string",
              "const": "progress"
            },
            "source_offset": {
              "description": "End offset acknowledged through the Craft only after durable commit.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "source_offset"
          ]
        },
        {
          "description": "Confirmed attachment to the retained helper at the requested source boundary (1.2).",
          "type": "object",
          "properties": {
            "helper_pid": {
              "description": "OS identity of the validated helper.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "kind": {
              "type": "string",
              "const": "run_recovered"
            },
            "source_offset": {
              "description": "Requested durable source boundary.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "helper_pid",
            "source_offset"
          ]
        },
        {
          "description": "The Harness ended, independently from a single turn's completion.",
          "type": "object",
          "properties": {
            "exit_code": {
              "description": "Native exit code, absent for a signal termination.",
              "type": [
                "integer",
                "null"
              ],
              "format": "int32"
            },
            "kind": {
              "type": "string",
              "const": "run_ended"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "Complete native event accompanied by optional portable views.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "output"
            },
            "native_event": {
              "description": "Original JSON bytes, including unknown data and numeric precision."
            },
            "presentation": {
              "description": "Presentation accompanies, never replaces, the native event.",
              "type": "array",
              "default": [],
              "items": {
                "$ref": "#/$defs/PresentationBlock"
              }
            }
          },
          "required": [
            "kind",
            "native_event"
          ]
        },
        {
          "description": "Native Command completion; the host persists it before acknowledging.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Command correlation identity.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "completed"
            },
            "native_conversation": {
              "description": "Native identity retained for an explicitly resumed later execution.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "id",
            "native_conversation"
          ]
        }
      ]
    },
    "CraftExtensionReply": {
      "description": "Bounded reply from the responsible Craft. Native errors never cross this boundary.",
      "oneOf": [
        {
          "description": "Native metadata, never executable GUI customization.",
          "type": "object",
          "properties": {
            "catalog": {
              "description": "Unconverted native inventory.",
              "$ref": "#/$defs/ExtensionCatalog"
            },
            "kind": {
              "type": "string",
              "const": "catalog"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind",
            "catalog"
          ]
        },
        {
          "description": "The native operation completed.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "applied"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind"
          ]
        },
        {
          "description": "No native mutation was attempted.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "refused"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind"
          ]
        }
      ]
    },
    "CraftExtensionRequest": {
      "description": "One-shot Craft extension v1 request on owner-only stdin/stdout.",
      "oneOf": [
        {
          "description": "Discover a native catalog.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "catalog"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind"
          ]
        },
        {
          "description": "Inspect one native target before accepting executable access.",
          "type": "object",
          "properties": {
            "extension_id": {
              "description": "Exact native target.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "inspect"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind",
            "extension_id"
          ]
        },
        {
          "description": "Perform one admitted lifecycle operation after Runs have finished.",
          "type": "object",
          "properties": {
            "confirmation": {
              "description": "Exact native metadata and accepted access.",
              "$ref": "#/$defs/ExtensionConfirmation"
            },
            "kind": {
              "type": "string",
              "const": "apply"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind",
            "confirmation"
          ]
        }
      ]
    },
    "CraftFeature": {
      "description": "A supported Harness feature. Unknown optional features are disabled.",
      "type": "object",
      "properties": {
        "name": {
          "description": "Feature name; v1 understands turns, actions, resume, fork,\nremote_tools, utility, and review.",
          "type": "string"
        },
        "required": {
          "description": "Whether installation must reject an unrecognized feature.",
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "name"
      ]
    },
    "CraftFileChange": {
      "description": "A native file-operation receipt. Timing alone is never a receipt.",
      "type": "object",
      "properties": {
        "activity_id": {
          "description": "Native operation identity retained with the source record.",
          "type": "string"
        },
        "after_mode": {
          "description": "Resulting Git mode.",
          "type": "string"
        },
        "after_object": {
          "description": "Resulting Git blob or gitlink object, all zeros for deletions.",
          "type": "string"
        },
        "before_mode": {
          "description": "Original Git mode.",
          "type": "string"
        },
        "before_object": {
          "description": "Original Git blob or gitlink object, all zeros for additions.",
          "type": "string"
        },
        "path": {
          "description": "Repository-relative path.",
          "type": "string"
        }
      },
      "required": [
        "activity_id",
        "path",
        "before_object",
        "after_object",
        "before_mode",
        "after_mode"
      ]
    },
    "CraftFork": {
      "description": "Immutable source context for a new native Harness Conversation. Receiving\nthis handshake is not authority to perform the fork before `Start`.",
      "type": "object",
      "properties": {
        "checkpoint_commit": {
          "description": "Checked-out source commit retained by the checkpoint.",
          "type": "string"
        },
        "checkpoint_tree": {
          "description": "Exact working-tree object retained by the checkpoint.",
          "type": "string"
        },
        "checkpoint_turn": {
          "description": "One-based checkpoint boundary selected by the user.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "source_conversation_id": {
          "description": "Jet Conversation that owns the selected Run.",
          "type": "string",
          "format": "uuid"
        },
        "source_native_conversation": {
          "description": "Native Harness Conversation that owns the selected source content.",
          "type": "string"
        },
        "source_run_id": {
          "description": "Jet Run that owns the selected checkpoint.",
          "type": "string",
          "format": "uuid"
        }
      },
      "required": [
        "source_native_conversation",
        "source_conversation_id",
        "source_run_id",
        "checkpoint_turn",
        "checkpoint_commit",
        "checkpoint_tree"
      ]
    },
    "CraftHello": {
      "description": "First host control payload after the `jet-craft\\n` preface.",
      "type": "object",
      "properties": {
        "execution_id": {
          "description": "Stable identity persisted by the host before dispatching external work.",
          "type": "string",
          "format": "uuid"
        },
        "fork": {
          "description": "Absent unless this new execution should fork an existing native\nConversation from the selected immutable checkpoint.",
          "anyOf": [
            {
              "$ref": "#/$defs/CraftFork"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "protocol": {
          "description": "Host Craft-protocol support, independent of its GUI/helper protocols.",
          "$ref": "#/$defs/ProtocolOffer"
        },
        "resume": {
          "description": "Absent for a new execution; present only for explicit recovery.",
          "anyOf": [
            {
              "$ref": "#/$defs/CraftResume"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "specification": {
          "description": "Host specification-schema support, negotiated independently.",
          "$ref": "#/$defs/ProtocolOffer"
        }
      },
      "required": [
        "protocol",
        "specification",
        "execution_id"
      ]
    },
    "CraftHostAccess": {
      "description": "Disclosed same-user host access; not a portable sandbox guarantee.",
      "oneOf": [
        {
          "description": "An executable the Craft expects to launch.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "executable"
            },
            "name": {
              "description": "Executable name or absolute path.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ]
        },
        {
          "description": "A filesystem area the Craft expects to use.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "filesystem"
            },
            "path": {
              "description": "Disclosed filesystem path.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "path"
          ]
        },
        {
          "description": "An environment input, never its secret value.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "environment"
            },
            "name": {
              "description": "Environment variable name.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ]
        },
        {
          "description": "An expected network destination.",
          "type": "object",
          "properties": {
            "destination": {
              "description": "Destination description shown during installation.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "network"
            }
          },
          "required": [
            "kind",
            "destination"
          ]
        }
      ]
    },
    "CraftObservedUsage": {
      "description": "What the Harness reported it consumed.",
      "type": "object",
      "properties": {
        "estimation": {
          "description": "Whether the Harness stated the counts or the Craft derived them.",
          "$ref": "#/$defs/CraftUsageEstimation"
        },
        "finality": {
          "description": "Whether the counts can still change.",
          "$ref": "#/$defs/CraftUsageFinality"
        },
        "measurement": {
          "description": "What it covers and what identifies it.",
          "$ref": "#/$defs/CraftUsageMeasurement"
        },
        "model": {
          "description": "The Model that did the work, when the Harness names one.",
          "type": [
            "string",
            "null"
          ]
        },
        "tokens": {
          "description": "The counts themselves.",
          "$ref": "#/$defs/CraftUsageTokens"
        }
      },
      "required": [
        "measurement",
        "estimation",
        "finality",
        "tokens"
      ]
    },
    "CraftQuotaScope": {
      "description": "What one Provider-reported quota window covers.",
      "oneOf": [
        {
          "description": "The Provider account as a whole.",
          "type": "object",
          "properties": {
            "covers": {
              "type": "string",
              "const": "provider_account"
            }
          },
          "required": [
            "covers"
          ]
        },
        {
          "description": "One Model of that account.",
          "type": "object",
          "properties": {
            "covers": {
              "type": "string",
              "const": "model"
            },
            "model": {
              "description": "The Model the window limits.",
              "type": "string"
            }
          },
          "required": [
            "covers",
            "model"
          ]
        }
      ]
    },
    "CraftQuotaUnit": {
      "description": "The unit a Provider stated one window in.",
      "oneOf": [
        {
          "description": "Inference tokens.",
          "type": "string",
          "const": "tokens"
        },
        {
          "description": "Requests.",
          "type": "string",
          "const": "requests"
        },
        {
          "description": "Provider-defined credits.",
          "type": "string",
          "const": "credits"
        },
        {
          "description": "Hundredths of a percent of the window, out of 10,000, for a\nProvider that reports a filled fraction rather than a limit.",
          "type": "string",
          "const": "share"
        }
      ]
    },
    "CraftQuotaWindow": {
      "description": "One quota window exactly as the Provider reported it.",
      "type": "object",
      "properties": {
        "estimation": {
          "description": "Whether the Provider stated the numbers or the Craft derived them.",
          "$ref": "#/$defs/CraftUsageEstimation"
        },
        "finality": {
          "description": "Whether the window has closed.",
          "$ref": "#/$defs/CraftUsageFinality"
        },
        "limit": {
          "description": "The limit the Provider stated, where it stated one.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "resets_in_seconds": {
          "description": "How long until it refills, where the Provider stated it. A Craft\nreports the remaining time rather than an instant, so the host\nconverts it with the Plane's own clock.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "scope": {
          "description": "What the window covers.",
          "$ref": "#/$defs/CraftQuotaScope"
        },
        "unit": {
          "description": "The unit the Provider stated it in.",
          "$ref": "#/$defs/CraftQuotaUnit"
        },
        "used": {
          "description": "How much of the window the Provider reported as consumed.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "window": {
          "description": "The Provider's own name for the window, such as its five-hour or\nweekly limit. The host keeps windows apart and never sums them.",
          "type": "string"
        },
        "window_seconds": {
          "description": "How long the window lasts, where the Provider stated it.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "window",
        "scope",
        "unit",
        "used",
        "estimation",
        "finality"
      ]
    },
    "CraftReady": {
      "description": "Successful negotiation; the host persists the version with the execution.",
      "type": "object",
      "properties": {
        "enabled_features": {
          "description": "Understood features; unknown optional features remain disabled.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "protocol": {
          "description": "Selected Craft protocol and capabilities.",
          "$ref": "#/$defs/NegotiatedProtocol"
        },
        "specification": {
          "description": "Declarations must match the host's accepted installed specification.",
          "$ref": "#/$defs/CraftSpecification"
        },
        "specification_protocol": {
          "description": "Independently selected specification schema minor.",
          "$ref": "#/$defs/NegotiatedProtocol"
        }
      },
      "required": [
        "protocol",
        "specification_protocol",
        "specification",
        "enabled_features"
      ]
    },
    "CraftRemoteTool": {
      "description": "A Craft can select only a pinned destination and action, never authority.",
      "type": "object",
      "properties": {
        "action": {
          "description": "Exact requested operation.",
          "$ref": "#/$defs/RemoteToolAction"
        },
        "destination_plane_id": {
          "description": "Selected destination.",
          "type": "string",
          "format": "uuid"
        },
        "operation_id": {
          "description": "Stable identity retained across Craft source replay.",
          "type": "string",
          "format": "uuid"
        },
        "workspace_id": {
          "description": "Selected registered Workspace.",
          "type": "string",
          "format": "uuid"
        }
      },
      "additionalProperties": false,
      "required": [
        "operation_id",
        "destination_plane_id",
        "workspace_id",
        "action"
      ]
    },
    "CraftResume": {
      "description": "Host-owned recovery context; never evidence that an unacknowledged action failed.",
      "type": "object",
      "properties": {
        "model": {
          "description": "Resolved Model that a Craft 1.10 resume must enforce.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_conversation": {
          "description": "Native Conversation to reopen without replaying Commands automatically.",
          "type": "string"
        },
        "version": {
          "description": "Version pinned when this execution first negotiated.",
          "$ref": "#/$defs/ProtocolVersion"
        }
      },
      "required": [
        "version",
        "native_conversation"
      ]
    },
    "CraftReviewInput": {
      "description": "Everything one reviewer may see. It is Conversation content and the\nexact requested action, with no paths, credentials, or tool definitions.",
      "type": "object",
      "properties": {
        "action": {
          "description": "The exact requested action, at most 4096 UTF-8 bytes.",
          "type": "string"
        },
        "tool": {
          "description": "Native tool or permission name, at most 128 UTF-8 bytes.",
          "type": "string"
        },
        "transcript": {
          "description": "Bounded visible transcript, oldest first, at most 12288 UTF-8 bytes.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "transcript",
        "tool",
        "action"
      ]
    },
    "CraftReviewModel": {
      "description": "Content-free selection returned by `--review-model`.",
      "type": "object",
      "properties": {
        "model": {
          "description": "Exact reviewer Model, never an alias resolved after selection.",
          "type": "string"
        },
        "reviewer": {
          "description": "Which reviewer the Craft will use for this Model.",
          "$ref": "#/$defs/CraftReviewer"
        },
        "version": {
          "description": "Contract version, exactly one.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "version",
        "model",
        "reviewer"
      ]
    },
    "CraftReviewReply": {
      "description": "One response, bounded to 64 KiB including JSON escaping.",
      "type": "object",
      "properties": {
        "model": {
          "description": "Actual Model reported by the Provider.",
          "type": "string"
        },
        "output": {
          "description": "Untrusted JSON, at most 4096 UTF-8 bytes, validated again by Core.",
          "type": "string"
        },
        "reviewer": {
          "description": "Which reviewer produced the judgement.",
          "$ref": "#/$defs/CraftReviewer"
        },
        "version": {
          "description": "Contract version, exactly one.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "version",
        "model",
        "reviewer",
        "output"
      ]
    },
    "CraftReviewRequest": {
      "description": "One request to `--review`; bounded to 128 KiB including JSON escaping.",
      "type": "object",
      "properties": {
        "binding_id": {
          "description": "The one Account binding whose Credential may be resolved.",
          "type": "string",
          "format": "uuid"
        },
        "credential_reference": {
          "description": "Transport authentication reference; never model input.",
          "$ref": "#/$defs/CredentialReference"
        },
        "input": {
          "description": "The entire reviewer input.",
          "$ref": "#/$defs/CraftReviewInput"
        },
        "model": {
          "description": "Exact selection, never replaced by a default.",
          "type": "string"
        },
        "version": {
          "description": "Contract version, exactly one.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "version",
        "model",
        "binding_id",
        "credential_reference",
        "input"
      ]
    },
    "CraftReviewer": {
      "description": "Which reviewer a Craft used. A Harness with its own separate reviewer\nthat satisfies this contract reports `Native`; a Craft that instead runs\nJet's versioned equivalent through the selected Account binding reports\n`Equivalent`.",
      "oneOf": [
        {
          "description": "The Harness's own separate reviewer.",
          "type": "string",
          "const": "native"
        },
        {
          "description": "Jet's versioned equivalent, run through the selected binding.",
          "type": "string",
          "const": "equivalent"
        }
      ]
    },
    "CraftSpecification": {
      "description": "Contents of `.jet/craft-spec.toml`, also sent as JSON during handshake.",
      "type": "object",
      "properties": {
        "broker_permissions": {
          "description": "Required broker declarations; the host separately authorizes each use.",
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/BrokerPermission"
          }
        },
        "features": {
          "description": "Harness feature declarations, never authorization grants.",
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/CraftFeature"
          }
        },
        "harness": {
          "description": "The single Harness adapted by this Craft.",
          "type": "string"
        },
        "host_access": {
          "description": "Required host-access disclosures.",
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/CraftHostAccess"
          }
        },
        "id": {
          "description": "Stable Craft identity.",
          "type": "string"
        },
        "protocol": {
          "description": "Craft protocol majors and each major's highest minor.",
          "$ref": "#/$defs/ProtocolOffer"
        },
        "schema": {
          "description": "This document's schema version, independent of Craft wire versions.",
          "$ref": "#/$defs/ProtocolVersion"
        }
      },
      "required": [
        "schema",
        "id",
        "harness",
        "protocol"
      ]
    },
    "CraftUsage": {
      "description": "One normalized Usage report (Craft 1.7). Reporting it grants nothing\nand changes no Run state; it is what the Harness said about what it\nused.",
      "oneOf": [
        {
          "description": "Consumption the Harness reported.",
          "type": "object",
          "properties": {
            "observed": {
              "description": "What it consumed.",
              "$ref": "#/$defs/CraftObservedUsage"
            },
            "source": {
              "type": "string",
              "const": "observed"
            }
          },
          "required": [
            "source",
            "observed"
          ]
        },
        {
          "description": "A quota window the Provider reported.",
          "type": "object",
          "properties": {
            "quota": {
              "description": "The window as reported.",
              "$ref": "#/$defs/CraftQuotaWindow"
            },
            "source": {
              "type": "string",
              "const": "quota"
            }
          },
          "required": [
            "source",
            "quota"
          ]
        },
        {
          "description": "A Provider that would not report its windows, so the host says so\nrather than presenting the windows it last saw as current.",
          "type": "object",
          "properties": {
            "reason": {
              "description": "Bounded, non-secret text naming why, at most 256 characters.",
              "type": "string"
            },
            "source": {
              "type": "string",
              "const": "unreachable"
            }
          },
          "required": [
            "source",
            "reason"
          ]
        }
      ]
    },
    "CraftUsageEstimation": {
      "description": "Whether the Harness or Provider stated the numbers, or the Craft\nderived them.",
      "oneOf": [
        {
          "description": "Stated by the Harness or the Provider.",
          "type": "string",
          "const": "measured"
        },
        {
          "description": "Derived by the Craft, and never presented as a Provider's own\naccounting.",
          "type": "string",
          "const": "estimated"
        }
      ]
    },
    "CraftUsageFinality": {
      "description": "Whether the numbers can still change.",
      "oneOf": [
        {
          "description": "The work the numbers cover had not finished.",
          "type": "string",
          "const": "interim"
        },
        {
          "description": "The work is over and the numbers no longer move.",
          "type": "string",
          "const": "final"
        }
      ]
    },
    "CraftUsageMeasurement": {
      "description": "What one consumption measurement covers, and what makes it one\nmeasurement rather than another. The host replaces a repeated\nmeasurement instead of adding it to the total (ADR-0023).",
      "oneOf": [
        {
          "description": "One turn, named by the identity the host delivered with it.",
          "type": "object",
          "properties": {
            "covers": {
              "type": "string",
              "const": "turn"
            },
            "native_usage_id": {
              "description": "The Harness's or Provider's own identity for the measurement.",
              "type": [
                "string",
                "null"
              ]
            },
            "turn": {
              "description": "Correlation identity of the turn the numbers cover.",
              "type": "string"
            }
          },
          "required": [
            "covers",
            "turn"
          ]
        },
        {
          "description": "The Run so far, as a cumulative total the Harness restates. A Run\nreporting these does not also have its turns counted.",
          "type": "object",
          "properties": {
            "covers": {
              "type": "string",
              "const": "run"
            },
            "native_usage_id": {
              "description": "The Harness's or Provider's own identity for the measurement.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "covers"
          ]
        }
      ]
    },
    "CraftUsageTokens": {
      "description": "The token counts one measurement carries. Tokens a Provider wrote to\nits cache count as input; an absent count is zero.",
      "type": "object",
      "properties": {
        "cached_input": {
          "description": "Tokens served from the Provider's cache.",
          "type": "integer",
          "format": "uint64",
          "default": 0,
          "minimum": 0
        },
        "input": {
          "description": "Tokens sent.",
          "type": "integer",
          "format": "uint64",
          "default": 0,
          "minimum": 0
        },
        "output": {
          "description": "Tokens generated.",
          "type": "integer",
          "format": "uint64",
          "default": 0,
          "minimum": 0
        },
        "reasoning": {
          "description": "Tokens spent on reasoning, where the Provider counts them apart.",
          "type": "integer",
          "format": "uint64",
          "default": 0,
          "minimum": 0
        }
      }
    },
    "CraftUtilityModel": {
      "description": "Content-free selection returned by `--utility-model`.",
      "type": "object",
      "properties": {
        "model": {
          "description": "Exact Model at minimum reasoning effort.",
          "type": "string"
        },
        "version": {
          "description": "Contract version, exactly one.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "version",
        "model"
      ]
    },
    "CraftUtilityReply": {
      "description": "One response, bounded to 64 KiB including JSON escaping.",
      "type": "object",
      "properties": {
        "model": {
          "description": "Actual Model reported by the Provider.",
          "type": "string"
        },
        "output": {
          "description": "Untrusted JSON, at most 8192 UTF-8 bytes, validated again by Core.",
          "type": "string"
        },
        "version": {
          "description": "Contract version, exactly one.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "version",
        "model",
        "output"
      ]
    },
    "CraftUtilityRequest": {
      "description": "One request to `--utility`; bounded to 128 KiB including JSON escaping.",
      "type": "object",
      "properties": {
        "binding_id": {
          "description": "The one Account binding whose Credential may be resolved.",
          "type": "string",
          "format": "uuid"
        },
        "credential_reference": {
          "description": "Transport authentication reference; never model input.",
          "$ref": "#/$defs/CredentialReference"
        },
        "input": {
          "description": "The entire model input, with no tools, paths, or native output.",
          "$ref": "#/$defs/UtilityInput"
        },
        "model": {
          "description": "Exact selection, never replaced by a default.",
          "type": "string"
        },
        "version": {
          "description": "Contract version, exactly one.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "version",
        "model",
        "binding_id",
        "credential_reference",
        "input"
      ]
    },
    "CredentialItem": {
      "description": "One platform credential-store item. The Plane names it after the binding\nalone, so no client-supplied text enters Plane-owned state.",
      "type": "object",
      "properties": {
        "account": {
          "description": "The item within that service.",
          "type": "string"
        },
        "service": {
          "description": "The service every Jet Credential item lives under.",
          "type": "string"
        }
      },
      "required": [
        "service",
        "account"
      ]
    },
    "CredentialReference": {
      "description": "The opaque reference a Plane keeps for one binding's Credential, and the\nplace the secret it resolves to belongs.",
      "oneOf": [
        {
          "description": "An item of the platform credential store.",
          "type": "object",
          "properties": {
            "item": {
              "description": "The item the backend resolves.",
              "$ref": "#/$defs/CredentialItem"
            },
            "source": {
              "type": "string",
              "const": "platform_store"
            }
          },
          "required": [
            "source",
            "item"
          ]
        },
        {
          "description": "An external authentication helper.",
          "type": "object",
          "properties": {
            "helper": {
              "description": "The helper's non-secret name.",
              "type": "string"
            },
            "source": {
              "type": "string",
              "const": "external_helper"
            }
          },
          "required": [
            "source",
            "helper"
          ]
        },
        {
          "description": "Native Harness authentication; the Plane references nothing.",
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "const": "harness_native"
            }
          },
          "required": [
            "source"
          ]
        },
        {
          "description": "Memory of the daemon start that established it.",
          "type": "object",
          "properties": {
            "established_at_daemon_start": {
              "description": "The daemon start whose memory holds the Credential. A later\nstart holds nothing, so the binding must be established again.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "source": {
              "type": "string",
              "const": "session_only"
            }
          },
          "required": [
            "source",
            "established_at_daemon_start"
          ]
        }
      ]
    },
    "ErrorCategory": {
      "description": "Stable error categories exposed to clients (ADR-0068).",
      "oneOf": [
        {
          "description": "The request was malformed or violated a precondition.",
          "type": "string",
          "const": "invalid_input"
        },
        {
          "description": "The Actor may not perform the request.",
          "type": "string",
          "const": "unauthorized"
        },
        {
          "description": "The request conflicts with current state.",
          "type": "string",
          "const": "conflict"
        },
        {
          "description": "A required resource is temporarily unavailable.",
          "type": "string",
          "const": "unavailable"
        },
        {
          "description": "The peers cannot agree on a protocol, codec, or version.",
          "type": "string",
          "const": "incompatible"
        },
        {
          "description": "The request was throttled.",
          "type": "string",
          "const": "rate_limited"
        },
        {
          "description": "The addressed resource does not exist.",
          "type": "string",
          "const": "not_found"
        },
        {
          "description": "The result of external work could not be established.",
          "type": "string",
          "const": "outcome_unknown"
        },
        {
          "description": "An unexpected internal failure.",
          "type": "string",
          "const": "internal"
        }
      ]
    },
    "ExtensionAction": {
      "description": "Operations supported by the responsible native adapter.",
      "oneOf": [
        {
          "description": "Install a native extension.",
          "type": "string",
          "const": "install"
        },
        {
          "description": "Update a native extension.",
          "type": "string",
          "const": "update"
        },
        {
          "description": "Stop loading a native extension.",
          "type": "string",
          "const": "disable"
        },
        {
          "description": "Remove a native extension.",
          "type": "string",
          "const": "remove"
        }
      ]
    },
    "ExtensionCatalog": {
      "description": "Native catalog data, interpreted by its Craft and rendered as data by clients.",
      "type": "object",
      "properties": {
        "craft_id": {
          "description": "Installed Craft identity.",
          "type": "string"
        },
        "harness": {
          "description": "Harness whose native formats this catalog describes.",
          "type": "string"
        },
        "native_metadata": {
          "description": "Bounded native JSON containing sources, versions, publishers and components.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "craft_id",
        "harness",
        "native_metadata"
      ]
    },
    "ExtensionConfirmation": {
      "description": "Exact native metadata and authority shown before any lifecycle mutation.",
      "type": "object",
      "properties": {
        "action": {
          "description": "Exactly one lifecycle operation.",
          "$ref": "#/$defs/ExtensionAction"
        },
        "catalog": {
          "description": "Catalog snapshot which the Craft must revalidate before execution.",
          "$ref": "#/$defs/ExtensionCatalog"
        },
        "extension_id": {
          "description": "Native identity, for example plugin@marketplace.",
          "type": "string"
        },
        "scope": {
          "description": "Native installation scope.",
          "$ref": "#/$defs/ExtensionScope"
        },
        "trust": {
          "description": "Same-user executable access accepted by the interactive client.",
          "$ref": "#/$defs/ExtensionTrust"
        }
      },
      "additionalProperties": false,
      "required": [
        "catalog",
        "extension_id",
        "action",
        "scope",
        "trust"
      ]
    },
    "ExtensionScope": {
      "description": "Native scopes currently admitted by the Plane.",
      "oneOf": [
        {
          "description": "The native Harness's user configuration on the selected Plane.",
          "type": "string",
          "const": "user"
        }
      ]
    },
    "ExtensionTrust": {
      "description": "Explicit consent to the native executable boundary, never GUI code.",
      "oneOf": [
        {
          "description": "Skills may direct tools; hooks, MCP servers and plugins execute as the user.",
          "type": "string",
          "const": "same_user_executable"
        }
      ]
    },
    "FileRevision": {
      "description": "Exact Git content and mode observed for one file.",
      "type": "object",
      "properties": {
        "mode": {
          "description": "Git mode, or `000000` when the file is missing.",
          "type": "string"
        },
        "object": {
          "description": "Git blob object, or all zeroes when the file is missing.",
          "type": "string"
        }
      },
      "required": [
        "object",
        "mode"
      ]
    },
    "FileTarget": {
      "description": "A registered root through which an ordinary file operation is addressed.",
      "oneOf": [
        {
          "description": "A registered Project's Local checkout.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "project"
            },
            "project_id": {
              "description": "Project identity.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "project_id"
          ]
        },
        {
          "description": "A Conversation-owned Workspace.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "workspace"
            },
            "workspace_id": {
              "description": "Workspace identity.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "workspace_id"
          ]
        }
      ]
    },
    "Name": {
      "description": "One resolved user-facing name and its authority.",
      "type": "object",
      "properties": {
        "source": {
          "description": "Authority that supplied `value`.",
          "$ref": "#/$defs/NameSource"
        },
        "value": {
          "description": "Original validated text; clients encode it for their render context.",
          "type": "string"
        }
      },
      "required": [
        "value",
        "source"
      ]
    },
    "NameSource": {
      "description": "Authority that supplied a current Conversation or Run name.",
      "oneOf": [
        {
          "description": "An interactive user's authoritative choice.",
          "type": "string",
          "const": "manual"
        },
        {
          "description": "A validated Utility-model result.",
          "type": "string",
          "const": "utility"
        },
        {
          "description": "A structured title supplied by the Harness through its Craft.",
          "type": "string",
          "const": "harness_native"
        },
        {
          "description": "Stable local text used without Utility work.",
          "type": "string",
          "const": "deterministic"
        }
      ]
    },
    "NegotiatedProtocol": {
      "description": "The immutable selection callers persist with the execution identity.",
      "type": "object",
      "properties": {
        "capabilities": {
          "description": "Intersection of understood capability names, sorted and deduplicated.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "family": {
          "description": "Independently negotiated contract.",
          "$ref": "#/$defs/ProtocolFamily"
        },
        "version": {
          "description": "Selected version; updates must retain support while it is active.",
          "$ref": "#/$defs/ProtocolVersion"
        }
      },
      "required": [
        "family",
        "version",
        "capabilities"
      ]
    },
    "NoVisaDestination": {
      "description": "One destination selected by the desktop, never by the Craft.",
      "type": "object",
      "properties": {
        "plane_id": {
          "description": "Expected paired Plane identity.",
          "type": "string",
          "format": "uuid"
        },
        "ssh_endpoint": {
          "description": "System SSH destination, resolved through the user's SSH configuration.",
          "type": "string"
        },
        "workspace_id": {
          "description": "Registered destination Workspace.",
          "type": "string",
          "format": "uuid"
        }
      },
      "additionalProperties": false,
      "required": [
        "plane_id",
        "workspace_id",
        "ssh_endpoint"
      ]
    },
    "NoVisaSelection": {
      "description": "Immutable origin and destination selection retained with the execution.",
      "type": "object",
      "properties": {
        "account_binding_id": {
          "description": "Native authentication stays on this Plane.",
          "type": "string",
          "format": "uuid"
        },
        "conversation_id": {
          "description": "Authoritative Conversation on the origin.",
          "type": "string",
          "format": "uuid"
        },
        "destinations": {
          "description": "Destinations the broker may contact.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/NoVisaDestination"
          }
        },
        "jet_equivalent": {
          "description": "Jet-provided equivalents, subject to destination authority and bounds.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "native_unavailable": {
          "description": "Native remote behavior that this mode cannot promise.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "origin_plane_id": {
          "description": "Native execution's Home Plane.",
          "type": "string",
          "format": "uuid"
        }
      },
      "required": [
        "jet_equivalent",
        "native_unavailable",
        "origin_plane_id",
        "account_binding_id",
        "conversation_id",
        "destinations"
      ]
    },
    "Presentation": {
      "description": "Presentation understood by every GUI; never executable UI code.",
      "oneOf": [
        {
          "description": "Plain text rendered without markup interpretation.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "text"
            },
            "text": {
              "description": "Human-visible text.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "text"
          ]
        },
        {
          "description": "Markdown rendered with the GUI's normal sanitization rules.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "markdown"
            },
            "text": {
              "description": "Human-visible Markdown.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "text"
          ]
        },
        {
          "description": "Named native actions routed back through authenticated Jet Commands.",
          "type": "object",
          "properties": {
            "actions": {
              "description": "Choices available for this native event.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/PresentationAction"
              }
            },
            "kind": {
              "type": "string",
              "const": "actions"
            }
          },
          "required": [
            "kind",
            "actions"
          ]
        }
      ]
    },
    "PresentationAction": {
      "description": "An action description grants no broker permission or approval authority.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Harness-defined identity scoped to this execution.",
          "type": "string"
        },
        "label": {
          "description": "Plain-text user-facing label.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "label"
      ]
    },
    "PresentationBlock": {
      "anyOf": [
        {
          "$ref": "#/$defs/Presentation"
        },
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "not": {
                "enum": [
                  "text",
                  "markdown",
                  "actions"
                ]
              }
            }
          },
          "required": [
            "kind"
          ]
        }
      ]
    },
    "ProtocolFamily": {
      "description": "Separately versioned contracts; sharing a codec never couples versions.",
      "oneOf": [
        {
          "description": "GUI to daemon protocol.",
          "type": "string",
          "const": "client"
        },
        {
          "description": "Harness adapter protocol.",
          "type": "string",
          "const": "craft"
        },
        {
          "description": "Execution helper protocol.",
          "type": "string",
          "const": "helper"
        },
        {
          "description": "Craft specification schema.",
          "type": "string",
          "const": "specification"
        }
      ]
    },
    "ProtocolOffer": {
      "description": "A peer's highest minor for each supported major, plus capability names.",
      "type": "object",
      "properties": {
        "capabilities": {
          "description": "Optional capabilities; unknown names confer no authority.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "family": {
          "description": "Contract to which this offer applies.",
          "$ref": "#/$defs/ProtocolFamily"
        },
        "versions": {
          "description": "One entry per major; every minor from zero through this minor works.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ProtocolVersion"
          }
        }
      },
      "required": [
        "family",
        "versions"
      ]
    },
    "ProtocolVersion": {
      "description": "A major and its additive minor, independent of product version.",
      "type": "object",
      "properties": {
        "major": {
          "description": "Incompatible schema generation, starting at one.",
          "type": "integer",
          "format": "uint32",
          "maximum": 4294967295,
          "minimum": 0
        },
        "minor": {
          "description": "Additive schema generation, starting at zero.",
          "type": "integer",
          "format": "uint32",
          "maximum": 4294967295,
          "minimum": 0
        }
      },
      "required": [
        "major",
        "minor"
      ]
    },
    "RecoveryAction": {
      "description": "Structured action a client may take to recover from an error.",
      "oneOf": [
        {
          "description": "Refresh a file before preparing another direct edit.",
          "type": "object",
          "properties": {
            "current_revision": {
              "description": "Exact file state now authoritative.",
              "$ref": "#/$defs/FileRevision"
            },
            "path": {
              "description": "Validated path relative to that root.",
              "type": "string"
            },
            "target": {
              "description": "Registered root to refresh.",
              "$ref": "#/$defs/FileTarget"
            },
            "type": {
              "type": "string",
              "const": "refresh_file"
            }
          },
          "required": [
            "type",
            "target",
            "path",
            "current_revision"
          ]
        },
        {
          "description": "Refresh current Conversation state before preparing another Command.",
          "type": "object",
          "properties": {
            "conversation_id": {
              "description": "Conversation whose current state should be queried.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "refresh_conversation"
            }
          },
          "required": [
            "type",
            "conversation_id"
          ]
        },
        {
          "description": "Refresh current Run state before preparing another Command.",
          "type": "object",
          "properties": {
            "run_id": {
              "description": "Run whose current state should be queried.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "refresh_run"
            }
          },
          "required": [
            "type",
            "run_id"
          ]
        },
        {
          "description": "Reconnect and resume the semantic Event stream after this cursor.",
          "type": "object",
          "properties": {
            "after": {
              "description": "Last Event cursor the disconnected client received completely.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "type": {
              "type": "string",
              "const": "resume_events"
            }
          },
          "required": [
            "type",
            "after"
          ]
        }
      ]
    },
    "RemoteEnvironment": {
      "description": "Explicit destination environment change; absent values remove a variable.",
      "type": "object",
      "properties": {
        "name": {
          "description": "Variable name, without equals signs or NULs.",
          "type": "string"
        },
        "value": {
          "description": "Explicit value or removal.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "RemoteGitOperation": {
      "description": "Read-only Git operations with bounded output.",
      "oneOf": [
        {
          "description": "Porcelain v1 status, including untracked paths.",
          "type": "string",
          "const": "status"
        },
        {
          "description": "Working-tree diff without external diff or text conversion commands.",
          "type": "string",
          "const": "diff"
        }
      ]
    },
    "RemoteToolAction": {
      "description": "Operations available through the remote broker.",
      "oneOf": [
        {
          "description": "One reviewed input batch in a real PTY, closed after the batch exits.",
          "type": "object",
          "properties": {
            "columns": {
              "description": "Fixed terminal dimensions.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "directory": {
              "description": "Workspace-relative cwd; empty selects its root.",
              "type": "string"
            },
            "input": {
              "description": "Exact terminal input, shown in destination review.",
              "type": "string"
            },
            "rows": {
              "description": "Fixed terminal dimensions.",
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "type": {
              "type": "string",
              "const": "terminal"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "directory",
            "input",
            "rows",
            "columns"
          ]
        },
        {
          "description": "Argument-array process invocation, reviewed exactly once at destination.",
          "type": "object",
          "properties": {
            "arguments": {
              "description": "Executable followed by literal arguments; never a shell command string.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "directory": {
              "description": "Workspace-relative cwd; empty selects its root.",
              "type": "string"
            },
            "environment": {
              "description": "Explicit changes to a minimal environment.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/RemoteEnvironment"
              }
            },
            "type": {
              "type": "string",
              "const": "process"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "arguments",
            "directory",
            "environment"
          ]
        },
        {
          "description": "Read-only Git inspection with fixed arguments and hooks disabled.",
          "type": "object",
          "properties": {
            "operation": {
              "description": "Closed inspection vocabulary.",
              "$ref": "#/$defs/RemoteGitOperation"
            },
            "type": {
              "type": "string",
              "const": "git"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "operation"
          ]
        },
        {
          "description": "Visibly labelled shell syntax, requiring exact destination review.",
          "type": "object",
          "properties": {
            "directory": {
              "description": "Workspace-relative directory; empty selects the Workspace root.",
              "type": "string"
            },
            "environment": {
              "description": "Explicit environment changes, applied to a minimal environment.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/RemoteEnvironment"
              }
            },
            "script": {
              "description": "Shell source, never interpolated into SSH or a process argument array.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "shell"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "directory",
            "environment",
            "script"
          ]
        },
        {
          "description": "Atomically replace at most 64 KiB inside the Workspace.",
          "type": "object",
          "properties": {
            "content": {
              "description": "New UTF-8 content.",
              "type": "string"
            },
            "path": {
              "description": "Workspace-relative file path.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "write_file"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "path",
            "content"
          ]
        },
        {
          "description": "Read at most 64 KiB of UTF-8 file content.",
          "type": "object",
          "properties": {
            "path": {
              "description": "Validated Workspace-relative path.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "read_file"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "path"
          ]
        }
      ]
    },
    "RemoteToolOutcome": {
      "description": "A remote failure belongs to this call, not the origin Run or another Plane.",
      "oneOf": [
        {
          "description": "The destination returned a result, possibly awaiting exact review.",
          "type": "object",
          "properties": {
            "result": {
              "description": "Destination outcome.",
              "$ref": "#/$defs/RemoteToolResult"
            },
            "type": {
              "type": "string",
              "const": "completed"
            }
          },
          "required": [
            "type",
            "result"
          ]
        },
        {
          "description": "The request failed or has an unknown outcome; never retry mutations blindly.",
          "type": "object",
          "properties": {
            "error": {
              "description": "Stable, bounded error without transport secrets.",
              "$ref": "#/$defs/WireError"
            },
            "type": {
              "type": "string",
              "const": "failed"
            }
          },
          "required": [
            "type",
            "error"
          ]
        }
      ]
    },
    "RemoteToolResult": {
      "description": "Bounded response from one destination operation.",
      "oneOf": [
        {
          "description": "This action has not executed and needs a destination user decision.",
          "type": "object",
          "properties": {
            "operation_id": {
              "description": "Stable identity of the action to inspect and review.",
              "type": "string",
              "format": "uuid"
            },
            "type": {
              "type": "string",
              "const": "approval_required"
            }
          },
          "required": [
            "type",
            "operation_id"
          ]
        },
        {
          "description": "Bounded process output after native exit.",
          "type": "object",
          "properties": {
            "exit_code": {
              "description": "Native exit status, absent when terminated by a signal.",
              "type": [
                "integer",
                "null"
              ],
              "format": "int32"
            },
            "stderr": {
              "description": "At most 64 KiB, rendered with replacement for invalid UTF-8.",
              "type": "string"
            },
            "stdout": {
              "description": "At most 64 KiB, rendered with replacement for invalid UTF-8.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "process"
            }
          },
          "required": [
            "type",
            "stdout",
            "stderr"
          ]
        },
        {
          "description": "The file replacement and directory sync completed.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "written"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "description": "UTF-8 content inside the selected Workspace.",
          "type": "object",
          "properties": {
            "content": {
              "description": "At most 64 KiB.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "file"
            }
          },
          "required": [
            "type",
            "content"
          ]
        }
      ]
    },
    "RestartMetadata": {
      "description": "Stable metadata explaining why a snapshot must be restarted.",
      "oneOf": [
        {
          "description": "Required Event replay is no longer retained.",
          "type": "object",
          "properties": {
            "current_snapshot_revision": {
              "description": "Current Event high-water cursor for a replacement snapshot.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "minimum_available_cursor": {
              "description": "Oldest cursor from which continuous replay remains possible.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "reason": {
              "type": "string",
              "const": "cursor_expired"
            }
          },
          "required": [
            "reason",
            "minimum_available_cursor",
            "current_snapshot_revision"
          ]
        },
        {
          "description": "The supplied Event cursor belongs to a later or different Plane.",
          "type": "object",
          "properties": {
            "current_snapshot_revision": {
              "description": "Current Event high-water cursor for the replacement snapshot.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "reason": {
              "type": "string",
              "const": "cursor_ahead"
            }
          },
          "required": [
            "reason",
            "current_snapshot_revision"
          ]
        },
        {
          "description": "A later page no longer belongs to the current projection state.",
          "type": "object",
          "properties": {
            "current_snapshot_revision": {
              "description": "Current Event high-water cursor for the replacement first page.",
              "type": "string",
              "pattern": "^(0|[1-9][0-9]*)$"
            },
            "reason": {
              "type": "string",
              "const": "pagination_stale"
            }
          },
          "required": [
            "reason",
            "current_snapshot_revision"
          ]
        }
      ]
    },
    "RetentionPolicy": {
      "description": "Whether Jet keeps a Conversation after its final Run.",
      "oneOf": [
        {
          "description": "Keep the Conversation and its history. The default.",
          "type": "string",
          "const": "retain"
        },
        {
          "description": "Forget the Conversation once it has no live Run and no other\nprotected state.",
          "type": "string",
          "const": "forget_after_final_run"
        }
      ]
    },
    "RevisionConflict": {
      "description": "Structured state returned when a Revision precondition is stale.",
      "type": "object",
      "properties": {
        "current_revision": {
          "description": "Revision that is authoritative now, carried as a decimal string\n(ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "safe_state": {
          "description": "Safe current state with which the caller can refresh.",
          "$ref": "#/$defs/ConflictState"
        }
      },
      "required": [
        "current_revision",
        "safe_state"
      ]
    },
    "Run": {
      "description": "One Run of a Conversation.",
      "type": "object",
      "properties": {
        "conversation_id": {
          "description": "The Conversation it executes.",
          "type": "string",
          "format": "uuid"
        },
        "created_at_unix_ms": {
          "description": "When it was created, in signed Unix milliseconds.",
          "type": "integer",
          "format": "int64"
        },
        "ended_at_unix_ms": {
          "description": "When it reached a terminal state, if it has.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        },
        "lifecycle": {
          "description": "Current lifecycle state.",
          "$ref": "#/$defs/RunLifecycle"
        },
        "name": {
          "description": "Resolved name and its authority. Absent before protocol minor 20.",
          "anyOf": [
            {
              "$ref": "#/$defs/Name"
            },
            {
              "type": "null"
            }
          ]
        },
        "revision": {
          "description": "Monotonic version used by conflict-sensitive Commands, carried as a\ndecimal string (ADR-0089).",
          "type": "string",
          "pattern": "^(0|[1-9][0-9]*)$"
        },
        "run_id": {
          "description": "Durable identity.",
          "type": "string",
          "format": "uuid"
        }
      },
      "required": [
        "run_id",
        "conversation_id",
        "revision",
        "lifecycle",
        "created_at_unix_ms"
      ]
    },
    "RunActivity": {
      "description": "Why an active Run is working or waiting.",
      "oneOf": [
        {
          "description": "Performing work.",
          "type": "string",
          "const": "working"
        },
        {
          "description": "Needs user input.",
          "type": "string",
          "const": "waiting_for_user"
        },
        {
          "description": "Needs an approval decision.",
          "type": "string",
          "const": "waiting_for_approval"
        },
        {
          "description": "Needs authentication.",
          "type": "string",
          "const": "waiting_for_auth"
        },
        {
          "description": "Needs available quota.",
          "type": "string",
          "const": "waiting_for_quota"
        },
        {
          "description": "Reconnecting to its execution.",
          "type": "string",
          "const": "reconnecting"
        }
      ]
    },
    "RunLifecycle": {
      "description": "Mutually exclusive lifecycle state of one Run.",
      "oneOf": [
        {
          "description": "Recorded but not yet launching.",
          "type": "string",
          "const": "created"
        },
        {
          "description": "Launching its Harness.",
          "type": "string",
          "const": "starting"
        },
        {
          "description": "Executing.",
          "type": "string",
          "const": "active"
        },
        {
          "description": "Ending gracefully.",
          "type": "string",
          "const": "stopping"
        },
        {
          "description": "Terminal: finished its work.",
          "type": "string",
          "const": "completed"
        },
        {
          "description": "Terminal: ended with an error.",
          "type": "string",
          "const": "failed"
        },
        {
          "description": "Terminal: ended on request.",
          "type": "string",
          "const": "canceled"
        },
        {
          "description": "Terminal: its execution can no longer be observed.",
          "type": "string",
          "const": "lost"
        }
      ]
    },
    "TurnOutcome": {
      "description": "Terminal outcome of one turn, independent of Run lifecycle.",
      "oneOf": [
        {
          "description": "The Harness completed its turn.",
          "type": "string",
          "const": "completed"
        },
        {
          "description": "The turn ended early with partial changes preserved.",
          "type": "string",
          "const": "interrupted"
        }
      ]
    },
    "UtilityInput": {
      "description": "Allowlisted content; authentication and job metadata are never part of this input.",
      "oneOf": [
        {
          "description": "Naming context, excluding native output.",
          "type": "object",
          "properties": {
            "opening_context": {
              "description": "At most 4096 UTF-8 bytes.",
              "type": "string"
            },
            "purpose": {
              "type": "string",
              "const": "naming"
            },
            "title": {
              "description": "At most 256 UTF-8 bytes.",
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "purpose",
            "title",
            "opening_context"
          ]
        },
        {
          "description": "One immutable turn checkpoint.",
          "type": "object",
          "properties": {
            "instructions": {
              "description": "At most 2048 UTF-8 bytes of Plane guidance.",
              "type": "string"
            },
            "patch": {
              "description": "At most 16384 UTF-8 bytes.",
              "type": "string"
            },
            "purpose": {
              "type": "string",
              "const": "git_text"
            }
          },
          "additionalProperties": false,
          "required": [
            "purpose",
            "patch",
            "instructions"
          ]
        },
        {
          "description": "Unapproved natural-language rule.",
          "type": "object",
          "properties": {
            "prompt": {
              "description": "At most 4096 UTF-8 bytes.",
              "type": "string"
            },
            "purpose": {
              "type": "string",
              "const": "autodelete"
            }
          },
          "additionalProperties": false,
          "required": [
            "purpose",
            "prompt"
          ]
        }
      ]
    },
    "WireError": {
      "description": "Stable error body carried by every failed reply.",
      "type": "object",
      "properties": {
        "category": {
          "description": "Category the client may branch on.",
          "$ref": "#/$defs/ErrorCategory"
        },
        "code": {
          "description": "Domain-specific code such as `protocol.unsupported_version`.",
          "type": "string"
        },
        "message": {
          "description": "Safe human-readable description free of native error strings.",
          "type": "string"
        },
        "recovery_actions": {
          "description": "Structured actions that can safely recover from this error.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/RecoveryAction"
          }
        },
        "restart": {
          "description": "Structured metadata when a stale read must restart.",
          "anyOf": [
            {
              "$ref": "#/$defs/RestartMetadata"
            },
            {
              "type": "null"
            }
          ]
        },
        "retryable": {
          "description": "Whether repeating the same request may succeed.",
          "type": "boolean"
        },
        "revision_conflict": {
          "description": "Current resource state when an expected Revision was stale.",
          "anyOf": [
            {
              "$ref": "#/$defs/RevisionConflict"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "category",
        "code",
        "retryable",
        "message"
      ]
    },
    "WorkingTree": {
      "description": "Where a Conversation does its work.",
      "oneOf": [
        {
          "description": "In no Project. Nothing on disk belongs to the Conversation.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "no_project"
            }
          },
          "required": [
            "kind"
          ]
        },
        {
          "description": "In a managed Workspace of a Project, isolated from every other\nConversation. The Workspace itself comes with the Conversation\nsnapshot.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "workspace"
            },
            "project_id": {
              "description": "The Project the Workspace was created from.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "project_id"
          ]
        },
        {
          "description": "In the Project's own Local checkout, without isolation. The Plane\nadmits one live managed Run there at a time and cannot lock the\nprocesses outside its management.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "local_checkout"
            },
            "project_id": {
              "description": "The Project whose checkout it works in.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "kind",
            "project_id"
          ]
        }
      ]
    }
  }
}
